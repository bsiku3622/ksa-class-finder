# Tasks

- [x] 모바일 반응형 디자인 적용: BottomNav, 레이아웃 조정, 텍스트/패딩 반응형
- [x] JWT 인증 시스템 구현: SQLite User/Session 테이블, 기기당 최대 2세션, admin CLI
- [x] 세션 기반 인증으로 전환: JWT 제거, 1계정 1세션, session_token DB 조회
- [x] 프론트엔드 로그인 UI 구현: LoginPage, 인증 상태 관리, 로그아웃 버튼 (PC/모바일)
- [x] 배포 환경 분리: VITE_API_BASE_URL 환경변수, axios 인스턴스, 백엔드 CORS 설정
- [x] Admin 앱 구현: 사용자/세션/데이터 관리 UI + 백엔드 admin 엔드포인트
- [x] Browse/Settings 탭 재편: Students+Teachers→Browse, 가이드북 포함 Settings 탭 추가
- [x] 과목 별칭(alias) 시스템: SubjectAlias 모델, admin CRUD 엔드포인트, 검색 엔진 통합, Admin UI
- [x] Data Management 아코디언: 학생 이름 편집, 교사 이름 변경(전체 일괄), Subject Aliases 통합
- [x] 백엔드 GET / 쿼리 개선: N+1 → joinedload/selectinload로 쿼리 수 최소화
- [x] Sync 결과 상세: parser_run 완료 후 추가/수정/삭제 레코드 수 반환 및 표시
- [x] 검색 히스토리: 최근 검색어 localStorage 저장, 검색창 포커스 시 드롭다운 표시
- [x] URL 공유: 현재 검색 URL 클립보드 복사 버튼
- [x] 빈 교실 → 검색 연동: Rooms 페이지 교실 클릭 시 해당 강의실 Search로 이동
- [x] 교사 부하 시각화: Analysis 페이지에 교사별 주당 담당 교시 수 차트 추가
- [x] 충돌 감지: student:A & student:B 검색 시 공통 수업 외 시간 충돌 수업도 표시
- [x] 번들 사이즈 개선: dynamic import로 페이지별 코드 스플리팅 (HeroUI 청크 분리)
- [x] 로컬 HTTPS: mkcert + Vite https 설정
- [x] 가이드 문서 정비: 전체 .guide.md 및 CLAUDE.md 현재 코드 기준 최신화
- [x] 효율성 수정: Rate Limiter 메모리 누수 정리, chosung 캐싱, AnalysisPage useMemo 5→2 통합

## Security Audit
- [x] [SEC-01] SQL Injection: ORM 파라미터화 쿼리 검증 + LIKE wildcard 분석 → 안전 (ORM 파라미터화, LIKE wildcard는 admin 전용으로 실질적 위협 없음)
- [x] [SEC-02] Command Injection: subprocess 실행 코드 분석 및 검증 → 안전 (하드코딩 명령, shell=False, user input 미전달)
- [x] [SEC-03] XSS: React 프론트엔드 + API 응답 출력 분석 → 안전 (dangerouslySetInnerHTML 없음, React 자동 이스케이프, 백엔드 JSON-only)
- [x] [SEC-04] CSRF: Bearer 토큰 인증 구조 취약점 분석 → 안전 (Bearer 헤더 방식, 쿠키 미사용, 브라우저 자동 전송 불가)
- [x] [SEC-05] 인증 우회: 모든 endpoint 보호 여부 검증 → 안전 (모든 endpoint Depends(get_current_user/admin), 만료 세션 자동 삭제)
- [x] [SEC-06] IDOR/인가 취약점: resource ownership 체크 검증 → 안전 (세션 삭제 ownership 체크, admin role 체크, 자기 자신 보호)
- [x] [SEC-07] 입력값 검증: username/password/aliases 길이·형식 제한 추가 → 수정 (max_length, pattern 검증, device_type Literal, q max_length=100)
- [x] [SEC-08] 파일 업로드 취약점: 기능 존재 여부 확인 → 해당없음 (파일 업로드 endpoint 없음)
- [x] [SEC-09] 비밀정보 노출: sync stderr 클라이언트 노출 수정 → 수정 (stderr → server log only, 클라이언트에 generic 메시지)
- [x] [SEC-10] 보안 헤더: X-Content-Type-Options, X-Frame-Options, Referrer-Policy 추가 → 수정 (SecurityHeadersMiddleware 추가, HSTS env var 조건부)
- [x] [SEC-11] Rate Limiting: /auth/login 브루트포스 방어 구현 → 수정 (IP당 60초 10회 제한, 성공 시 카운터 초기화, 429 반환)
- [x] [SEC-12] 비즈니스 로직: sync 엔드포인트 어뷰즈 가능성 분석 → 안전 (admin 전용, 300초 timeout, subprocess 단일 실행)
- [x] [SEC-13] Race Condition: 동시 로그인 세션 생성 분석 → SQLite 직렬화로 안전. PostgreSQL 이전 시 FOR UPDATE 락 필요
- [x] [SEC-14] Dependency 취약점: requirements.txt 버전 미고정 수정 → 수정 (>=최소버전 명시, bcrypt>=4.0.0, python-multipart>=0.0.12)
- [x] [SEC-15] 로그/에러 처리: 민감정보 로그 기록 여부 확인 → 안전 (user enumeration 방지, SEC-09에서 stderr 노출 수정)

## 2026-07-29 — KEIS API 전환 + 학기별 데이터
- [x] parser.py: KEIS restapi 기준 재작성 (kyosi 필드로 교시 결정, 강의실 "배정중" 대응)
- [x] parser_run.py: 학기별 수집 (--year/--semester), 원자적 교체, 재시도·과반 실패 가드
- [x] models.py: Class에 year/semester 추가, UniqueConstraint 학기 단위로 확장
- [x] migrations.py: classes 테이블 재생성 마이그레이션 (기존 데이터 2026-1 지정, 멱등)
- [x] terms.py: 학기 해석 유틸 (current_term / list_terms / resolve_term)
- [x] main.py: GET / 학기 파라미터, GET /terms 추가
- [x] admin_router.py: teachers/subjects 학기 필터, /admin/terms, /admin/sync 학기 지정
- [x] 프론트: TermSwitcher 컴포넌트 + 학기별 캐시 분리 + 선택 학기 보존
- [x] 서버 배포 및 2026-2 데이터 수집

## 2026-07-29 — 수강 변경 탐색(Trade) + 유사도 검색
- [x] tradeEngine.ts: 유지/이동/드랍/추가를 하나의 백트래킹 탐색으로 통합
- [x] TradePage: 학생 선택 → 과목별 액션 지정 → 조합 결과 + 시간표 미리보기
- [x] SectionsTimetable: 한 과목의 전 분반을 한 그리드에 표시, 충돌 분반 회색 처리
- [x] 드랍 과목 빨간색 표시 + 목록 하단 정렬
- [x] 추가 과목을 My Subjects 리스트에 편입, 분반 고정(자동/특정 분반) 지원
- [x] 드랍으로 열리는 과목 목록(Opened by Drop)
- [x] searchEngine: fuzzyMatch 도입 — "정3" → "정보과학3" (별칭 등록 불필요)
- [x] features.ts: TRADE_FEATURE 플래그로 기간 종료 시 일괄 차단
- [x] 트레이드 계획 자동 저장/복원 (localStorage)
- [x] 교환 상대 찾기 — 서로 분반을 맞바꿀 수 있는 학생 목록
- [x] 시간표 강조 칸 채움 진하게 + 테두리
- [x] 검색: "체4/5" → 체육4 5분반 (분반 지정에도 유사도 적용)
- [x] 충돌 분반 핑크 표시 + 강제 선택 시 충돌 과목 주황 하이라이트
- [x] 드랍 반영 누락 버그 수정 (activeSchedule 기준으로 충돌 판정)
- [x] 과목·분반 클릭 시 "체육4(4)/4" 잘못된 검색어 생성 버그 수정
- [x] 분반 검색 콤마 목록 지원 (체육4/2,3)
- [x] 분반 이동 목표를 직접 선택 (moveTargets) + 즉시 시간표 반영
- [x] 이동 = 파랑 색상 분리, 선택 버튼 색을 카드에 맞춤
- [x] 이동·추가 간 상호 충돌 판정 누락 수정 + 자동 조합 미리보기
- [x] 과목별 학점 데이터 도입 (SweetZamong → SubjectCredit) + 트레이드 학점 집계
- [x] 분반 교환 구인 글 템플릿 + 복사 버튼
- [x] Sweet Zamong 통합 — 교육과정 카탈로그 + 선수관계 그래프 + 졸업 요건 진척도 (PlanPage)
- [x] 성적(평어) 입력 + GPA, 계획·이수 기록을 계정별 DB 저장으로 전환
- [x] 계정에 학번 등록 (학번+이름 대조) — Plan은 본인 고정, 미등록 시 모달 안내
- [x] 한 학번 = 한 계정 (DB 유니크 제약으로 동시 등록까지 차단)
- [x] 22~26학번 전체 명단 확보 (외국인 포함 677명) + 2024~2025 과거 학기 수집
- [x] 과목 구조를 departments/courses/subjects/classes 4층으로 분리 (EC/KC 구분, 영문명 분리, 별칭 폐기)
- [x] 교육과정 그래프를 Browse로 이동 (전체 보기 + 선택 강조), Plan → Zamong 개명
- [x] 학교 구글 계정으로 학번 확인 (@ksa.hs.kr) — 닫을 수 없는 모달로 강제
- [x] 구글 로그인 경로 제거 — 로그인은 아이디·비밀번호만 (초대제 유지)
- [x] 학기 선택을 드롭다운으로
- [x] 4층 구조 정리 이후 문서·프론트 뒷정리 (고아 파일 3개 삭제, 가이드 6개 수정)
- [x] 권한을 role 3단계(user/manager/admin)로 이관
- [x] 연간 학사일정 PDF 파서 + 달력 화면 + 개인 일정·반복 + 일정 제안 승인 흐름
- [x] [bench] `frontend/` → `bench-frontend/` 복사 (ksa-bench 시작점, 포트·이름·환경변수 분리)
- [x] [bench] 백엔드 ASGI 진입점 분리 — bench 프로세스에는 전체 제공 라우터를 등록하지 않음
- [x] [bench] 학생 매핑을 벌크 응답에서 분리하고 서버 질의로 전환 (한 번에 한 명)
- [x] [bench] 검색 개편 — 다중 검색 제거, 후보 목록/개인 상세 분리, `/browse` 학생 목록 제거
- [x] [bench] Trade 복원 (분반 명단 사용) — 매칭 방식 재설계는 나중으로
- [x] [bench] 친구 시스템 (단방향 등록 + 공강 격자)
- [x] 교시별 시각표 도입 (`backend/periods.py`) + `GET /periods`
- [x] 친구(단방향) + 공강 격자 + "지금 공강" — **class-explorer·ksa-bench 공통**
- [x] 백엔드 단일 서버로 통합 — 두 프론트가 같은 API 를 씁니다
- [x] [bench] 수업 데이터 localStorage 캐시 제거 (명단이 브라우저에 남지 않도록)
- [x] 저녁(17:30~19:00) 반영 — 수업과 동시 진행이라 교시 시각은 그대로
- [x] 홈 화면 — 지금 교시·가야 할 교실·오늘 시간표·방학 메시지·공강인 친구 (`/` → 홈, 검색은 `/search`)
- [x] Friends 탭 제거 → 홈 위젯 + 모달 (Timetable Compare 가 이미 있어서)
- [x] 급식 — DB(`meal_menus`)에 쌓고, 없을 때만 ksain API 를 부릅니다
- [ ] ksain 게시글 API 로 학사일정 보강 (ksalendar 참고)
- [ ] [bench] 관리 화면 — `/admin/*` 통째로가 아니라 안전한 것만 골라 새로
- [ ] [bench] 배포 — systemd 유닛 2번째, nginx 블록, Netlify 사이트 추가
- [x] 친구 모달 개편 — 검색기 하나로 (등록된 사람 필터 → 없으면 추가), 모달 위치·인풋 크기 정리
- [x] 급식을 DB 에 쌓기 (`meal_menus`) — 없을 때만 ksain API 로 받아 저장
- [x] 급식 날짜 넘기기 (`GET /meal`) — 어제·내일, 앞뒤 31일까지
- [x] 시각을 KST 로 고정 (`periods.now()`) — 배포 서버가 UTC 라 교시가 9시간 어긋났습니다
- [x] 홈에 Trade 배너 (수강 변경 기간에만)
- [x] 방학 화면을 학기 중과 같은 레이아웃으로 (큰 카드 대신 연한 한 줄)
- [x] 홈 재배치 — 시계 카드 제거, Now·급식 2열, Trade 배너를 맨 위로(핑크)
- [x] 급식을 끼니 토글로 (아침·점심·저녁 버튼 + 날짜 화살표)
- [x] 친구 관리를 홈 모달에서 Browse 탭으로 이동
- [x] 급식 로딩 스피너 · `No Data Found` · 2열 높이 맞춤
- [x] HeroUI 스피너 교체 (`RetroSpinner`) — Tailwind v4 가 node_modules 클래스를 안 만듭니다
- [x] 스피너를 도는 원으로 (paper-ui 이식)
- [x] 트레이드 — 드랍한 과목은 시간표에서 제거
- [x] 트레이드 — 추가 후보에서 이수·선수 미충족 과목 제외, 검색에서는 회색으로 뒤에
- [x] 트레이드 — 과목 호버 시 시간표에서 그 과목만 남기기
- [x] 컴포넌트 인벤토리 페이지 (`/inventory`, dev 전용) — funky-ui 토큰 재설계용 표본집
- [x] 홈 — 방학·주말·공휴일엔 오늘 시간표를 아예 비우기 (학사일정 기준)
- [x] 홈 UI 전면 개편 — 하루를 시간 축 위에 그리는 DAY RULER + 지금 캐럿
- [x] 홈 — 과목 색을 학과로 (`getDepartmentColor`, 시안·핑크 제외 11색)
- [x] 홈 상태 미리보기 (dev 전용) — 방학에 붙잡혀 수업 중 화면을 못 보던 문제
- [x] 홈 — 가로 자를 걷어내고 Today 목록에 세로 시간 축으로 합치기
- [x] 홈 색 정리 — 지금만 핑크, 나머지 화이트 (학과는 띠로만) · 급식 끼니색 · 방학 막대 계절색
- [x] 홈 — 히어로 카드 제거, 날짜·시계를 Today 카드 머리로 (목록과 같은 말을 반복하고 있었음)
- [x] 홈 — 2열 유지 + 가로 자 복구 (Today 안에), 급식 폭 2/3 (2.3fr : 1fr)
- [x] 홈 배치 V2 (낮고 가로로 긴 판본) — 지금·학사일정 | 지금 중심 스크롤 목록. V1 과 DEV 바에서 전환
- [x] 홈 배치 V2 확정 — 프로덕션은 V2, V1 은 개발 전용으로 남김
- [x] 홈 문구 사용자 판본 반영 (JSON 왕복)
- [x] 넘치는 한 줄을 `…` 대신 좌우로 훑게 (`MarqueeText`) — Trade 배너
- [x] 급식 메뉴 글씨 13 → 15px
- [x] 사이드바 — System Status 맨 아래 고정 + 메뉴만 스크롤, Admin 아래 줄 제거
- [x] 모바일 하단 바 — 버튼 5개(자주 여는 넷 + 햄버거)
- [x] 메뉴 순서 재배치 (매일 여는 넷 → 남을 보는 것 → 내 것) + `lib/navigation.ts` 로 한 곳에서 정의
- [x] 모바일 헤더 — 로고·학기 선택 둘만, 로그아웃은 More 안으로 (두 번 눌러야 실행)
- [x] More 판을 오른쪽 구석에 (`w-52`), 버튼 크기 축소
- [x] `.env` git 추적 해제 + `.gitignore` 에 추가, `.env.example` 로 대체
- [x] 문서의 색 토큰이 코드와 어긋난 것 정리 — 값은 `index.css` 한 곳에서만
- [x] 자몽 전면 재개편 — 학교 배포본 Zamong 워크북의 UX 이식 (카드에서 학기·평어를 직접 고름)
- [x] 교육과정 데이터 — 과목 분류(핵심/심화/AP/특강/융합) + 심화필수 표시를 워크북에서 추출
- [x] 이수 기록에 학기·EC 여부 추가 (`CourseGrade.term` / `is_ec`)
- [x] 자몽에서 수강 이력 완전 분리 — 첫 진입 밑칠 한 번만 쓰고 화면에서는 걷어냄
- [x] `import_curriculum` 이 이수 기록을 지우던 것 — 과목 이름으로 이관하게
- [x] 자몽 로드맵 탭 (1~8학기 + 계절 + EC) — 수강 내역 탭 대체
- [x] 자몽 — 로드맵에서 드래그로 학기 이동
- [x] 자몽 — Zamong 엑셀 업로드 (서버 파싱, 첫 설정 배너 + 상시 버튼)
- [x] 응답 캐싱 (`lib/cache.ts`) — 교육과정·교시·급식 + 같은 키 요청 묶기
- [x] 자몽·브라우즈 — 타 학과 선수는 판에 안 그리고 카드에 글로
- [x] 자몽 카드 셀 색 정상화 (검은 채움 → 흰 바탕 + 테두리)
- [x] 동시수강 과목(이론↔실험)을 같은 열에 나란히 배치 — 자몽·브라우즈 공통
- [x] 불러오기 실패 시 빈 상태가 저장돼 기록을 지우던 문제 (자몽·트레이드)
- [x] 자몽 UI 확대 (카드 208×136, 입력칸 28px) + 셀렉트 화살표 · 정렬 정리
- [x] 로드맵 줄에서 검색으로 과목 넣기 + 학기별 학점 표시
- [x] 자몽 표 셀 높이 통일 + 학기별 정렬(학과 → 선수 깊이 → 가나다) + 카드 무게 완화
- [x] 달력 글씨 크기 정상화
- [x] 학사 사이트(my.ksa.hs.kr) 연동 조사 — 보류 (리포트: ~/Reports/2026-08-10_16-37-…)
- [x] 재수집이 분반 id를 새로 매기던 문제 — Class 를 키로 재사용(upsert)
- [x] 홈에 내 주간 시간표 위젯 (`GET /home` 에 week 추가 + WeekTimetable)
- [x] 배포본에 학교 계정 연결 버튼이 없던 것 — Netlify 환경변수 등록 + 없을 때 안내 표시
- [x] 구글 학번 확인을 리다이렉트 방식으로 — 모바일에서 팝업이 막혀 버튼이 안 눌리던 것
- [x] `/auth/google` 흰 화면 — vite base 를 절대 경로로
- [x] 홈 주간 시간표 재디자인 — 칸을 칩으로, 시작 교시 정렬, 점심·저녁 구멍 표시
- [x] 다중 기기 로그인 — 1계정 1세션 해제(상한 5) + 기기 목록·폐기 화면
- [x] 안드로이드 앱 — 위젯(지금·급식) + 로그인
- [x] Admin 수집 이름을 KEIS 로 정정 + 학기 골라서 수집 + 수집 직전 DB 백업(아카이브)
- [x] 홈 레이아웃 V3 — 지금을 히어로로 올리고 자(DayRuler) 복귀, 일정·급식을 위젯 행으로
- [x] 홈 재설계 — 히어로/자 복귀, 학과색, 주간 격자 2열·PNG 내보내기
- [x] 홈에서 기존/트레이드 시간표 전환 — 정정 기간 + 저장된 계획이 있을 때만
- [x] 홈 시간표 전환을 기기에 기억 — 트레이드 계획을 기본값으로 둘 수 있게
- [x] 홈 맨 아래에 수강 과목 목록 (아코디언)
- [x] 오늘 목록에서 공강도 진행 중이면 강조
- [x] 주간 시간표 제목을 Timetable 로 + 칩의 과목·분반·교사를 검색 링크로
- [x] 오늘 목록에서도 과목·분반·교사 검색 링크
- [x] PNG 를 오늘·지금 강조 없이 찍는 체크박스
- [x] 그 체크박스를 `현재 요일 표시`(기본 켜짐)로 뒤집어 화면에서도 강조가 사라지게
- [x] 주간 시간표 모바일 대응 — 가로 스크롤 + 교시·요일 고정 + 오늘 열로 열기
- [x] 홈 과목 목록을 검색 아코디언(`SubjectAccordionItem`)으로 교체 — 전용 UI 제거
- [x] 시간표 머리 줄이 모바일에서 겹치던 것 — 줄바꿈 허용
- [x] 오늘 일정·급식 글자 크기를 가운데(13.5px)로, 히어로 날짜를 교사 줄과 같은 13px 로
- [x] 오늘 일정·급식 글자 굵기 통일 + 화면의 대제목을 전부 영어로
- [x] 검색 명단에서 안 맞는 사람 흐리게 하던 것 제거 — 찾는 사람만 통통 튀게
- [x] 학생 배지 호버 효과 제거 + 강조를 통통 튐에서 진동으로
- [x] 주간 격자 칩의 갭이 한쪽에만 보이던 것 — 네 변 모두 1px 로
- [x] 배지 진동에 기울기 추가 + 남은 호버 확대 제거
- [x] 연강 사이 쉬는시간이 공강으로 뜨던 것 — 쉬는시간 개념 추가 + 연강은 강의중 유지
- [x] 노트북을 덮었다 열면 홈이 오류 카드로 바뀌던 것 — 실패해도 화면 유지 + 깨어나면 즉시 갱신
- [x] 홈 주기 요청 제거 — 한 번 받고 프론트에서 계산, 깨어남·날짜 변경에만 재요청
- [x] 리팩토링 A — 연강·쉬는시간 판정을 `lib/schedule.ts` 하나로 모으기 (5곳 흩어진 임계값 제거)
- [x] 리팩토링 B — 세션 토큰·인증 헤더를 `lib/session.ts` 하나로 모으기 (10곳 하드코딩 제거)
- [x] 리팩토링 D — `searchEngine.ts` 의 `any` 32개 걷어내고 타입 붙이기
- [x] 데이터 버전 구조 — 수집 결과를 학기별 버전으로 쌓고 변경이 있을 때만 버전업
- [x] 캐시 무효화 — `/auth/me` 가 실어 준 버전과 다르면 학기 캐시 폐기 (전원 대상)
- [x] 수집 변경 요약 — 직전 버전과의 차이를 Admin 화면에 표시
- [x] 버전 이력 뷰어 — 회차별 요약과 변경 내역 조회
- [x] 서버 옛 백업 파일에 담긴 데이터 회차 표시 + 백업 위치를 DB 옆으로
- [x] HeroUI 제거 — Tooltip 자체 구현, 나머지 5개는 소자로 교체 (JS 200KB → 97KB)
- [x] `/browse` 가상 스크롤 — 학생 677명 한 번에 그리던 것 (DOM 7,397 노드)
- [x] Data Versions 에서 학기 선택 — 지금은 수집 대상 학기에 묶여 있음
- [x] 옛 백업에서 과거 회차 복원 — 2026-1 추가수강신청 전 상태 등
- [x] 모바일 전 페이지 점검 — 잘려서 못 누르던 곳 정리 + admin·trade 주소 직접 진입 복구
- [x] 시연용 계정 — 구글 연동 없이 들어와 내 학번으로 보이는 계정을 admin 에서 생성
- [x] 홈에서 수업 없는 날 빈 카드가 검은 줄로 남던 것
- [x] 공개 소개 화면 — 로그인 앞에 두고 `/` 를 여기로 (SEO 메타·robots 포함)
- [x] 개인정보처리방침 + 연락처 — 법 제30조 필수 기재사항과 애드센스 쿠키 고지 반영
- [x] 홈 오늘 목록 — 스크롤이 자꾸 지금으로 돌아가던 것, 초기 배치만 + `지금` 버튼
- [x] 홈 오늘 목록에 시작–끝 시각 + 히어로에 앞뒤 화살표(`지금으로` 로 복귀)
- [x] 계획 시간표에서 같은 수업이 두 번 그려지던 것 — 추가해 둔 과목이 실제 수강이 된 경우
- [x] 수강 정정 마감(8/28 17:00) 에 배너·Trade 자동으로 내려가게 — 서버 시계 기준
