## 2026-08-23 — 로그인 앞에 소개 화면 하나

- 변경 파일: 새로 `pages/LandingPage.tsx` · `public/robots.txt` · `public/sitemap.xml`,
  고친 곳 `App.tsx` · `index.html` · `CLAUDE.md`
- 요약: 로그인 안 한 사람이 `/` 로 오면 무엇을 하는 앱인지 설명하는 화면이 먼저 뜹니다.
  로그인은 `/login` 으로 한 걸음 밀었습니다

주소를 열면 곧장 로그인 창이 떴습니다. 그러면 **밖에서 볼 때 이 주소에는 아무 내용도
없습니다** — 크롤러도 로그인 폼만 봅니다. 그래서 `/` 를 소개 화면이 받게 하고, 다른
주소로 바로 들어온 사람은 이미 앱을 아는 사람이라 그대로 로그인으로 보냅니다.

⚠️ **이 화면에는 실제 데이터가 한 줄도 들어가면 안 됩니다.** 로그인 앞이라 누구나
봅니다. 히어로에 놓은 시간표 카드의 이름·과목·교실은 전부 지어낸 것이고 앞으로도
그래야 합니다. 통계 숫자도 넣지 않았습니다 — 학기마다 달라져 금방 낡고, 정확히
맞추려면 로그인 없이 읽히는 API 를 하나 열어야 합니다.

⚠️ `RetroButton` 의 `variant="white"` 는 **면만 칠하고 글자색은 물려받습니다.** 마지막
CTA 구역이 `text-white` 라 흰 버튼에 흰 글자가 나왔습니다. 앱 안에서는 버튼이 늘 밝은
바탕 위에 있어서 여태 안 드러났던 것입니다.

처음 쓴 카피는 문단마다 길이가 똑같고 중점으로 나열해서 **광고 문구처럼 읽혔습니다.**
길이를 흩고("학사 통계" 는 한 줄, "빈 강의실" 은 두 줄), 명사 나열을 원인·결과 문장으로
바꿨습니다 — "학생·과목·교사·강의실을 검색창 하나에서" 대신 "이름을 넣으면 그 사람
시간표가, 교실을 넣으면 그 교실 일정이". 버튼도 `lg` 에서 기본 크기로 낮췄습니다.

`robots.txt` 는 `/` 만 열고 나머지를 막습니다. 앱 안쪽 주소는 어차피 로그인 화면만
나오는데, 그게 색인에 남으면 같은 빈 페이지가 열 개 있는 사이트로 읽힙니다.

## 2026-08-23 — 시연용 계정, 그리고 주말마다 홈에 박히던 검은 줄

- 변경 파일: `models` · `migrations` · `admin_router` · `auth_router` · `home_router` ·
  `friends_router` · `curriculum_router` · `bench_router` · `App.tsx` · `AdminPage` ·
  `home/TodayCardV3` · `lib/utils`
- 요약: 학교 구글 계정 없이 들어와 **만든 사람의 학번으로 보이는** 계정을 admin 에서
  만들 수 있게 했고, 수업 없는 날 홈에 남던 빈 카드를 걷었습니다

## 왜 학번을 그대로 못 붙였나

`users.stu_id` 는 **유니크**입니다. "한 학번은 한 계정" 이 라우터와 DB 양쪽에 박혀 있고,
25-059 는 이미 본계정이 쥐고 있습니다. 그래서 시연 계정에 같은 학번을 넣는 건 애초에
불가능했습니다 — 유니크를 푸는 선택지는 원칙이 무너져서 버렸습니다.

대신 **보는 눈만 빌려 주는 칸**(`users.demo_stu_id`, 유니크 없음)을 따로 두고,
`User.effective_stu_id` 가 `demo_stu_id or stu_id` 를 돌려주게 했습니다. 학번으로 무언가를
읽는 자리 열두 곳이 전부 이걸 거치므로 화면 쪽은 시연인지 아닌지 몰라도 됩니다.

⚠️ **구글 연동만은 `stu_id` 를 그대로 봐야 합니다.** 거기는 "화면이 누구로 보이는지" 가
아니라 **누구인지를 확정하는** 자리라, 빌린 학번으로 판단하면 남의 학번에 계정을 붙일 수
있습니다. `effective_stu_id` 의 독스트링에 이 구분을 적어 뒀습니다.

## 게이트

`email` 이 비면 `GoogleLinkModal` 이 화면을 덮는데, 시연 계정은 붙일 학교 계정이 아예
없는 사람에게 주는 것이라 여기서 막으면 영영 못 들어옵니다. `/auth/me` 에 `is_demo` 를
실어 그 경우만 건너뛰게 했습니다. **평범한 계정은 그대로 막힙니다**(직접 만들어 확인함).

⚠️ **빌려 줄 수 있는 학번은 만드는 사람 본인 것뿐입니다.** 학번을 파라미터로 받으면
이 창구로 남의 시간표를 열어 주는 계정을 만들 수 있게 됩니다. 편해 보여도 넓히지 마세요.

시연 계정이 자몽·트레이드에 적는 것은 `user_id` 에 붙으므로 학번 주인의 기록과 섞이지
않습니다. 뒤집어 말하면 그 기록은 시연 계정의 것이지 주인의 것이 아닙니다.

## 검은 줄

`TodayCardV3` 의 "오늘" 칸이 `<RetroCard>{hasTimetable && …}</RetroCard>` 모양이었습니다.
수업이 없는 날(주말·방학) `hasTimetable` 이 false 가 되면 **카드가 내용 없이 남아**,
테두리 4px 과 그림자만 그려진 검은 줄 하나가 화면에 박혔습니다. 조건을 카드 바깥으로
옮겼습니다.

바깥 `div` 는 남겼습니다 — 넓은 화면에서 그 자리(1fr)가 없어지면 옆의 주간 격자가 27rem
을 잃고 전폭으로 퍼져, 칸 하나가 200px 이 되면서 격자만 1300px 을 넘어갑니다. 대신 세로로
쌓이는 좁은 화면에서는 `hidden xl:flex` 로 간격까지 걷었습니다.

같은 실수를 막으려고 `design-guide.md` 카드 패턴에 규칙으로 적어 뒀습니다.

## 곁다리

레이블에 "정진우으로" 가 나와서 `particleRo()` 를 `lib/utils.ts` 에 넣었습니다. 받침이
없거나 `ㄹ` 이면 `로`. 한글이 아닌 글자로 끝나면 읽는 소리로 판단합니다 — 이름 없이
학번만 있을 때 `25-106` 은 "육" 으로 끝나 `으로` 입니다.

기록: 점검용 계정 `shottest` 와 시연 계정 `adsense-demo` 는 **로컬 DB 에만** 있습니다.

## 2026-08-22 — 모바일에서 잘려 안 보이던 것들, 그리고 주소로 못 들어가던 두 페이지

- 변경 파일: `molecules/PageHeader` · `EntityCard` · `SearchResultDisplay` ·
  `AdminPage` · `BrowsePage` · `CalendarPage` · `TradePage` · `ZamongPage` · `App.tsx`
- 요약: 390·360px 에서 전 페이지를 훑어 **잘려서 아예 못 누르던 곳** 여섯 군데를
  고쳤고, `/admin` `/trade` 를 주소창으로 열면 홈으로 튕기던 것을 잡았습니다

훑는 방법부터 정했습니다. 눈으로만 보면 놓치는 게 있어서, 페이지마다 **보이는 요소를
전부 재는 스크립트**를 같이 돌렸습니다 — 뷰포트 밖으로 나갔는데 스크롤되는 조상이
없는 요소, 그리고 `scrollWidth > clientWidth` 인데 `overflow-hidden` 인 상자. 후자가
이 앱에서 특히 위험합니다. `RetroCard` 가 `overflow-hidden` 이라 **넘친 것이 스크롤로
드러나지 않고 그냥 사라집니다.** `/admin` 계정 줄의 지우기 버튼이 정확히 그랬는데,
화면에는 멀쩡해 보이고 실제로는 카드 밖 55px 지점에 있었습니다.

고친 여섯:

| 자리 | 증상 | 처방 |
| --- | --- | --- |
| `PageHeader` 전체 | 액션 버튼이 제목을 덮음 (`Rooms` + `Reset Selection`) | `flex-col sm:flex-row` |
| `/browse` 탭 | `Friends` 탭이 화면 밖 (문서가 487px) | `flex-wrap` |
| `/calendar` | Requests 손잡이가 토요일 칸을 가림 | `md:pr-12` → `pr-12` |
| `/admin` 계정 줄 | 지우기 버튼이 카드 밖으로 잘림 | 조작 묶음에 `w-full sm:w-auto` |
| `/admin` 데이터 탭 | `Fetch from KEIS` 가 잘림 (200px 초과) | `flex-wrap` |
| `/zamong` 배너 | 본문이 한두 글자씩 접힘 | `flex-1` 에 `basis-64` |

⚠️ **`flex-wrap` 만으로는 안 접히는 경우가 있습니다.** 자몽 배너가 이미 `flex-wrap`
이었는데도 본문이 뭉개지고 있었습니다. `flex-1` 은 `flex-basis: 0` 이라 **줄을 넘길
이유가 생기지 않고** 옆의 `shrink-0` 버튼 묶음에 밀려 한없이 좁아집니다. 기준 폭
(`basis-64`)을 줘야 접힙니다. 나중에 같은 증상을 보면 여기부터 보세요.

`/calendar` 는 처방이 원래 있었는데 **브레이크포인트가 반대로 걸려 있었습니다.**
`md:pr-12` — 넓은 화면에만 자리를 비워 뒀으니, 정작 자리가 아쉬운 좁은 화면에서
서랍 손잡이가 내용을 덮었습니다. 대신 좁은 화면에서 달력 한 칸이 44px 로 줄어
일정 제목이 한 글자에서 잘립니다. 날짜를 누르면 아래에 전체가 나오니 그대로 뒀지만,
손잡이를 모바일에서 접어 넣는 쪽이 더 나을 수 있습니다.

**읽을 수는 있으나 쓸모없던 것도 둘 손봤습니다.** 검색 결과 카드의 수강 목록이
390px 에서 두 칸으로 갈리면 한 칸이 78px — `논리…` `미적…` 처럼 **세 글자에서**
잘립니다. 잘린 게 아니라 정보가 없는 것이라 좁은 화면에서는 한 줄씩 놓게 했습니다.

## 주소로 못 들어가던 `/admin` 과 `/trade`

점검하다 발견했습니다. `/admin` 을 주소창에 치면 홈으로 갑니다.

`/admin` 과 `/trade` 는 **역할·학기에 따라 나중에 등록되는** 라우트입니다. 첫 렌더에는
`currentUser` 가 아직 `null` 이라 라우트가 아예 없고, 그 순간 `path="*"` 이 걸려
`<Navigate to="/" replace />` 가 실행됩니다. `replace` 라서 **뒤로 가기로도 못
돌아옵니다.** 하단 네비로 눌러 들어가면 이미 로그인 정보가 있어 멀쩡하니, 주소창
진입과 **그 페이지에서의 새로고침**에서만 터집니다 — 모바일 브라우저가 탭을 자주
되살리는 걸 생각하면 드문 일이 아닙니다.

`currentUser` 가 정해질 때까지 판단을 미루는 것으로 고쳤습니다. 권한이 없는 사람은
역할이 확인된 뒤 그대로 홈으로 갑니다(직접 확인함). 없는 주소도 홈으로 갑니다.

⚠️ `term` 까지 기다리게 하고 싶은 유혹이 있는데 **하지 않았습니다.** `term` 은
localStorage 에서 동기로 복구되어 재방문자에게는 첫 렌더부터 있고, 데이터 요청이
실패하면 `term` 이 영영 안 채워져 **없는 주소가 로딩 화면에 갇힙니다.**

기록: 스크린샷은 `~/Bufs/Claude Files/2026-08-22/mobile-shots/`. 점검용 계정
`shottest` 를 로컬 DB 에만 만들어 뒀습니다(서버 아님).

## 2026-08-21 — 첫 로딩 JS 를 절반으로, `/browse` DOM 을 1/14 로

- 변경 파일: 새로 `atoms/RetroTooltip.tsx` · `atoms/RetroChip.tsx` ·
  `hooks/useVirtualGrid.ts`, 고친 곳 `SectionCard` · `SubjectAccordionItem` ·
  `SearchResultDisplay` · `AnalysisPage` · `Navigation` · `SearchInput` ·
  `BrowsePage` · `AdminPage` · `main.tsx` · `vite.config` · `index.css` ·
  `parser_run` · `backup`
- 요약: HeroUI 와 framer-motion 제거(첫 로딩 JS gzip 200KB → 101KB), `/browse`
  가상 스크롤(DOM 7,397 → 511), Data Versions 학기 선택, 2026-2 의 추가수강신청
  이전 상태를 옛 백업에서 1회차로 복원

먼저 재고 골랐습니다. 짐작과 달리 **검색 엔진은 문제가 아니었습니다** — 불린·초성까지
실데이터에서 1~7ms 입니다. JSON 응답도 이미 5.4배 압축되고 있었습니다(235KB → 43KB).
반면 `heroui` 청크가 gzip 103KB 로 **전체 JS 의 51%** 였습니다.

⚠️ **집계를 한 번 틀렸습니다.** 처음에 `@heroui` import 를 세면서 여러 줄을 하나로
이어 붙인 뒤 쉼표로 쪼개는 방식을 썼는데, 그러면 목록의 **첫 항목이 앞줄 꼬리에 붙어
사라집니다.** 그래서 `Navbar` 와 `Input` 을 놓치고 "여섯 개" 라고 보고했습니다. 실제로는
일곱 개였고, `Input` 은 검색창이라 대충 넘길 자리가 아니었습니다. 파일별로 세야 합니다.

걷어내고 보니 쓰던 일곱 중 로직이 있는 건 툴팁과 인풋뿐이었습니다. 나머지 다섯은
`<span>`·`<hr>`·`<div>` 였고, **이 앱은 HeroUI 의 생김새를 쓰지도 않았습니다** — 모서리를
전역으로 눌러 없애는 오버라이드를 얹은 채였으니 라이브러리를 쓰면서 계속 무효화하고
있었던 셈입니다. framer-motion 은 HeroUI 가 끌고 온 것이라 같이 빠졌습니다.

툴팁을 새로 만들 때 걸린 것은 **React 컴파일러 lint** 였습니다. `cloneElement` 로 ref 를
넘기는 건 툴팁 트리거의 표준 수법인데 "렌더 중 ref 접근" 으로 막힙니다. 자식을
`display:contents` 래퍼로 감싸고 **첫 자식을 재는** 방식으로 돌아갔습니다 — 레이아웃에
영향이 없고 자식의 ref 를 뺏지도 않아 오히려 낫습니다.

같은 규칙에 `useVirtualGrid` 도 두 번 걸렸습니다. 훅이 ref 를 **돌려주면** 컴파일러가
반환값 전체를 ref 로 물들여, `visible` 을 읽는 것까지 막습니다. **ref 는 부르는 쪽이
만들어 넘기는** 구조로 뒤집어야 통과합니다. 측정값(열 수·줄 높이)도 ref 에 두면 렌더에서
못 읽으니 state 여야 합니다.

`/browse` 가 무거웠던 건 전교생 677명을 한 번에 그려서입니다. 카드 높이가 365장 중
347장이 정확히 같아 한 줄 높이를 하나로 봐도 됐습니다. 스크롤 컨테이너가 따로 없고
문서 전체가 스크롤되는 구조라 `window.scrollY` 기준입니다.

⚠️ **`2026-1` 에는 복원할 것이 없었습니다.** 추가수강신청 전 데이터를 찾아 달라고 하셔서
백업 17개를 학기별로 대조했는데, 2026-1 은 **모든 백업에서 완전히 동일**했습니다(247분반
/3222수강, 해시까지). 가장 오래된 백업이 7월 29일인데 그 학기 정정 기간은 훨씬 전입니다.
대신 **2026-2 에 정확히 그 상태가 있었습니다** — 7/29~8/10 이 237분반/2952수강이고
8/11 재수집에서 251/3172 로 뜁니다.

처음 대조에서는 학기마다 상태가 두 개씩 나오는 것처럼 보였는데, **4층 구조 이전 백업은
과목명이 `"미적분학2(EC)"` 한 덩어리**라 표현만 다른 것이었습니다. `split_name()` 으로
정규화하니 실제 구별되는 상태는 2026-2 만 셋이었습니다.

복원하면서 한 가지 더 드러났습니다 — 8/10 백업은 **강의실이 전부 `배정중`** 입니다.
그대로 요약에 넣으면 "교실 이동 237건" 이 깔리는데 사실은 배정이 한 번 일어난 것이라,
`assigned` 로 따로 셉니다. 학기 초마다 생기는 일이라 수집 요약에도 같은 처리를 넣었습니다.

⚠️ 회차를 앞으로 밀 때 `UPDATE version=version+1` 은 **UNIQUE 제약에 걸립니다** —
올라가는 도중에 다음 회차와 부딪힙니다. 멀리(+1000) 밀었다 되돌려야 합니다.

배포 뒤에 발견한 것 하나 더: 백업 파일들이 `baeks:baeks 660` 이라 **서비스 계정이 읽지
못했습니다.** 디렉토리는 열어 뒀는데 파일이 막혀 있어서, 복원 스크립트를 서비스 계정으로
돌리자 그제야 드러났습니다. 그룹을 `ksaclass` 로 맞췄습니다.

## 2026-08-21 — 데이터에 회차를 붙이고, 그것으로 전교생 캐시를 무효화

- 변경 파일: 새로 `backend/versioning.py` · `frontend/src/components/admin/ChangeSummary.tsx`,
  고친 곳 `models` · `migrations` · `parser_run` · 라우터 6개 · `App.tsx` · `AdminPage`
- 요약: 수집이 행을 지우지 않고 `[version_from, version_to)` 로 닫도록 바꿨습니다.
  회차는 **바뀐 게 있을 때만** 오르고, `/auth/me` 에 실려 나가 모든 브라우저의 학기
  캐시를 갈아 줍니다. Admin 에 회차 이력과 변경 요약 화면을 붙였습니다

시작은 캐시 버그였습니다 — 재수집을 눌러도 **누른 관리자 본인까지** 최대 1시간 옛
시간표를 봤습니다. `clearCache()` 한 줄로 때울 수 있었지만 그러면 누른 사람만 고쳐지고
학생들은 그대로입니다. 회차를 응답에 실으면 전원이 스스로 버리므로, 캐시 무효화와
회차를 **한 설계로 묶었습니다.**

⚠️ **회차는 "돌린 횟수" 가 아니라 "달라진 횟수" 입니다.** 조사하다 서버 백업 15개가
실제로는 데이터 상태 3개뿐이라는 걸 알았습니다 — 8월 11·14·18·18·19일 백업 다섯 개가
내용이 완전히 같았습니다. 돌릴 때마다 남긴 결과입니다. 그래서 수집이 **먼저 현재
상태와 맞대어 보고**, 다르지 않으면 백업도 회차도 만들지 않습니다. 판정은 읽기만
하므로 DB 에 흔적이 없습니다.

⚠️ **폐강된 분반도 행이 남습니다.** 지우면 Trade 계획(`UserState.trade`)이 가리키는
분반 id 가 끊깁니다. 대신 조회하는 모든 자리에 "열려 있는 행만" 조건이 필요해졌는데,
`Class`·`ClassTime`·`Enrollment` 를 만지는 자리가 백엔드 8개 파일 64군데라 하나만
빠뜨려도 폐강된 분반이 조용히 섞입니다. 두 겹으로 막았습니다 — 조건은 `versioning.py`
의 `at_version()` 하나로 모으고, `Class.times` 같은 **관계 자체를 `primaryjoin` 으로
막아** 읽는 쪽이 조건을 기억하지 않아도 되게 했습니다. 관계를 `viewonly` 로 둔 건
컬렉션에서 빠지는 것이 삭제로 오해되지 않게 하려는 것입니다.

함정이 하나 있었습니다. UNIQUE 제약에 `version_from` 을 넣어야 했습니다 — 한 학생이
수업을 뺐다가 다시 듣는 일이 실제로 있는데, 옛 제약(`stuId`, `classId`)이면 그 이력을
두 행으로 남길 수 없어 뭉개집니다. SQLite 는 제약을 ALTER 로 못 바꿔서 `classes` 와
`enrollments` 를 다시 세웠고, **id 를 그대로 옮겨** FK 와 Trade 계획을 지켰습니다.

`students`·`subjects` 에는 버전을 안 붙였습니다. 학기에 안 묶인 전역 테이블이라
학기별 회차를 매길 자리가 없습니다. 대신 이름을 고치면 **그 사람이 실제로 속한 학기의**
회차만 올립니다(`terms_of_student`). 대가는 과거 회차를 열어도 이름은 현재 값으로
보인다는 것 — 캐시는 정확히 갈리고 이름 이력만 없는 절충입니다.

검증은 실제 데이터로 했습니다. DB 에서 KEIS 응답을 역구성해 같은 데이터를 다시 넣으면
`changed=False`, 수강 하나를 빼면 정확히 그것만 잡히고 분반 id 는 그대로였습니다.
그 다음 진짜 수집을 돌렸더니 8월 11일 이후 실제 변화가 있었습니다 — 영어Ⅱ 1분반의
담당이 Kevin Anderson → Casey Stojak, 교실 이동 8건, 수강 +163/-190. 이때 **교사
교체가 "폐강 + 신설" 로 보이는 게 읽기 나빠서** 짝을 지어 따로 빼는 처리를 넣었습니다.
분반을 `(과목, EC, 분반, 교사)` 로 잡으니 틀린 표현은 아니지만, 사람에게는 한 사건입니다.

브라우저에서도 양방향을 확인했습니다. 회차가 같으면 새로고침해도 `/auth/me` 만 나가고
학기 데이터 요청이 아예 없고, 서버에서 회차를 올리면 다음 새로고침에 `GET /` 가 나가
캐시가 갈립니다.

되돌리기 어려운 지점은 **스키마**입니다. 컬럼 추가는 멱등이지만 테이블을 다시 세운
`classes`·`enrollments` 는 예전 코드로 되돌리면 UNIQUE 제약이 맞지 않습니다. 배포 전에
서버 DB 스냅샷을 따로 떴습니다.

⚠️ 배포하고 나서야 **백업 디렉토리가 만들어질 수 없다**는 걸 알았습니다. `BACKUP_DIR`
이 `backend/backups/` 였는데, 서버에서 `backend/` 는 `baeks:baeks 755` 이고 서비스는
`ksaclass` 로 돕니다. 첫 수집이 `os.makedirs` 에서 막히고, 백업을 못 만들면 수집을
접도록 해 뒀으니 **버튼을 누르는 순간 아무것도 안 되는** 상태였습니다. 이걸 못 보고
넘어갈 뻔한 이유는 로컬에서는 같은 계정이라 아무 문제가 없어서입니다.

고치면서 위치 자체를 **DB 옆**으로 옮겼습니다(`data/backups/`). 그 디렉토리는 이미
`baeks:ksaclass 770` 이라 서비스가 쓸 수 있고, 저장소 밖이라 `git clean -fdx` 에도
안 날아갑니다. 서버 DB 가 symlink 라서 `os.path.realpath()` 로 실제 위치를 따라가게
했고, 로컬은 진짜 파일이라 예전과 같은 자리입니다.

서버에 손으로 떠 둔 옛 백업 16개는 `data/backups/` 로 옮기며 이름에 데이터 상태를
붙였습니다(`setN`). **번호가 같으면 내용이 완전히 같은 파일**이라 중복이 한눈에 보입니다
— set1 둘, set2 여덟, set3 여섯이었습니다. 마이그레이션 체크포인트(`before-google`
같은 것)는 수집이 아니라서 회차를 매기지 않고 원래 이름을 라벨에 남겼습니다. 지우지는
않았습니다. `LEGACY-MANIFEST.md` 와 `undo-legacy-move.sh` 를 같이 뒀습니다.

마지막으로 서비스 계정으로 실제 수집을 두 번 돌려 확인했습니다. 첫 번째는 서버가
8월 11일자 데이터였던 터라 v2 가 생겼고(교사 교체 1 · 교실 이동 8 · 수강 +163/-190),
두 번째는 백업도 회차도 만들지 않고 끝났습니다.

## 2026-08-21 — 리팩토링: 흩어진 규칙 셋을 모으고 검색 엔진에 타입을 붙임

- 변경 파일: 새로 `frontend/src/lib/schedule.ts` · `frontend/src/lib/session.ts`,
  고친 곳 16개 (`homeView` · `WeekTimetable` · `TodayTimeline` · `DayRuler` ·
  `searchEngine` · `App` + API 모듈 4개 + 페이지 4개)
- 요약: 같은 규칙이 파일마다 복사돼 있던 셋(연강·쉬는시간 판정 / 세션 토큰 / 검색
  엔진의 `any`)을 각각 한 곳으로 모았습니다. lint 오류 58 → 12

리팩토링 대상은 먼저 재고 골랐습니다. 프론트 20,695줄 / 백엔드 6,323줄인데
**백엔드는 라우터 단위로 이미 갈려 있고 중복 함수도 없어서 손대지 않았습니다.**
후보 일곱 중 위험이 낮고 이득이 확실한 셋만 했고, 상태 구조(`App.tsx` 의 22개 prop
드릴링)와 시간표 렌더러 다섯 종 통합은 **일부러 남겼습니다** — 방향이 여러 갈래라
논의 없이 고르면 안 되는 것들입니다.

**A. 연강·쉬는시간 판정 → `lib/schedule.ts`**

임계값이 다섯 곳에 있었고 값이 `20`·`20`·`20`·**`15`** 로 어긋나 있었습니다
(`DayRuler` 의 축 눈금만 15). 실제 교시 간격이 10·60·70분뿐이라 결과는 같았지만,
문제는 값이 아니라 **구조**였습니다 — 이틀 전 "연강 중인데 공강으로 뜬다" 를 고칠 때
`homeView.ts` 하나만 손댔고, 나머지 셋은 각자의 조건문을 그대로 들고 있었습니다.

⚠️ **되돌리지 마세요.** 판정을 컴포넌트로 되가져가면 같은 버그가 자리마다 따로
납니다. `isBreakGap` · `isSameClass` · `continuesClass` · `gapName` · `mergeSpans`.

**B. 세션 토큰 → `lib/session.ts`**

`"ksa_session_token"` 이 열 곳에 박혀 있었습니다 — 여섯 파일이 각자 상수로 다시
선언, 넷은 인라인. `authHeader()` 는 여덟 번 복붙돼 있었고 그 사이 **토큰이 없을 때
돌려주는 값이 갈려 있었습니다**(`undefined` vs `{}`). axios 가 둘 다 받아 줘서
드러나지 않았을 뿐입니다. `{}` 로 통일했습니다.

⚠️ `App.tsx` 에는 이미 `setSessionToken` 이라는 **상태 setter** 가 있어서
`setSessionToken as persistSessionToken` 으로 받습니다. 그냥 import 하면 조용히
가려집니다.

**D. `searchEngine.ts` 의 `any` 32개**

프로젝트 lint 오류 58건 중 40건이 이 파일이었습니다. 타입을 붙이자 **`data` 가
`SubjectData` 라고 선언된 채 `subject_id` 칸이 비어 나가고 있던 게** 드러났습니다 —
읽는 화면이 없어서 아무도 몰랐습니다. 값을 실어 보내 타입을 사실로 만들었습니다.
결과 엔티티에 내부용 `subjectsRaw` 맵이 딸려 나가던 것도 뺐습니다.

`extractEntities` 는 같은 20줄이 세 번(강의실·교사·학생) 반복돼 있었는데, **강의실만
시간에 조건이 붙어 있어서**(찾는 방에서 열리는 교시만) 차이를 찾으려면 세 덩이를
나란히 놓고 비교해야 했습니다. `ensure`/`addSection`/`addTimes` 로 모으고 그 차이는
`keep` 인자 하나로 남겼습니다.

**검증 — "동작이 같다" 를 눈으로 안 때웠습니다.**

리팩토링은 결과가 같아야 의미가 있는데 이 프로젝트에는 테스트가 없습니다. 그래서
`git show HEAD:` 로 **이전 판본을 옆에 두고** esbuild 로 둘 다 번들해 실제 데이터
(2026-2, 109과목·366명)로 돌려 비교했습니다.

- 검색: 실제 데이터에서 뽑은 검색어 71개 — 차이 **0**
- 홈 파생값(`deriveHomeView`): 요일 5종 × 하루 1440분 + 수업 없는 날 = 8,640개 시점
  — 차이 **0**. 표본에 연강이 9건 들어 있는 것까지 확인했습니다

⚠️ **하니스가 헛돌지 않는지 먼저 확인해야 합니다.** 양쪽이 다 빈 결과를 내면 차이도
0 이라 아무것도 증명하지 못합니다. 14가지 유형(빈 검색·구분자·불린·초성·요일교시…)이
전부 비어 있지 않은 결과를 내는 걸 따로 찍어 봤습니다.

화면은 Playwright 로 홈·검색을 확인했고, DEV 상태 전환의 `연강 쉬는시간` 에서
히어로가 `6–7교시 · 쉬는시간` 을 유지하는 것까지 봤습니다.

## 2026-08-20 — 홈: 한 번 받고 프론트에서 계산 (주기 요청 제거)

- 변경 파일: `frontend/src/pages/HomePage.tsx`, `frontend/src/components/MealCard.tsx`
- 요약: 1분마다 `GET /home` 을 다시 받던 것을 없앴습니다. 받아 둔 교시 시각표·시간표로
  "지금" 을 계산하고, 다시 받는 건 깨어남·날짜 변경·수동 재시도 세 자리뿐입니다

**애초에 물어볼 이유가 없었습니다.** 화면에서 움직이는 값(지금 몇 교시·남은 시간·다음
수업)은 전부 `lib/homeView.ts` 가 `liveMinute` 하나로 계산합니다 — 서버가 주는
`now.period`·`current`·`next` 는 **화면이 쓰지도 않습니다**(같은 걸 한 번 더 계산해 줄
뿐입니다). 그래서 1분마다 받던 건 같은 답을 다시 받으면서 실패할 기회만 만들고 있었고,
실제로 노트북을 덮었다 열 때마다 오류 카드가 뜨던 원인이 그것이었습니다.

**시계는 기기가 굴립니다.** 받은 순간 서버 시계와의 차이를 한 번 재 두고, 그 뒤로는
기기 시계에 그 차이를 더합니다. ⚠️ 예전 방식(`서버 분 + 흐른 시간` 을 1439 로 자르기)은
노트북을 덮어 두면 **시계가 23:59 에 붙어 버립니다** — 흐른 시간이 몇 시간이 되니까요.

**다시 받는 자리는 셋뿐입니다** — 깨어남(`visibilitychange`·`online`), 날짜 변경, 수동
재시도. ⚠️ 날짜 검사는 기기 날짜가 어긋난 사람에게 요청이 쏟아지지 않게 **마지막으로
받은 지 1분은 지났을 때만** 부릅니다.

**대신 하루 종일 열어 둔 탭에서는 학사일정·급식 정보가 받아 둔 시점 그대로입니다.**
그게 문제가 되면 주기 요청을 되살리지 말고 날짜 검사 옆에 조건을 더하세요 — 창을 다시
보는 순간에는 어차피 새로 받습니다.

**급식도 같은 규칙으로 맞췄습니다.** 같은 날을 다시 받다 실패하면 있던 메뉴를 그대로
두고, 다른 날짜를 받다 실패하면 비웁니다 — 어제 메뉴를 오늘 것처럼 두는 게 더 나쁩니다.

## 2026-08-20 — 덮었다 열면 홈이 "화면을 불러오지 못했습니다" 로 바뀌던 것

- 변경 파일: `frontend/src/pages/HomePage.tsx`
- 요약: 주기 요청이 실패해도 마지막 화면을 그대로 두고, 실패는 한 줄로만 알립니다.
  깨어나면(`visibilitychange`·`online`) 즉시 다시 받습니다

**원인은 한 줄이었습니다** — `catch { setState(null) }`. 홈은 1분마다 다시 받는 화면이라,
노트북을 덮었다 열면 **깨어난 직후 네트워크가 붙기 전에** 주기 요청이 한 번 나가 실패
하고, 그 순간 멀쩡히 보고 있던 시간표가 통째로 오류 카드가 됐습니다. 받아 둔 게 있는데
버릴 이유가 없습니다.

**실패는 상태가 아니라 사정입니다.** 이제 `failed` 로 표시만 하고 마지막 응답을 계속
그립니다. 위에 `연결이 끊겨 N분 전 받은 화면을 보여 주고 있습니다` 한 줄과 다시 시도
버튼만 얹습니다. ⚠️ **오류 카드는 한 번도 못 받았을 때만** 나옵니다.

**깨어나면 기다리지 않습니다.** 덮어 둔 동안 타이머가 멈춰 있어서, 열자마자 보이는 건
잠들기 전 시간표입니다 — 다음 주기까지 최대 1분을 그렇게 두면 "지금 몇 교시" 가 틀린
채로 떠 있습니다. `visibilitychange` 와 `online` 에서 곧바로 다시 받습니다.

로컬에서 백엔드를 죽여 놓고 깨어남 신호를 보내 확인했습니다 — 화면은 그대로 있고 안내
줄만 붙으며, 백엔드를 살리고 다시 시도를 누르면 줄이 사라집니다.

## 2026-08-20 — 연강 사이 쉬는시간이 "공강" 으로 뜨던 것

- 변경 파일: `frontend/src/lib/homeView.ts`, `frontend/src/components/home/TodayCardV3.tsx`,
  `frontend/src/components/home/WeekTimetable.tsx`, `frontend/src/lib/homeDemo.ts`
- 요약: 교시 사이를 **쉬는시간**으로 구별하고, 연강 사이 10분에는 그 수업이 계속되는
  것으로 잡습니다. DEV 상태에 `연강 쉬는시간` 표본을 추가했습니다

**교시 번호로만 "지금" 을 판정한 게 원인이었습니다.** 7·8교시 연강 문학을 듣는 중에
16:35(교시 사이 10분)가 되면 `livePeriod` 가 null 이 되고, 히어로가 그대로 **"공강"** 을
띄웠습니다 — 수업 중인 사람에게 교실을 나가라고 말하는 화면이었습니다.

**자(`DayRuler`)와 오늘 목록은 원래 맞게 그리고 있었습니다.** 둘은 덩어리의 시작~끝으로
보고 있어서 그 10분에도 진행 중이었고, **히어로만** 교시 번호로 판정해 혼자 어긋났습니다.
주간 격자도 같은 함정이 있어 `livePeriod` 대신 `currentPeriods` 를 보게 고쳤습니다 —
네 자리가 같은 값을 봐야 합니다.

**세 상태를 구별합니다** — 수업 중 / 쉬는시간(교시 사이 20분 이하) / 공강(교시 안인데
수업이 없음). 점심·저녁·자습은 서버가 이름을 주므로(`break_name`) 그 이름을 그대로
씁니다. ⚠️ 이름 있는 구간에는 "쉬는시간" 을 덧붙이지 않습니다 — `쉬는시간 · 점심` 처럼
두 번 말하게 됩니다.

**DEV 상태에 `연강 쉬는시간` 을 넣었습니다.** 이 버그는 하루에 몇 분밖에 안 나타나서
실제로 그 시각이 되기를 기다리지 않으면 확인할 수 없습니다. 표본 하루의 6·7교시가 이미
연강(일반지구과학)이라 그 사이 시각(15:35)만 골라 두면 됩니다.

검증은 `deriveHomeView` 를 실제로 돌려서 했습니다 — 연강 사이(수업 유지) · 다른 수업
사이(쉬는시간) · 진짜 공강 · 점심 네 경우가 모두 의도대로 나옵니다.

## 2026-08-20 — 배지 진동에 기울기, 남은 호버 제거

- 변경 파일: `frontend/src/index.css`, `frontend/src/components/atoms/StudentBadge.tsx`
- 요약: 진동에 ±1도 기울기를 얹고, `StudentBadge` 에 남아 있던 호버 확대를 뺐습니다

**평행이동만으로는 진동이 아니라 "밀림" 이었습니다.** 판이 좌우로 미끄러지는 느낌이라,
같이 기울어지게 하니 그제야 떨리는 물건으로 읽힙니다. ±1도면 11px 글자를 읽는 데
지장이 없습니다.

⚠️ `translate` 와 `rotate` **개별 속성**으로 씁니다. 배지가 누를 때 `scale` 을 쓰므로
셋을 같은 `transform` 에 얹으면 서로를 지웁니다 — 브라우저에서 재 보면 흔들림과 기울기가
같은 부호로 함께 움직입니다(예: `1.22px / 0.75deg`).

**`TradePage` 의 학생 검색 결과 버튼은 호버를 남겼습니다.** 거기는 늘어놓고 읽는 명단이
아니라 **골라 누르는 목록**이라, 커서가 닿은 항목이 반응하는 게 맞습니다.

## 2026-08-20 — 주간 격자 칩의 갭을 네 변 모두 같게

- 변경 파일: `frontend/src/components/home/WeekTimetable.tsx`
- 요약: 칩이 한쪽 격자선에만 딱 붙어 보이던 것을 고쳤습니다 — 네 변의 흰 틈이 모두
  1px 입니다

**`m-px` 는 사방으로 같은 값인데 결과가 안 같았습니다.** 격자선이 칸의 위·왼쪽
**안쪽**에 그려지기 때문입니다(`border-t`·`border-l`). 선이 있는 쪽은 그 1px 을 선이 다
먹어 칩이 선에 붙고, 반대쪽만 흰 틈이 남습니다. 브라우저에서 재 보니 왼쪽·위의 흰 틈이
0px, 오른쪽·아래가 1px 이었습니다.

**선이 있는 변만 1px 더 밉니다** — 위·왼 2px, 아래·오른 1px. ⚠️ 첫 줄은 예외로 위 1px
입니다: 그 위의 선은 요일 머리의 `border-b` 라 **칸 안쪽이 아니어서**, 똑같이 2px 을
주면 첫 줄만 흰 틈이 2px 이 됩니다.

갭을 아예 없애는 길도 있었지만 택하지 않았습니다 — 칩이 선을 덮으면서 붙어 있는 칩끼리
테두리가 2px + 2px 로 뭉치는데, 그건 `m-px` 를 처음 넣은 이유였습니다.

## 2026-08-20 — 배지 강조를 진동으로, 호버 효과 제거

- 변경 파일: `frontend/src/index.css`, `frontend/src/components/SectionCard.tsx`,
  `frontend/src/components/SearchResultDisplay.tsx`
- 요약: 찾는 사람의 강조를 위아래 튐(`badge-bounce`)에서 좌우 진동(`badge-buzz`)으로
  바꾸고, 학생 배지의 호버 효과를 뺐습니다

**진동은 계속 떨지 않습니다.** 주기(1.8s)의 앞 20%만 좌우 ±1.5px 로 떨고 나머지는
가만히 있습니다 — 폰 진동과 같은 리듬입니다. 쉬지 않고 흔들면 명단에 서른 명이 있는
화면에서 한 칸이 계속 움직여 옆을 읽는 걸 방해합니다.

**호버 효과를 뺀 이유가 여기에 있습니다.** 마우스가 지나가는 자리마다 배지가 커지면
움직이는 것이 둘이 되어, 정작 봐야 할 진동이 그 소음에 묻힙니다. 명단은 **읽는
물건**이라 클릭 피드백(`active:scale-95`)만 남겼습니다. 배지 공통 스타일(`.student-badge`)
의 `hover:bg-retro-accent-light` 도 같이 걷어냈습니다.

## 2026-08-20 — 검색 명단: 흐리게 하는 대신 찾는 사람이 통통 튑니다

- 변경 파일: `frontend/src/components/SectionCard.tsx`, `frontend/src/index.css`,
  `frontend/src/components/SubjectAccordionItem.tsx`, `frontend/src/pages/SearchPage.tsx`,
  `frontend/src/App.tsx`, `frontend/src/components/home/MySubjects.tsx`
- 요약: 검색어와 안 맞는 학생 배지를 `grayscale opacity-30` 으로 지우던 처리를 걷어내고,
  맞는 배지만 위아래로 통통 튀게 했습니다

**흐리게 하는 쪽이 잘못이었습니다.** 명단을 보는 이유의 절반은 "누구랑 같이 듣나" 인데,
찾는 사람 하나만 남기고 나머지를 지우면 화면이 반쯤 죽습니다. 전부 제 색으로 두고
**찾는 사람만 움직이게** 하면 남의 색을 뺏지 않고도 제일 먼저 눈에 들어옵니다.

⚠️ **`transform` 이 아니라 `translate` 속성으로 움직입니다.** 배지에 `hover:scale-105`
가 걸려 있는데 Tailwind v4 는 그걸 `scale` 속성으로 냅니다 — 같은 `transform` 에 얹으면
둘 중 하나가 지워져서, 튀는 배지에 마우스를 올려도 커지지 않거나 반대로 튐이 멈춥니다.
`MarqueeText` 가 이미 같은 이유로 `translate` 를 쓰고 있어 그 방식을 따랐습니다.

⚠️ **움직임 하나에만 기대지 않습니다.** `prefers-reduced-motion` 이면 애니메이션이 꺼지
므로, 맞는 배지는 면을 조금 더 채우고(20 → 38) 그림자를 세게 둬서 정지 화면에서도
구분됩니다. 캡처한 그림에서도 마찬가지입니다.

**`hasStudentInSearch` 를 통째로 지웠습니다.** 흐리게 만드는 판단에만 쓰이던 값이라
`App` 의 memo 부터 `SearchPage` · `SubjectAccordionItem` · `SectionCard` · 홈의
`MySubjects` 까지 따라 내려가던 prop 이 전부 죽었습니다 — 남겨 두면 다음 사람이 "이건
왜 넘기지" 를 다시 좇게 됩니다.

## 2026-08-20 — 제목을 전부 영어로 · 오늘 일정과 급식의 굵기 통일

- 변경 파일: `frontend/src/components/home/{TodayCardV3,TodayCardV2}.tsx`,
  `frontend/src/components/DeviceSessions.tsx`,
  `frontend/src/pages/{CalendarPage,ZamongPage,TradePage,RoomsPage}.tsx`
- 요약: 화면에 남아 있던 한글 제목을 영어로 맞추고, 오늘 일정 항목의 굵기를 급식과
  같은 `font-bold` 로 내렸습니다

**제목만 영어입니다.** 본문·버튼·안내 문구는 한국어 그대로고, 바꾼 건 섹션 소제목
(`RetroSubTitle`)과 페이지 제목·부제(`PageHeader`)뿐입니다. `오늘 일정` 옆에 `MEAL` 이
서 있는 식으로 섞여 있으면 그것만으로 어수선해 보입니다.

| 전 | 후 |
|---|---|
| `오늘 일정` | `Events` |
| `로그인한 기기` | `Devices` |
| 학사일정 / 부제 `Calendar` | `Calendar` / 부제 `Schedule` (제목·부제가 뒤집혀 있었습니다) |
| 부제 `교육과정 이수 현황` | `Curriculum Progress` |
| 부제 `수강 변경 탐색` | `Course Change` |
| 부제 `형설관` | `Hyung-seol Hall` |

**굵기는 700 으로 모았습니다.** 오늘 일정이 900, 급식이 700 이었는데 크기를 맞춘 뒤에도
한쪽만 굵어서 여전히 무게가 달랐습니다. 900 쪽으로 올리지 않은 건 급식이 예닐곱 줄짜리
목록이라 통째로 900 이면 그 칸이 화면에서 제일 무거워지기 때문입니다 — 목록의 기본값을
따릅니다.

## 2026-08-20 — 홈 과목 목록을 검색 아코디언으로 교체 · 시간표 머리 겹침 · 글자 크기

- 변경 파일: `frontend/src/components/home/{MySubjects,WeekTimetable,TodayCardV3}.tsx`,
  `frontend/src/components/MealCard.tsx`, `frontend/src/pages/HomePage.tsx`, `frontend/src/App.tsx`
- 요약: 홈 전용으로 그렸던 과목 목록을 걷어내고 **검색에서 쓰는 아코디언을 그대로**
  씁니다. 시간표 머리 줄이 폰에서 겹치던 것을 줄바꿈으로 고치고, 오늘 일정·급식·날짜
  글자 크기를 맞췄습니다

**⚠️ 어제 만든 홈 전용 과목 목록을 하루 만에 버렸습니다.** 접힌 줄에 분반·교사, 펼치면
교실·시간, 머리에 학점 합계까지 나름 다듬었지만 **검색에 이미 같은 물건이 있었습니다**
(`SubjectAccordionItem` — 자기 이름으로 검색하면 나오는 그 아코디언). 두 벌이 되면
한쪽만 고쳐 놓고 잊습니다. 지금 `MySubjects` 가 하는 일은 **내가 듣는 분반만 남긴
`SubjectData` 를 만들어 넘기는 것** 하나입니다.

⚠️ 그 아코디언에 넘기는 `searchTerm` 에 **내 학번을 넣어야 합니다.** 비우면
`hasStudentInSearch` 가 켜진 채 맞는 사람이 하나도 없어서 **명단 전체가 회색**이 됩니다.
`isSingleStudentSearch` 를 켜면 머리 칩이 `SECTION 5` 로 바뀌는 것까지 검색 화면과 같은
모습이 됩니다.

**시간표 머리 줄이 폰에서 겹쳤습니다.** 제목·`계획` 표식·주 교시 수·체크박스·PNG 까지
다섯 덩어리인데, 오른쪽 뭉치에 `shrink-0` 이 걸려 있어 390px 에서 380px 을 넘기면 서로
타고 올라갔습니다. `flex-wrap` 으로 오른쪽 뭉치를 통째로 아랫줄에 내려보내고 `ml-auto`
로 오른쪽에 붙였습니다.

**한 행에 나란히 선 칸은 글자 크기를 맞춥니다.** 오늘 일정(12px)과 급식 메뉴(15px)가
3px 차이라 같은 층의 정보인데 무게가 달라 보였습니다 — 딱 가운데인 13.5px 로 모았고,
히어로의 날짜는 바로 아래 `교실 · 교사 · 분반` 줄과 같은 13px 로 통일했습니다.

로컬 임시 계정 + Playwright 로 390px·1440px 를 확인했고(계정은 삭제), 스크린샷은
`~/Bufs/Claude Files/2026-08-20/` 에 있습니다.

## 2026-08-19 — 주간 시간표 모바일 대응

- 변경 파일: `frontend/src/components/home/WeekTimetable.tsx`
- 요약: 좁은 화면에서 격자를 가로로 흘리고(최소 폭 35rem), 교시 열·요일 머리를 고정
  하고, 열 때 오늘 열을 가운데로 맞춥니다

**다섯 열을 우겨넣는 걸 포기했습니다.** 390px 에서 한 칸이 57px 이면 `5분반 · 박인숙`
이 잘리고 과목명도 두 줄 안에 안 들어갑니다 — 시간표를 여는 이유(어느 교실로, 누구
수업)가 통째로 사라지는데 "한 주가 한 화면에" 만 남습니다. 최소 폭을 주니 한 칸이
99px 이 되어 세 줄이 다 읽힙니다.

⚠️ **`xl:min-w-0` 을 빠뜨리면 데스크톱이 깨집니다.** 그 폭에서 격자는 오늘 목록과 2열로
서면서 제 폭이 34rem 남짓이 되므로, 최소 폭 35rem 이 그대로 남으면 **없던 가로 스크롤**
이 생깁니다. 1440px 에서 `scrollWidth === clientWidth`(648) 로 확인했습니다.

⚠️ **고정(sticky)된 칸의 배경은 불투명해야 합니다.** 요일 머리는 `bg-black/[0.03]`·
`bg-retro-primary/25` 였는데 97%·75% 가 비어 있어서, 세로로 흘리면 칩이 요일 글자를
통과해 보입니다. 같은 색을 흰색과 미리 섞은 값(`#f7f7f7`·`#ffd3ee`)을 박아 뒀습니다 —
투명도로 되돌리면 그 증상이 그대로 돌아옵니다.

**열 때 오늘 열을 가운데로 옮깁니다.** 안 그러면 목요일인 사람은 열 때마다 손으로
밀어야 합니다. 다 보이는 화면에서는 `scrollLeft` 를 아예 건드리지 않습니다 — 조건
없이 만지면 브라우저가 포커스를 옮기며 페이지가 덜컥 움직이는 경우가 있습니다.

**확인 방법을 하나 만들었습니다.** 로컬 DB에 임시 계정을 만들고(`create_user` 후
`stu_id`·`email` 을 직접 채움) Playwright로 390·360·1440px 를 찍어 봤습니다. 스크린샷은
`~/Bufs/Claude Files/2026-08-19/`. 확인이 끝나고 계정은 지웠습니다 — 로그인 화면 때문에
그동안 화면을 눈으로 못 보고 배포하고 있었는데, 이 방법이면 볼 수 있습니다.

## 2026-08-19 — 주간 격자의 강조 스위치를 화면까지 확장

- 변경 파일: `frontend/src/components/home/WeekTimetable.tsx`
- 요약: `강조 없이`(내보내기 전용) → `현재 요일 표시`(기본 켜짐)로 뒤집고, 끄면
  화면에서도 오늘 열·요일 머리·지금 칩의 핑크가 사라지게 했습니다

**내보내기 전용 옵션이 잘못된 자리였습니다.** 결과를 화면에서 확인할 방법이 없어서,
어떻게 찍히는지 알려면 실제로 찍어서 파일을 열어 봐야 했습니다. 화면이 곧 미리보기가
되게 하니 그 왕복이 사라집니다.

덤으로 **`requestAnimationFrame` 두 번 넘기기가 통째로 없어졌습니다.** 찍는 순간에만
강조를 뗐다 되돌리려니 React 가 다시 그리고 브라우저가 칠할 때까지 기다려야 했는데,
화면 상태가 곧 내보낼 상태라 그냥 찍으면 됩니다. ⚠️ 다시 내보내기 전용으로 되돌리면
그 곡예도 같이 돌아옵니다.

기본값은 켜짐입니다 — 홈은 "지금" 을 말하는 화면이고, 강조를 끄는 건 시간표를 남에게
보낼 때처럼 **오늘이 뜻을 잃는 자리**에서만 하는 일입니다. 끈 상태를 기억하지는
않습니다(새로 열면 다시 켜집니다). 매번 끄게 되는 일이 잦아지면 그때 `ksa_home_view`
처럼 기기에 남기면 됩니다.

## 2026-08-19 — 홈: 과목 목록 · 공강 강조 · 시간표에서 검색 · PNG 강조 제외

- 변경 파일: `frontend/src/components/home/{MySubjects(신규),WeekTimetable}.tsx`,
  `frontend/src/components/TodayTimeline.tsx`, `frontend/src/lib/{searchEngine,homeView}.ts`,
  `frontend/src/pages/HomePage.tsx`
- 요약: 홈 맨 아래에 수강 과목 목록을 붙이고, 시간표 글자를 검색으로 잇고, PNG 를
  강조 없이 찍는 선택지를 넣었습니다. 주간 격자 제목은 `Timetable` 로 바꿨습니다

**⚠️ 문서에 적혀 있던 규칙 하나를 뒤집었습니다 — "공강에는 지금을 붙이지 않는다".**
근거는 "핑크는 뭘 하고 있다는 뜻이니 빈 자리를 제일 세게 만들지 말자" 였고, 자·격자
에서는 그대로 둡니다. 다만 **목록에는 캐럿이 없습니다** — 공강 시간에 목록만 보면
지금이 어디인지 짚어 주는 게 하나도 없었습니다. 그래서 목록의 공강 줄만 예외로 두되
**면을 수업(12%)보다 옅게(8%)** 칠했습니다. 두 농도가 뒤집히면 원래 걱정이 그대로
현실이 되니, 옅게 두는 쪽을 지켜야 합니다.

**시간표 글자를 검색으로 이었습니다.** 주간 격자 칩과 오늘 목록에서 과목명·분반·교사를
누르면 각각 그 과목 / 그 분반 / 그 선생님이 검색됩니다. ⚠️ **검색어를 손으로 만들면
안 됩니다** — `( ) + & / ! :` 가 전부 질의 문법의 글자라, `미적분학2(EC)` 를 그대로
넘기면 `(EC)` 가 논리식 괄호로 파싱되고 `Cognitive Neuroscience: The…` 는 `:` 앞이
prefix 로 잘립니다. `searchEngine` 에 `subjectQuery`·`sectionQuery`·`teacherQuery` 를
두고 거기서만 만들게 했습니다.

**괄호는 지우되 안의 글자는 남깁니다.** `fuzzyMatch` 의 `normalize()` 가 검색 대상에서도
구두점을 떼기 때문에, `미적분학2 EC` 는 `미적분학2(EC)` 에만 붙고 한국어반 `미적분학2`
에는 안 붙습니다 — 통째로 잘라 내면 그 구분이 사라집니다. 실제 엔진에 넣어 확인했습니다
(EC·한글 괄호 특강·영문 콜론 과목 모두 그 과목만 나옵니다).

**PNG 는 `강조 없이` 체크박스로 오늘·지금을 뺀 채 찍습니다.** 남에게 보내는 그림에서
"오늘"·"지금" 은 찍는 순간의 사정일 뿐이라 다음 날부터 틀린 표시가 됩니다. ⚠️ 함정은
**상태를 켠 즉시 찍으면 강조가 그대로 담긴다**는 것 — React 가 다시 그리고 브라우저가
칠할 때까지 `requestAnimationFrame` 을 두 번 넘긴 뒤에 찍습니다. 요일 머리·열 기둥·
지금 칩이 **같은 값(`markDay`)** 을 보게 묶어서, 한 군데만 강조가 남는 일이 없습니다.

**맨 아래 과목 목록은 격자가 못 하는 말을 합니다.** 격자는 "언제" 라서 같은 과목이
요일마다 흩어지는데, 몇 과목·몇 학점인지는 목록이 훨씬 잘 셉니다. 접힌 줄에도 분반·
교사를 남긴 이유는, 그게 없으면 "내가 몇 분반이지" 를 확인하려고 열두 줄을 다 열게
되기 때문입니다. 학점을 모르는 과목이 있으면 합계에 `+` 를 붙입니다 — 0으로 세고
더하면 틀린 합계가 확신에 차 보입니다.

## 2026-08-19 — 홈 시간표 전환을 기기에 기억

- 변경 파일: `frontend/src/pages/HomePage.tsx`
- 요약: `[기존 시간표 | 트레이드 계획]` 에서 고른 쪽을 `ksa_home_view` 에 남겨,
  트레이드 계획을 홈의 기본값으로 둘 수 있게 했습니다

설정 화면을 따로 만들지 않았습니다 — **마지막에 고른 쪽이 곧 기본값**입니다. 계획을
짜 놓고 정정 기간 내내 그걸 보는 사람이 홈을 열 때마다 같은 버튼을 누른다면, 그건
기본값을 잘못 잡았다는 뜻이지 설정이 없다는 뜻이 아닙니다. 저장 방식은 학기 선택
(`ksa_selected_term`)과 같은 localStorage 이고, 계정이 아니라 기기에 붙습니다.

⚠️ **어제 넣었던 "계획이 없으면 `view` 를 `current` 로 되돌리는" 효과를 지웠습니다.**
기억한 값으로 열기 시작하면 이게 **계획을 받아 오는 동안 먼저 돌아서**, 기억해 둔
`trade` 가 매번 `current` 로 덮이고 기본값이 한 번도 안 먹습니다. 화면이 계획인지는
상태를 고쳐서 정할 게 아니라 `planned = view === "trade" && plannedSections !== null`
로 파생시켜야 그 경합이 없습니다 — 다시 효과로 되돌리지 마세요.

기억은 **버튼을 누른 순간에만** 적습니다. 계획이 없어 기존 시간표가 보이는 건 사용자가
고른 게 아니라서, 그걸로 덮으면 계획이 돌아왔을 때 또 눌러야 합니다.

## 2026-08-18 — 홈에서 기존/트레이드 시간표 전환

- 변경 파일: `frontend/src/pages/HomePage.tsx`, `frontend/src/lib/{plannedHome(신규),tradeEngine}.ts`,
  `frontend/src/hooks/useTradePlan.ts`(신규), `frontend/src/components/home/{TodayCardV3,WeekTimetable}.tsx`,
  `frontend/src/pages/TradePage.tsx`, `frontend/src/App.tsx`
- 요약: 수강 정정 기간에 `/trade` 계획이 있으면 홈 위에 `[기존 시간표 | 트레이드 계획]`
  이 붙고, 고르면 홈 전체가 계획 시간표로 갈립니다

**화면을 두 벌 만들지 않았습니다.** `HomeData` 의 `today`·`week`(그리고 `current`·`next`)
만 갈아 끼운 사본을 아래로 흘려보냅니다(`lib/plannedHome.ts`). 히어로·자·목록·주간
격자가 각자 "계획일 때는 이렇게" 를 알 필요가 없고, 파생값은 종전대로 `homeView.ts`
한 곳에서 셉니다. 카드마다 분기를 심는 길로 갔으면 네 자리가 따로 놀았을 겁니다.

**계획 계산은 `tradeEngine.buildPlannedSchedule` 하나로 모았습니다.** 원래 트레이드
화면의 80줄짜리 `useMemo` 안에 있던 것을 그대로 끌어냈고, 홈은 같은 함수를 부릅니다.
⚠️ 여기서 갈라지면 같은 계획인데 두 화면의 시간표가 달라집니다.

**⚠️ `previewKey`(목록에서 고른 조합)를 저장 항목에 새로 넣었습니다.** 이게 없으면
홈과 트레이드가 각자 첫 조합을 골라서, "자동" 으로 맡긴 항목이 있을 때 결과가
갈립니다. 저장 형식이 늘어난 것이라 되돌리려면 두 화면을 같이 봐야 합니다.

**남의 계획은 안 그립니다.** 트레이드는 친구 계획을 봐주려고 다른 학생도 열 수 있어서,
저장된 `stuId` 가 내 학번이 아니면 홈에서는 전환 자체가 안 뜹니다. 계획이 시간표를
하나도 안 바꿀 때(`sameSections`)도 마찬가지입니다 — 같은 걸 두 번 보여 줄 이유가
없습니다.

**함정 둘.**

1. 교실은 `t.room || s.room` 순서로 골라야 합니다. 서버(`/home`)가 교시별 교실을
   먼저 보고 없을 때만 분반 교실을 쓰는데, 분반 교실을 먼저 쓰면 실험 분반에서 두
   시간표가 어긋납니다
2. `useTradePlan` 은 못 불러왔을 때 조용히 `null` 로 둡니다. 트레이드 화면 쪽 ⚠️
   (`restored` 를 켜면 안 된다)와 반대인 이유는 **홈은 읽기만 하기 때문**입니다 —
   저장을 안 하니 덮어쓸 위험이 없습니다

계획을 보는 중이라는 표식은 시안(Trade 색)입니다 — 히어로에 `계획 미리보기`, 주간
격자에 `계획`, PNG 파일 이름에 `_계획`. 아직 등록된 시간표가 아니라서, 어느 쪽을 보고
있는지 헷갈리면 엉뚱한 교실로 갑니다.

## 2026-08-18 — 홈 재설계 (V3 배포본 · 학과색 · 주간 격자 개편)

- 변경 파일: `frontend/src/components/home/{TodayCardV3(신규),WeekTimetable}.tsx`,
  `frontend/src/components/{DayRuler,TodayTimeline}.tsx`,
  `frontend/src/lib/{homeView,utils}.ts`, `frontend/src/pages/HomePage.tsx`
- 요약: 레이아웃 판본 V3 를 배포본으로 올리고, 홈 전체에 학과색을 도입했습니다.
  주간 격자는 오늘 목록과 2열로 나란히 서고 PNG 로 내보낼 수 있습니다

레퍼런스로 받은 화면들에서 가져온 건 **배치뿐**입니다 — 큰 진입점, 주인공에게 준 면적,
시간의 양을 보여 주는 막대, 늘 보이는 "다음". 디자인 언어(굵은 테두리·하드 쉐도우·
직각)는 그대로입니다.

**V2 가 평평했던 원인은 폭이 아니라 면적이었습니다.** 지금·수업·급식 세 칸에 폭을
달리 줬는데도 같은 높이로 서 있으니 같은 무게로 읽혔습니다. 폭을 더 조정하는 길로
가면 안 됩니다.

⚠️ **문서에 적혀 있던 두 규칙을 뒤집었습니다.**

1. **"과목마다 색을 달리하지 마세요"** → 학과색을 씁니다. 예전에 실패한 건 팔레트를
   **면으로 꽉 채워서**였고, 문제는 색이 아니라 **농도**였습니다. 8~14% 로 깔고 100%
   는 "지금" 에만 남기면 색이 소음이 아니라 좌표가 됩니다
2. **"오늘을 핑크로 칠하지 마세요"** → 오늘 머리(25%)와 열 기둥(7%)이 핑크입니다.
   검정으로 뒤집었더니 흰 바탕에서 그 칸이 격자에서 제일 무거웠습니다

**"지금" 을 형광으로 꽉 채우지 않습니다.** 자·목록·격자 어디서든 100% 로 칠하면 그 칸
하나가 화면에서 제일 센 물건이 되어 옆을 읽는 걸 방해했습니다 — 학과색 칩과 같은
문법(옅은 채움 + 진한 테두리)에 색만 핑크입니다.

**시간을 재는 물건은 길이가 시간에 비례해야 합니다.** 지적을 받고 셋을 고쳤습니다 —
목록의 공강 줄 높이(수업과 같게), 목록의 연강 줄 높이(교시 수 × 3.25rem), 격자의
점심·저녁 구멍(교시 칸에 비례). 70분 점심이 16px 이면 50분 수업(64px)보다 짧아 보입니다.

**연강은 네 자리가 같은 조건으로 잇습니다** — 자·오늘 목록·주간 격자·히어로. 하나만
안 이으면 같은 하루가 자리마다 다르게 세어지고, 히어로는 20:20 에 끝난다면서 NEXT 에
같은 과목을 또 겁니다.

**함정 셋** (같은 실수를 반복하지 않게)

- ⚠️ `gridRow: "2 / -1"` — 행이 **암시적 그리드**면 `-1` 이 끝을 못 짚고 조용히 **헤더
  한 칸 높이**로 쪼그라듭니다. 오늘 기둥이 42px 만 칠해지던 원인이고, 에러가 없어서
  스크린샷으로도 한 번 놓쳤습니다. `2 / span {행 수}` 로 세야 합니다
- ⚠️ `overflow-x-auto` 만 주면 `overflow-y` 가 `auto` 로 계산됩니다 — 칸의 하드 쉐도우
  2px 때문에 **세로 스크롤**이 생깁니다. `overflow-y-hidden` + `py-1`
- ⚠️ **글자 하한은 12px** 입니다. `김정은 · 1분반` 같은 부연을 10px 로 줄였더니 그냥
  안 읽혔습니다 — 줄이 답답하면 글자가 아니라 **여백**을 건드립니다

`html-to-image` 를 새로 넣었습니다(주간 격자 PNG 내보내기). ⚠️ **스크롤 상자가 아니라
안쪽 격자를 `scrollWidth/scrollHeight` 로 찍어야** 화면 밖 교시까지 담깁니다.

확인은 Playwright 로 실제 화면을 띄워서 했습니다 — 로컬 DB 에 임시 세션을 넣어
로그인하고, DEV 상태 스위치로 공강·수업중을 오갔습니다. 스크린샷은
`~/Bufs/Claude Files/2026-08-18/`.

## 2026-08-18 — 홈 레이아웃 V3 (배포본 교체)

- 변경 파일: `frontend/src/components/home/TodayCardV3.tsx`(신규), `frontend/src/pages/HomePage.tsx`
- 요약: 세 칸을 나란히 세우던 V2 를 걷어내고 행을 둘로 갈랐습니다 — 위는 "지금"(히어로 + 오늘 일정), 아래는 하루(자 + 스크롤 목록 + 급식). V2 는 V1 과 함께 개발 전용으로 내렸습니다

레퍼런스로 받은 화면 넉 장은 전부 **부드러운 스킨**이었지만 가져온 건 배치뿐입니다 —
큰 진입점, 주인공에게 준 면적, 시간의 양을 보여 주는 막대, 늘 보이는 "다음". 디자인
언어(굵은 테두리·하드 쉐도우·직각)는 그대로 두었습니다.

**V2 가 평평했던 원인은 폭이 아니라 면적이었습니다.** 지금·수업·급식 세 칸에 폭을
달리(18rem/1fr/17rem) 줬는데도 셋이 같은 높이로 서 있으니 같은 무게로 읽혔습니다.
폭을 더 조정하는 길로 가면 안 됩니다 — 행을 갈라야 위아래 크기가 달라집니다.

**히어로는 문서에 적혀 있던 부활 조건을 채우고 세웠습니다.** 예전에 "지금 무슨 수업"
을 큰 글씨로 외치는 카드를 뺀 이유가 *바로 아래 핑크 줄이 똑같은 말을 해서* 였고,
그때 "목록이 못 하는 말(남은 시간·이동 여부)을 시키면 다시 세워도 된다" 를 같이 적어
두었습니다. 그래서 히어로가 말하는 셋은 전부 목록이 못 하는 것입니다 — 남은 시간
진행바, 늘 보이는 NEXT(V2 는 공강일 때만 말했습니다), 그리고 `current.room !==
next.room` 으로 판단하는 **교실 이동**. ⚠️ **셋 중 하나라도 빼면 예전에 걷어냈던 그
카드로 돌아갑니다.**

자(`DayRuler`)는 V1 이후 처음 돌아왔습니다. V2 에서 빠진 건 자리가 없어서였는데,
"지금" 이 위로 올라가며 아래 칸이 통째로 비었습니다.

**이어서 — 자와 목록이 "탁하다" 는 지적을 받고 고쳤습니다.** 화면을 직접 보니 원인이
셋이었습니다.

1. ⚠️ **자의 지난 교시가 `bg-black/15` 로 면을 채우고 있었습니다.** 저녁에 열면 지나간
   칸이 예닐곱이라 자가 통째로 잿빛이었습니다 — `design-guide.md` 의 **"면을 채우는
   건 지금 하나뿐"** 규칙을 어긴 게 그 규칙이 적힌 컴포넌트 자신이었습니다. 흰 배경에
   `border-black/25` + 그림자 없음으로 바꿔, 지남과 예정을 **테두리 세기와 그림자
   유무**로만 가릅니다
2. **공강 줄이 수업 줄과 같은 크기였습니다**(`py-3` · 15px). 공강 네 시간짜리 날이면
   목록의 절반이 "공강" 이라 수업을 찾으려면 그 사이를 훑어야 했습니다 — `py-1.5` ·
   12px 로 낮췄습니다
3. **스크롤 상자가 목록의 첫 줄을 반쯤 지운 것처럼 보였습니다.** 안쪽 목록이 제
   테두리를 갖고 있어 그게 같이 스크롤된 탓이라, 경계선을 **상자 바깥**으로 옮기고
   높이를 13rem → 17rem 으로 키웠습니다

지난 줄의 `opacity-40` 도 55 로 올렸습니다 — 40 은 "지났다" 가 아니라 "안 읽힌다" 였습니다.

확인은 Playwright 로 실제 화면을 띄워서 했습니다(로컬 DB 에 임시 세션을 넣어 로그인).
스크린샷은 `~/Bufs/Claude Files/2026-08-18/home-v3-{before,mid,after}.png`.

옛 판본 분기에 `import.meta.env.DEV &&` 를 그대로 유지했습니다 — 없으면 rollup 이 세
판본을 다 싣습니다. 빌드 후 `dist/assets/HomePage-*.js` 에서 V1·V2 고유 문구가 안
잡히는 것으로 확인했습니다.

## 2026-08-18 — 수집: 이름을 KEIS 로 · 학기 선택 · 반영 직전 DB 스냅샷

- 변경 파일: `backend/backup.py`(신규), `backend/parser_run.py`, `backend/admin_router.py`,
  `frontend/src/pages/AdminPage.tsx`, `frontend/src/pages/SettingsPage.tsx`, `.gitignore`
- 요약: Admin 의 `Fetch from KSAIN` 버튼이 실제로는 KEIS 를 부르고 있었습니다. 이름을
  맞추고, 어느 학기를 받을지 모달에서 고르게 하고, 갈아 끼우기 직전에 DB 스냅샷을
  남깁니다.

**이름부터.** 데이터 소스를 `api.ksain.net` → `keis.ksa.hs.kr` 로 옮긴 게 한참 전인데
버튼 라벨만 옛 이름이었습니다. ksain.net 은 지금 **급식만** 담당합니다(`KSAIN_API_KEY`) —
그래서 `/about` 의 출처 줄은 `KEIS (수업) · KSAIN (급식)` 으로 나눠 적었습니다. 환경변수
이름은 그대로 둡니다. 그건 진짜 ksain 것입니다.

**학기 선택은 백엔드가 이미 받고 있었습니다.** `POST /admin/sync` 는 처음부터 `year`·
`semester` 를 받는데 화면이 빈 body 로만 불러서, 언제나 DB 최신 학기만 갈렸습니다. 모달에
`/admin/terms` 로 받은 학기 칩을 깔고 `새 학기` 는 연도 직접 입력으로 뒀습니다 — **DB에
없는 학기를 처음 채우는 길**이 그거라서, 칩만으로는 새 학기를 영영 못 만듭니다.

**⚠️ 백업을 못 만들면 수집을 접습니다.** 재수집은 한 학기를 통째로 갈아 끼우는 작업이고,
API 가 이상한 값을 줬거나 학기를 잘못 골랐다는 걸 나중에 알아채면 되돌릴 곳이 파일뿐입니다.
그래서 백업 실패를 경고가 아니라 중단으로 뒀습니다(`--no-backup` 으로 생략 가능).
파일 복사가 아니라 sqlite `backup()` API 를 씁니다 — 쓰는 중에 떠도 반쪽이 안 나옵니다.

**자동 삭제는 넣지 않았습니다.** 오래된 것부터 지우는 규칙을 두면 하필 그게 필요한 날에
없습니다. 대신 `/admin/backups` 가 총 용량을 같이 주고, 화면에 그 숫자를 띄워 사람이
판단하게 했습니다. 한 번에 약 3MB 씩 쌓입니다. 예전에 손으로 떠 둔
`ksa_timetable.db.bak-*` 는 **옮기지 않고** 목록에만 `legacy` 로 얹었습니다 — 파일을
움직이면 그게 뭐였는지 아는 사람의 기억과 어긋납니다.

복원 버튼은 두지 않았습니다. 목록에서 파일명을 보고 서버에서 직접 바꿔 끼우는 편이,
잘못 누르면 DB 가 통째로 뒤로 가는 버튼보다 낫습니다.

검증: 수집 경로를 실제로 태우되 `replace_term_data` 만 가로채 원본 DB 를 지켰습니다 —
2026-2 기준 677명 중 366명 수집·실패 0·26.3초, 백업 2.8MB 정상. 겸사겸사 결과 줄의
`{elapsed}s` 가 `26.3ss` 로 찍히던 것도 고쳤습니다(서버가 이미 `s` 를 붙여 줍니다).

## 2026-08-18 — 안드로이드 앱 (홈 화면 위젯: 지금·급식)

- 변경 파일: `android/` (신규), `CLAUDE.md`, `.gitignore`
- 요약: Kotlin + Jetpack Glance 로 위젯 두 개. 앱 화면은 로그인·새로고침·위젯 추가·
  로그아웃뿐입니다. 웹과 **같은 서버, 같은 세션**을 씁니다.

시간표를 앱에서 다시 그리지 않았습니다. 웹이 이미 하고 있고, **폰에서 웹으로 안 되는
건 홈 화면에 얹히는 것 하나**입니다. 그것만 하는 앱이면 유지할 게 적습니다.

앱보다 다중 기기 로그인을 먼저 한 이유가 여기서 드러납니다 — 앱은 웹과 다른 기기라,
1계정 1세션이던 시절에는 앱에서 로그인하는 순간 브라우저가 튕겼습니다. 앱 전용 API 는
하나도 만들지 않았습니다.

**⚠️ 위젯은 네트워크를 직접 치지 않습니다.** `RefreshWorker`(WorkManager)가 미리 받아
`SharedPreferences` 에 `Snapshot` 하나로 넣어 두고, 위젯 둘이 그것만 읽고 그립니다.
런처가 위젯을 그리는 순간에 통신하면 홈 화면이 그동안 멈춥니다. 스냅샷을 하나로 둔 건
웹의 `lib/homeView.ts` 와 같은 이유입니다 — 위젯마다 따로 계산하면 둘이 다른 시각을
말합니다.

갱신 주기는 **15분이 바닥**입니다(안드로이드가 `PeriodicWorkRequest` 에 건 하한). 교시는
50분마다 바뀌니 최악의 경우 15분 늦습니다. 정확히 맞추려면 교시 경계마다
`setExactAndAllowWhileIdle` 을 걸어야 하는데 `SCHEDULE_EXACT_ALARM` 권한 창이 뜨고
도즈에서 배터리 경고가 붙습니다 — **15분 늦는 쪽을 골랐습니다.** 위젯을 여는 이유는
"지금 어디 가야 하지" 지 초 단위 정확도가 아닙니다.

### 디자인을 한 번 갈아엎었습니다

처음엔 교시를 작은 핑크 칩으로 얹고 나머지를 글자로만 뒀는데, 실제로 홈 화면에 올려
보니 **아래 절반이 통째로 비고 색이 손톱만큼**이라 아무 말도 못 했습니다. 웹에서 "지금"
은 카드 폭을 꽉 채우는 핑크 띠인데 그게 칩 하나로 쪼그라든 셈이었습니다.

지금은 **색 덩어리 하나가 위젯의 대부분**입니다. 지금 수업이 있으면 핑크, 공강이면 흰
덩어리에 다음 수업 — 색이 곧 "이미 시작했는가" 입니다. 급식은 끼니색(아침 노랑·점심
주황·저녁 보라)을 씁니다. ⚠️ **급식에 핑크를 쓰면 안 됩니다** — 핑크는 옆 위젯이 쓰는
"지금" 입니다.

### 안드로이드 쪽 함정 셋

- **⚠️ Glance 에는 `border` modifier 가 없습니다.** 위젯은 RemoteViews 로 그려지고
  RemoteViews 가 테두리를 모릅니다. 배경 자체를 **선이 그려진 그림**으로 깝니다
- **⚠️ Android 12+ 런처가 위젯을 둥글게 잘라냅니다.** 직각으로 그렸더니 잘리는 게
  모서리만이 아니라 **테두리 선 자체**였습니다 — 윗변만 남고 좌·우·아랫변이 통째로
  사라졌습니다(이 디자인은 그 선이 전부입니다). 막을 수 없어서 맞췄습니다:
  `@dimen/widget_corner` 가 v31 이상에서 시스템 반경을 받습니다. 안쪽 덩어리는 사방에서
  물러나 있어 마스크에 안 닿으므로 직각 그대로입니다
- **⚠️ 급식 위젯 기본 크기를 3×2 로 줄이지 마세요.** 3×3 이 절반쯤 비어 보여서 줄여
  봤는데 여섯 줄짜리 메뉴에서 **마지막 줄이 글자 중간에서 잘렸습니다.** 스크롤은 되지만
  잘린 줄은 고장으로 읽힙니다 — 꼬리의 빈 자리는 목록 위젯이면 다 그렇습니다

`requestPinGlanceAppWidget` 으로 **앱 안에 "위젯 추가" 버튼**을 뒀습니다. 없으면 홈을
길게 눌러 → 위젯 → 목록에서 앱을 찾아 → 끌어다 놓아야 하는데, 이 앱의 전부인 기능을
앱이 안 알려 주는 건 이상합니다.

### 검증

에뮬레이터(API 36.1)에 올려 위젯 둘을 홈 화면에 실제로 놓고 확인했습니다. 로그인 없이
그림만 보려면 `Store` 를 `run-as` 로 직접 심으면 됩니다 — 위젯은 그것만 읽습니다.
⚠️ **`input swipe` 로 드래그를 흉내 내면 런처가 안 집어 듭니다** — 앱의 "위젯 추가"
버튼 → 시스템 확인 창 탭이 확실합니다.

⚠️ JDK 가 PATH 에 없습니다(homebrew keg-only). `export JAVA_HOME=/opt/homebrew/opt/openjdk@21`.

## 2026-08-18 — 다중 기기 로그인 (1계정 1세션 해제)

- 변경 파일: `backend/auth.py`, `backend/auth_router.py`, `backend/models.py`,
  `backend/migrations.py`, `backend/admin_router.py`,
  `frontend/src/lib/sessionsApi.ts`, `frontend/src/components/DeviceSessions.tsx`,
  `frontend/src/pages/SettingsPage.tsx`
- 요약: 한 계정이 기기 5대까지 동시에 로그인합니다. 로그아웃은 이 기기만 끊고,
  `/about` 에 기기 목록·폐기 화면이 생겼습니다.

폰 앱을 붙이기 전에 먼저 해야 하는 일이라 순서를 당겼습니다. 앱은 웹과 다른 기기에서
로그인하는데, 지금 구조로는 둘이 서로를 밀어냅니다.

**표는 처음부터 1:N 이었는데 코드가 1:1 을 강제하고 있었습니다.** `sessions` 에
`user_id`·`device_type`·`ip_address`·`last_used_at` 이 다 있고 `GET /auth/sessions` 와
`DELETE /auth/sessions/{id}` 까지 구현돼 있었는데, `login()` 이 맨 앞에서
`clear_user_sessions()` 로 기존 행을 통째로 지우고 있었습니다(주석: "1계정 1세션").
`logout()` 도 같은 함수를 불렀고요. 그래서 **스키마 마이그레이션 없이 두 줄이 문제**
였습니다.

지우려다 멈춘 이유는 그게 실수가 아니라 의도로 보였기 때문입니다 — 계정을 관리자가
발급하는 서비스라 **계정 공유를 막는 장치**였습니다. 그래서 없애지 않고 **상한**으로
바꿨습니다. 반 전체가 한 계정을 쓰면 서로를 밀어내서 아무도 못 쓰니 목적은 그대로
지켜지고, 본인이 폰·노트북을 같이 쓰는 길만 열립니다. 상한은 5(폰 + 노트북 + 학교 PC +
태블릿 + 여유 1).

⚠️ **상한을 넘겼을 때 새 로그인을 거절하지 않습니다.** 거절이 더 곧은 방법 같지만,
폰을 잃어버렸거나 브라우저를 갈아엎은 사람이 **영영 못 들어옵니다** — 자기 세션을
지우려면 로그인부터 해야 하니까요. 가장 오래 안 쓴 세션을 밀어냅니다.

⚠️ **`logout-all` 은 누른 기기를 남깁니다.** 비밀번호가 샜다고 느낄 때 누르는 버튼인데
누른 사람까지 튕겨 내면 곧바로 다시 로그인해야 해서 오히려 불안합니다.

**만료를 슬라이딩으로 바꾼 건 위젯 때문입니다.** 고정 30일이면 매달 한 번씩 전원이
영문 모르고 로그아웃되는데, 특히 폰 위젯처럼 배경에서 도는 물건은 어느 날 조용히 빈
채로 남고 사용자는 이유를 알 수 없습니다. `get_current_session` 이 **창의 절반이
지났을 때만** 30일을 다시 채웁니다 — 매 요청마다 값을 바꾸면 SQLite 에 의미 없는
쓰기가 계속 쌓입니다. ⚠️ 대가로 **탈취된 토큰도 계속 삽니다.** 그래서 세션 목록과
폐기 버튼을 화면에 내놓는 게 선택이 아니라 짝입니다.

`get_current_user` 를 `get_current_session` 위에 얹었습니다. 로그아웃이 "이 기기만"
이 되려면 **어느 행으로 들어왔는지**를 알아야 하는데, 예전엔 유저만 넘겨받아서 계정의
세션을 전부 지우는 것 말고 할 수 있는 게 없었습니다.

`device_label` 컬럼(`"Chrome · Android"`)을 추가했습니다. 이게 없으면 목록에 `web`
세 줄이 나란히 떠서 **어느 게 잃어버린 폰인지 못 가리고**, 그러면 폐기 버튼이
무용지물입니다. 서버가 User-Agent 에서 뽑습니다 — 클라이언트가 보낸 이름을 쓰지 않으니
폰 앱은 자기 User-Agent 를 알아보게 붙이면 그대로 잡힙니다.

⚠️ **UA 파서는 순서가 곧 규칙입니다.** Edge 는 `Chrome/` 을, Chrome 은 `Safari/` 를,
안드로이드는 `Linux` 를 달고 다닙니다. 뒤에서부터 찾으면 전부 `Safari · Linux` 가 됩니다.

⚠️ **`GET /auth/sessions` 응답이 배열에서 객체로 바뀌었습니다** (`{max, sessions}`).
화면이 "5대 중 3대" 를 말하려면 상한이 필요해서인데, 예전 판본을 그대로 `map()` 하는
코드가 있으면 터집니다. 지금은 부르는 데가 새 화면 하나뿐입니다.

화면(`DeviceSessions`)에서 되돌리면 안 되는 것 하나 — **"이 기기" 표시**. 없으면 지금
보고 있는 세션을 스스로 끊고 그 자리에서 로그인 화면으로 튕깁니다. 그 줄에는 끊기
버튼을 아예 안 답니다.

검증은 실제 서버에 던져서 했습니다 — 세 기기 동시 로그인, 한 기기 로그아웃 시 나머지
생존, 7번 로그인 후 5개로 정리되고 가장 오래된 것이 401, `logout-all` 후 현재 기기만
생존, 슬라이딩 만료 3케이스(20일 남음 → 그대로 / 3일 남음 → 30일로 / 만료 → 401).

## 2026-08-18 — 홈 주간 시간표 재디자인 (표 → 지도)

- 변경 파일: `frontend/src/components/home/WeekTimetable.tsx`
- 요약: 스프레드시트처럼 그리던 격자를 **테두리 있는 칩 + 빈 자리**로 바꾸고, 글자를
  위쪽 정렬로 옮겨 연강이 몇 교시에 시작하는지 읽히게 했습니다. 점심·저녁 구멍을
  빗금 띠로 새깁니다.

붙인 지 일주일 만에 "존나 구리다" 는 평을 받았고, 실물을 띄워 보니 두 가지가 동시에
망가져 있었습니다.

**첫째, 주의 모양이 안 보였습니다.** 수업 칸을 `bg-black/[0.05]` 로, 격자선을
`border-black/10` 으로 그렸는데 — 굵은 검은 선이 기본인 앱에서 5% 회색은 아무 말도 안
합니다. 한 주가 통째로 옅은 얼룩이라 **어디가 비었는지 세어야** 알 수 있었습니다.

**둘째, 어느 교시인지 못 읽었습니다.** 이게 더 심각했습니다. 글자를 칸 가운데에 뒀더니
연강 덩어리(`일반지구과학` 6–7교시)의 이름이 6과 7 **사이**에 걸려서, 6교시 수업인지
7교시 수업인지 알 방법이 없었습니다. 시간표가 교시를 못 알려 주고 있었습니다.

고친 방향은 새로 지어낸 게 아니라 `design-guide.md` 가 자(`DayRuler`)에서 이미 쓰던
언어를 가져온 것입니다 — **테두리는 칸마다, 빈 자리는 진짜 비움.** 검은 덩어리가 있는
곳이 수업이고 흰 곳이 공강이라, 이제 모양이 먼저 보입니다. "카드 안에 카드를 넣지
마세요" 와 부딪히지 않는 근거도 자와 같습니다 — 목록이 아니라 눈금이고 칸 하나하나가
데이터입니다. 다만 하드 쉐도우는 안 붙였습니다(스물일곱 개면 자갈밭이 됩니다).

⚠️ **점심·저녁을 격자에 새긴 건 보기 좋으라고가 아닙니다.** 4교시(~12:30)와 5교시
(13:40~) 사이는 70분인데 번호는 이어집니다 — 그래서 같은 과목이 4·5교시에 있으면
**점심을 가로지르는 연강**으로 묶이고 있었습니다. `periods` 에서 20분 넘게 벌어진 자리를
찾아 띠를 넣고, 그 위로는 잇지 않습니다. 하드코딩이 아니라 파생값이라 교시 시각표가
바뀌어도 따라갑니다. 띠 이름은 `breaks` 중 **가장 많이 겹치는** 것을 고릅니다 — 점심
구멍에는 `점심`(40분)과 `학급모임`(20분)이 둘 다 걸쳐서, 처음 찾은 걸 쓰면 배열 순서에
따라 답이 달라집니다.

되돌리기 쉬운 지점 셋을 남겨 둡니다. 전부 한 번씩 잘못 짚었다가 화면을 보고 되돌린
것들입니다.

- **세로 격자선을 빼지 마세요.** 칩의 검은 테두리가 요일을 가른다고 보고 뺐는데,
  **수업이 없는 행에서는 가를 게 없어서** 8교시 목요일을 찾으려면 요일 머리부터 짚어
  내려와야 했습니다. 격자는 여기서 장식이 아니라 좌표입니다
- **교실에 `mt-auto` 를 걸지 마세요.** 칩 바닥에 밀면 두 교시짜리에서 그 자리가 **아래
  교시**여서, 6–7교시 수업의 교실이 7교시 것처럼 읽힙니다
- **오늘을 핑크로 칠하지 마세요.** 핑크는 "지금" 입니다. 오늘 요일 머리까지 핑크였어서
  한 화면에 같은 색이 두 뜻으로 있었습니다 — 검은 바에서 흰색으로 **반전**하면 색을
  안 쓰고도 됩니다

행 높이가 **좁은 화면에서 더 큰 것**(52px vs 44px)도 일부러입니다. 폰은 한 칸이 60px
남짓이라 이름이 두 줄로 펴져야 하고, 데스크톱은 200px 이라 어떤 과목명도 한 줄에
들어갑니다. 폭이 없으면 높이로 갚습니다. 덕분에 폰에서 교실을 다시 띄울 수 있었습니다 —
예전엔 `md:` 아래에서 통째로 숨겨서 폰에서는 "어디로 가지" 를 알 수 없었습니다.

이름은 꼬리 괄호를 뗍니다(`융합특강(AI시대의인간기술)` → `융합특강`). `(EC)` 도 뗐는데,
작은 꼬리표로 남겼더니 그 8px 이 `미적분학2` 를 `미적분학…` 으로 밀어냈습니다. **여긴
내 시간표라 (EC) 가 가리는 게 없습니다** — 같은 과목의 한국어반과 영어반을 동시에 듣는
사람은 없으니까요. 구별이 필요한 화면(검색·트레이드)의 `getKoreanName()` 은 그대로입니다.

## 2026-08-15 — `/auth/google` 에서 흰 화면 (vite `base: './'`)

- 변경 파일: `frontend/vite.config.ts`
- 요약: 상대 base 때문에 **두 단계 주소**에서 에셋 경로가 밀려 스크립트가 아예 안
  떴습니다. `base: '/'` 로 고쳤습니다.

구글에서 돌아오면 흰 화면만 나온다는 제보로 잡았습니다. 콘솔에는 MIME 오류 세 줄:

```
https://classes.bsiku.dev/auth/assets/index-….js
                          ^^^^^ 여기
Failed to load module script … MIME type of "text/html"
```

`base: './'` 라 `index.html` 이 `./assets/…` 를 가리키는데, SPA 는 **어느 주소로 들어와도
같은 파일**을 받습니다. 그래서 주소 깊이만큼 경로가 밀립니다.

| 주소 | `./assets/x.js` 가 가리키는 곳 | |
|---|---|---|
| `/` | `/assets/x.js` | ✅ |
| `/search` | `/assets/x.js` | ✅ 한 단계는 **우연히** 맞습니다 |
| `/auth/google` | `/auth/assets/x.js` | ❌ Netlify 폴백이 HTML 을 돌려줍니다 |

⚠️ **지금까지 라우트가 전부 한 단계여서 안 터졌던 것뿐입니다.** `/auth/google` 이 이 앱의
**첫 두 단계 주소**였습니다. 앞으로 `/foo/bar` 를 하나라도 더 만들면 같은 자리에서 같은
방식으로 죽습니다 — `base` 는 절대 경로로 두세요.

`base: './'` 는 `c5941ea hotfix` 에서 들어온 값인데 그때 왜 필요했는지는 남아 있지
않습니다. 루트 도메인에 배포하므로 상대 경로일 이유가 없습니다.

**증상이 흰 화면인 게 함정입니다.** 스크립트가 안 뜨니 우리 코드가 한 줄도 안 돌고,
따라서 앱이 남기는 로그도 없습니다. 콘솔을 봐야 알 수 있고, 안 보면 "리다이렉트가
잘못됐나" 를 계속 뒤지게 됩니다.

## 2026-08-13 — 구글 학번 확인을 리다이렉트 방식으로

- 변경 파일: `frontend/src/lib/googleAuth.ts`(신규), `GoogleLoginButton.tsx`,
  `GoogleLinkModal.tsx`, `App.tsx`, `backend/auth_router.py`, 가이드 4종
- 요약: 구글이 그린 버튼(GSI)을 걷어내고 페이지를 통째로 넘기는 리다이렉트로 바꿨습니다.
  **모바일에서 버튼이 안 눌린다**는 제보가 출발점입니다.

**원인은 팝업이었습니다.** GSI 버튼은 우리 박스 위에 투명하게 겹쳐 둔 **구글 iframe**
인데, 눌리면 그 iframe 안에서 `window.open` 이 돕니다. iOS Safari 와 인앱 브라우저는
서드파티 iframe 이 연 팝업을 막으므로 **아무 일도 안 일어난 것처럼** 보입니다. 데스크톱
에서는 멀쩡해서 늦게 잡혔습니다.

제가 앞서 "모바일 정상" 이라고 했던 검증은 부족했습니다 — `elementFromPoint` 로 **눌리는
것**까지만 봤고 팝업이 뜨는지는 안 봤습니다. 실제로 눌러 보니 `display=popup` 짜리 창이
열리는 게 그때 보였습니다.

⚠️ **돌아온 주소는 화면이 그려지기 전에 낚아채야 합니다.** 처음엔 모달의 `useEffect`
에서 읽었는데 토큰이 사라졌습니다 — `/auth/google` 로 돌아오면 `currentUser` 를 아직 못
받은 찰나에 **연결 창 대신 라우터가 먼저 그려지고**, 맞는 라우트가 없어
`<Navigate to="/">` 가 주소를 갈아치우면서 조각을 통째로 날립니다. 그래서 `googleAuth.ts`
는 **모듈을 읽는 순간** 한 번 꺼내 메모리에 들고 있습니다 (겸사겸사 주소도 `/` 로 정리).

라우트를 새로 두지 않은 이유는 미연결 계정에는 **경로와 상관없이** 연결 창이 뜨기
때문입니다 — 창이 곧 콜백 화면입니다. 대신 연결 성공 뒤 `App.tsx` 가 `navigate("/")` 로
넘깁니다. `replaceState` 로 주소만 바꾸면 라우터가 몰라서 빈 화면이 됩니다.

`state` 를 꼭 두세요. 없으면 남이 만든 주소(`/auth/google#id_token=<남의 토큰>`)를 열게
해서 **내 계정에 남의 학번을 붙일** 수 있습니다. `nonce` 는 서버가 토큰 안의 값과
대조하는데, 옛 화면을 띄워 둔 브라우저를 위해 없으면 통과시킵니다.

⚠️ **구글 콘솔에 리디렉션 URI 를 먼저 등록해야 합니다** (`/auth/google`, 배포·로컬 둘 다).
안 하고 배포하면 `redirect_uri_mismatch` 로 **데스크톱까지** 막힙니다. 배포 가이드에
"리디렉션은 쓰지 않는다" 고 적혀 있던 줄도 같이 고쳤습니다.

검증은 로컬에서 세 갈래를 다 돌렸습니다 — 정상 흐름(서버가 가짜 토큰 거절까지 확인),
`state` 불일치("만료된 요청입니다"), 사용자 취소("취소했습니다"). 버튼이 만드는 주소는
구글이 `redirect_uri_mismatch` 에 우리 URI 를 그대로 찍어 줘서 확인했습니다.

## 2026-08-11 — 배포본에서 학교 계정 연결 버튼이 사라져 있던 것

- 변경 파일: `frontend/src/components/GoogleLoginButton.tsx`,
  `frontend/src/components/GoogleLinkModal.tsx`, `deploy-guide.md`
- 요약: 배포 빌드에 `VITE_GOOGLE_CLIENT_ID` 가 없어 구글 버튼이 **코드째** 빠져 있었고,
  없을 때 안내를 띄우게 고쳤습니다. 환경변수 자체는 **Netlify** 에서 설정해야 합니다.

⚠️ **프론트는 Vercel 이 아니라 Netlify 에 있습니다.** `deploy-guide.md` 가 Vercel 이라고
적고 있었는데, `classes.bsiku.dev` 응답에 `x-nf-request-id` 와 `cache-status: "Netlify
Edge"` 가 붙고 Vercel 계정(zevoers·classmuse)에는 이 프로젝트가 **없습니다**. 빌드 설정도
`frontend/netlify.toml` 이 들고 있습니다. 환경변수를 없는 곳에 넣고 반영을 기다리면 영영
안 되므로 가이드를 고쳤습니다 — 이 사고의 진짜 뿌리일 가능성이 큽니다.

⚠️ **`VITE_*` 는 빌드 시점에 번들에 박히는 값입니다.** 없으면 버튼이 안 그려지는 정도가
아니라, rollup 이 `CLIENT_ID` 가 늘 빈 문자열임을 증명하고 **죽은 가지를 통째로 잘라
냅니다.** 배포본(`index-CLbWmEnl.js`)을 받아 확인해 보니 `학교 계정 연결`·`link-google`
문자열은 있는데 `gsi/client`·`googleusercontent` 는 **0개**였습니다 — 모달은 뜨는데 그
안에 누를 것이 없었다는 뜻입니다.

`GoogleLinkModal` 은 **닫을 수 없는 창**이라 이게 곧 막다른 길입니다. 새로 들어온
사람에게는 "연결 전에는 이용할 수 없습니다" 와 로그아웃 버튼만 남습니다. 설정 하나가
빠졌을 뿐인데 화면에는 아무 흔적이 없는 게 문제라, `GOOGLE_LOGIN_READY` 를 내보내
모달이 안내를 띄우게 했습니다 (콘솔에도 남깁니다). 환경변수 없이 빌드해 문구가
살아남는 것까지 확인했습니다.

**로컬은 멀쩡했습니다** — `frontend/.env` 에 값이 있고 그 파일은 `.gitignore` 대상이라
빌드 서버가 볼 수 없습니다. 그래서 로컬에서 아무리 눌러 봐도 재현되지 않습니다. 같은
종류의 값(`VITE_*`)을 새로 들일 때는 **배포 번들에 그 문자열이 있는지**로 확인하는 게
제일 빠릅니다.

**조치 완료** — Netlify(`ksa-class-finder`)에 `VITE_GOOGLE_CLIENT_ID` 를 등록하고 깃 연동
빌드를 다시 걸었습니다. 등록 전 `env:list` 에는 `VITE_API_BASE_URL` **하나뿐**이었습니다.
새 번들(`index-DJzhlOZ6.js`)에 `gsi/client`·`googleusercontent` 가 들어간 것까지
확인했습니다 (255,314 → 257,798 bytes).

⚠️ **환경변수만 저장하면 안 바뀝니다.** `VITE_*` 는 빌드 때 박히므로 반드시 재빌드까지
가야 합니다 — CLI 로는 `netlify api createSiteBuild` 가 대시보드의 "Trigger deploy" 와
같습니다 (`netlify deploy` 는 **로컬 작업 트리**를 올리므로 이 용도에는 쓰지 마세요).

백엔드에도 `GOOGLE_CLIENT_ID` 가 있어야 `/auth/link-google` 이 503 을 내지 않습니다 —
프론트만 고치면 버튼은 보이는데 눌러도 실패합니다. 이건 서버(N100)에서 확인해야 합니다.

## 2026-08-11 — 홈에 내 주간 시간표 위젯

- 변경 파일: `backend/home_router.py`, `frontend/src/components/home/WeekTimetable.tsx`,
  `frontend/src/pages/HomePage.tsx`, `frontend/src/lib/friendsApi.ts`,
  `frontend/src/lib/homeDemo.ts`, 가이드 4종
- 요약: `GET /home` 이 요일별 한 주 시간표(`week`)를 같이 돌려주고, 홈 아래에 주간
  격자 카드가 붙습니다.

**오늘 시간표는 이미 있었으므로 비어 있던 건 "한 주"였습니다.** 그래서 위 카드는
오늘을, 아래 격자는 이 학기를 말하게 나눴습니다.

⚠️ **`week` 는 방학·주말·휴업에도 비우지 않습니다.** `today` 를 학사일정으로 거르는
규칙(학기 데이터가 요일 단위라 방학 중 월요일에도 월요일 시간표가 나옵니다)을 그대로
적용하면 방학에 자기 시간표를 볼 길이 없어집니다 — 오늘이 아니라 학기를 말하는
값이라서요. 대신 "오늘"·"지금" 강조(핑크)만 `has_class` 로 끕니다.

질의는 오늘치 대신 **한 주를 한 번에** 받게 바꿨습니다. 요일 조건만 빼면 되고 한 사람의
한 학기가 30줄 남짓이라, 같은 질의를 두 벌 두는 게 더 비쌉니다. `today` 는 거기서
오늘 요일만 꺼낸 것입니다.

**함정 하나.** 수업 칸을 옅은 회색으로 깔고 칸 경계를 빈 칸과 같은 `black/10` 으로
뒀더니, 세로로 붙은 서로 다른 수업(목요일 5·6·7교시)이 **회색 한 덩어리**로 읽혀서
연강을 이어 붙인 게 아무 뜻이 없었습니다. 수업 칸의 **윗선만 `/25`** 로 올려서
갈랐습니다 — 이어 붙인 칸 안에는 선이 아예 없으므로 둘이 구별됩니다.

가로선을 칸 **위쪽**에 긋는 것도 같은 자리에서 정했습니다. 아래쪽에 그으면 마지막
줄의 선이 카드 테두리와 겹쳐 두 겹이 되고, 격자 맨 아래까지 닿는 수업 칸마다 예외를
둬야 합니다.

연강은 과목·분반뿐 아니라 **교실까지 같아야** 잇습니다. 교실이 바뀌면 사이에 이동이
있는데 한 덩어리로 묶으면 그걸 놓칩니다.

진행 중인 수업이 연강이면 **덩어리 전체**가 핑크입니다. 위 목록은 교시 단위라 6교시만
핑크인데, 격자의 단위는 교시가 아니라 수업이라 일부러 다르게 뒀습니다.

## 2026-08-10 — 재수집이 분반 id 를 새로 매기던 문제 (Class upsert)

- 변경 파일: `backend/parser_run.py`, `backend/parser_run.guide.md`, `backend/CLAUDE.md`
- 요약: `replace_term_data()` 가 `Class` 행을 지웠다 다시 넣어 재수집마다 id 가
  바뀌던 것을 `(subject_id, section, teacher)` 키 재사용으로 바꿨습니다.

**추가수강신청 반영본을 받으려다 발견했습니다.** 지우고 다시 넣는 방식이라 재수집할
때마다 `Class.id` 가 전부 새로 매겨지는데, **Trade 계획(`UserState.trade`)이 분반을 id 로
가리킵니다** (`moveTargets`, `addSelections[].sectionId`). id 가 사라지면 그나마 조용히
무시되지만, 재사용되면서 같은 과목의 다른 분반에 붙으면 계획이 엉뚱하게 바뀝니다.

처음엔 수집 전후로 id 를 매핑해 `UserState` 를 다시 쓰는 스크립트를 생각했는데, 그건
매 학기 반복될 땜질이었습니다. **분반은 그대로고 명단만 바뀌는 수집이 훨씬 흔하다**는
게 핵심입니다 — DB 를 안 건드리고 응답만 받아 대조해 보니 2026-2 는 237개 중 **폐강 0 ·
신설 14 · 교사 변경 0 · 시간 변경 0** 이었고, 바뀐 건 교실(전부 `배정중` → 실제 교실)과
수강 명단(추가 246 · 제거 26)뿐이었습니다. 애초에 id 를 안 바꾸면 되는 일이었습니다.

⚠️ **키에 교사가 들어 있어서 교사 이름이 바뀌면 폐강 + 신설로 잡히고 id 가 바뀝니다.**
교사를 빼면 같은 과목 같은 분반이 합쳐져 더 위험해서 그대로 뒀습니다. 대신 출력에
`분반: 유지 N · 신설 N · 폐강 N` 을 찍게 했으니, **폐강 수가 튀면 표기가 흔들린 것으로
보고 멈추면 됩니다.**

`ClassTime` 과 `Enrollment` 는 그대로 전량 교체입니다 — 아무도 그 id 를 참조하지 않고,
부분 갱신보다 통째로 다시 받는 편이 어긋날 여지가 적습니다.

## 2026-08-10 — 학사 사이트(my.ksa.hs.kr) 연동 조사 — 보류

- 변경 파일: 없음 (조사만)
- 요약: 화면·표 구조 지도와 로그인 흐름을 확인하고, 지금은 만들지 않기로 했습니다.
  전문은 [조사 리포트](~/Reports/2026-08-10_16-37-ksa-academic-site-integration-survey.md).

**로그인 자동화가 막혀 있습니다.** `/loginCert` → SMS 인증 → `/loginCertChk` →
`/loginProcess` 인데 인증에 일일 한도가 있어(`W010`), 서버가 학생 계정으로 주기적으로
로그인해 긁는 구조는 못 씁니다. 남는 건 **이미 로그인된 브라우저 세션**을 쓰는 것뿐이고,
학사 사이트에 CSP 가 없어서 북마클릿이든 확장이든 기술적으로는 다 됩니다.

**뷰어 쪽을 먼저 잘라냈습니다.** 원래 끌렸던 건 생기부·상벌점 뷰어였는데, 그게 정확히
남의 기록을 우리가 보관하게 되는 부분입니다. 상벌점은 계속 바뀌는 값이라 저장하면 낡고
저장 안 하면 볼 때마다 학사 사이트를 거쳐야 해서, 직접 보는 것보다 나은 점을 대기 어려웠습니다.

남은 건 성적과 봉사 시수인데 **둘 다 이미 우리 DB 에 있는 값**입니다(자몽 평어는
`CourseGrade`, 시수는 `/state/zamong`). 새로 보관할 데이터가 없어 책임 문제는 사라졌지만,
동시에 이 기능이 **자몽을 채우는 두 번째 입구**일 뿐이라는 것도 분명해졌습니다 — 워크북
업로드가 이미 그 자리에 있습니다. 결정적인 건 **파서를 하나 더 떠안는다**는 점이었습니다.
학교가 페이지를 손보면 조용히 깨지고, 우리는 "안 불러와져요"를 들을 때 압니다. KEIS 파서는
앱 전체가 얹혀 있어 값을 하지만, 시드 경로 하나는 그렇지 않습니다.

폐기가 아니라 **보류**입니다. 워크북 업로드는 엑셀을 이미 채운 사람에게만 통하고 자몽이
노리는 사람은 그 반대라, 엑셀 없는 사람이 실제로 막히는지 보고 정하는 게 훨씬 쌉니다.

조사에서 건진 것 하나 — **봉사 시수는 대응이 1:1 입니다.** 자기계발·협업·세계시민 표에
누계 열이 있어서 **마지막 행의 숫자 셋만** 읽으면 자몽의 세 칸이 채워집니다. 활동 내용을
읽지 않고도 되므로, 나중에 만들더라도 "생활기록 문장은 안 건드린다"를 코드로 지킬 수 있습니다.

## 2026-08-10 — 자몽 정렬·무게 정리 + 달력 글씨 키우기

- 변경 파일: `frontend/src/components/zamong/{Roadmap,CourseCard}.tsx`,
  `frontend/src/components/CalendarGrid.tsx`, `frontend/src/pages/{ZamongPage,CalendarPage}.tsx`,
  `frontend/src/lib/{curriculum,zamong}.ts`
- 요약: 표 셀 높이 통일, 학기별 정렬 규칙, 카드 무게 완화, 달력 글씨 확대.

**표 셀 높이를 line-height 에 맡기고 있었습니다.** 패딩만 주니 내용마다 값이 달라져서
11px 헤더는 25px, 13px 글자는 27.5px, 입력칸이 든 칸은 28px 이 나왔습니다. 눈으로 두 번
헛짚고 나서야 셀마다 `getComputedStyle` 을 재서 찾았습니다 — 모든 셀에 `h-7 align-middle`
을 주니 한 번에 정리됐습니다(양쪽 표 전부 28px, 세로 중앙).

**학기 안 과목 정렬**을 가나다에서 **학과 → 선수 깊이 → 가나다** 로 바꿨습니다. 학과
순서는 서버가 주는 `display_order` 라 학과 버튼 줄과 같은 차례입니다 — 두 곳이 다르면
눈이 매번 다시 찾습니다. 깊이는 `courseDepths()` 가 배치와 **같은 규칙**(동시수강 짝
제외)으로 계산해서, 목록 순서가 판에서 보던 순서와 어긋나지 않습니다.

**과목을 눌렀을 때 나머지 카드를 흐리게 만들던 것도 걷어냈습니다.** 이어지지 않은 카드를
`opacity-15` 로 지웠는데, 한 장 누를 때마다 판이 통째로 하얘져서 방금 보고 있던 것까지
사라졌습니다. **호버와 클릭이 한 값에 섞여 있었습니다.** 마우스를 올리면 `focused` 가 켜지는데 나갈 때
끄지 않아서, 손을 치운 뒤에도 그 과목이 짚인 채로 굳었습니다. 호버는 나가면 바로 끄고
(`onMouseLeave`), 붙잡는 건 클릭(`selected`)이 맡게 나눴습니다. 상세는 `focused ?? selected`
라 훑는 동안은 그 과목을 보여 주고 손을 치우면 붙잡아 둔 것으로 돌아옵니다 — 호버만
쓰면 상세를 읽으러 눈을 내리는 순간 사라집니다.

상세의 관계 표기를 `선수: …` / `이 과목을 들으면 열립니다: …` 에서 **`선수 과목` /
`후수 과목`** 짝으로 바꿨습니다. 한쪽만 문장이면 서로를 보고 읽는 두 값이 다른 종류처럼
보입니다. 라벨 폭을 맞춰 한 열로 세웠습니다.

**격자 배치에는 카드 클릭이 안 달려 있었습니다** — 융합 판(선이 없어 격자로 그립니다)과
좁은 화면에서 카드를 눌러도 아무 일이 없었습니다. 사다리와 같은 핸들러를 답니다. 학과를
바꿀 때는 붙잡아 둔 과목을 놓습니다(그 판에 없는 과목입니다).

`stopPropagation` 을 줄 전체에 걸었다가 카드 한가운데(=칸)를 눌러도 아무 일이 없길래
칸에만 걸도록 좁혔습니다.

선 굵기는 **항상 2px** 로 뒀습니다. 짚은 선만 3px 로 굵히고 있었는데 카드 테두리가 2px
이라, 같은 핑크인데 선과 테두리 두께가 어긋났습니다. Browse 의 `CourseGraph` 도 같은
문제가 있어 함께 고쳤습니다.

**지우는 대신 더하되, 누른 카드 하나만** 테두리를 핑크로 세웁니다. 이어진 과목까지
칠해 봤더니 판의 절반이 핑크가 됐습니다 — 어디로 이어지는지는 선이 이미 말하고,
테두리는 "지금 짚은 게 이것" 만 말하면 됩니다.

**채운 카드의 그림자를 뺐습니다.** 검은 테두리에 하드 그림자까지 붙으니 두 변이 두 겹으로
두꺼워져서, 판에 스무 장 깔리면 채운 카드만 시커멓게 튀었습니다. 카드 제목의 포커스 링도
껐습니다 — 셀렉트 쪽 포커스는 키보드에 필요하므로 그대로 뒀습니다.

**학과 버튼이 Browse 모드 탭과 달랐습니다.** 여기만 크기·간격을 손으로 키워 놨던 걸
`RetroButton size="sm"` + `gap-3` 으로 되돌렸습니다(패딩 6/12, 12px, 높이 32px 로 일치 확인).

달력은 격자가 칸 높이에 묶여 있어 한 단계만(9→10, 10→11, 11→13), 옆 목록·상세는 두 단계
올렸습니다. 밑줄 범례 문구는 `심화필수` → **`트랙필수`** 로 바꿨습니다 — 실제로 걸리는
곳이 학과 트랙 조건이라 그쪽이 무엇인지 바로 말해 줍니다. 코드·주석은 워크북 원본 용어를
유지합니다.

## 2026-08-10 — 자몽 UI 키우기 + 로드맵에서 과목 넣기

- 변경 파일: `frontend/src/components/zamong/{CourseCard,Roadmap}.tsx`,
  `frontend/src/lib/{zamong,curriculum}.ts`, `frontend/src/pages/ZamongPage.tsx`
- 요약: 글자·입력칸을 한 단계씩 키우고, 로드맵 줄에서 검색으로 과목을 넣게 했습니다.

**전부 앱 기준보다 작았습니다.** 카드가 184×100 에 글자가 8.5~11.5px 이었는데, 디자인
가이드에서 제일 작은 글자가 배지 10px 입니다. 카드는 208×136, 과목명 14px, 입력칸은
높이 19px → 28px. 로드맵 표는 9.5/11 → 11/13px.

고치면서 걸린 것들:

- **`appearance-none` 이 셀렉트 화살표를 지우고 있었습니다.** `SelectCell` 로 감싸
  `ChevronDown` 을 직접 답니다 — 없으면 눌러서 여는 칸인지 안 보입니다
- **EC 버튼이 있는 카드만 학기 칸이 짧았습니다.** 자리를 항상 잡아 두게 고쳤습니다
- **로드맵 줄의 왼쪽 블록을 `items-baseline` + `pt-1` 로 맞춰 놨었습니다.** 오른쪽 줄
  높이는 입력칸이 정하고 줄이 접히면 더 커져서 매번 어긋납니다 — 같은 높이 상자
  (`h-7`) 안에서 가운데 정렬로 바꿨습니다
- 학기 이름과 학점의 폭을 각각 고정했습니다. 붙여 두면 `계절학기` 줄에서만 학점이
  밀립니다

**빈 자리가 그대로 입력칸입니다.** 처음엔 "+ 과목 넣기" 버튼을 눌러야 칸이 나오게
했는데, 단계만 하나 더 있을 뿐 하는 일이 같아 테두리 없는 입력칸으로 늘 열어 뒀습니다.
포커스는 테두리가 아니라 바탕색으로 표시합니다.

**학점은 줄 맨 오른쪽으로 옮겼습니다.** 이름 옆에 두면 이름 폭을 고정해야 `계절학기`
줄에서 안 밀리는데, 그러면 짧은 이름 뒤로 빈 자리가 길게 남습니다.

**입력칸에서 테두리·호버·포커스 표시를 전부 뺐습니다.** 비교과 시수 세 칸만 상자를
두르고 있어서 옆칸(총 시수·계열 학점)과 어긋나 보였습니다 — 그 줄은 전부 숫자입니다.
숫자 스피너도 껐습니다. 위쪽 표 두 개의 여백도 아래 학기 줄과 비슷한 밀도로 맞췄습니다.

줄 높이가 52 → 33px 로 줄었습니다. 알약 높이는 **패딩이 아니라 상자(`h-7`)** 가 잡습니다 —
패딩으로 키우면 글자만큼만 커져서 핑크 면이 글씨에 달라붙습니다.

## 2026-08-10 — 불러오기 실패가 저장을 덮어쓰던 문제

- 변경 파일: `frontend/src/pages/{ZamongPage,TradePage}.tsx`, `frontend/src/lib/userState.ts`
- 요약: 요청이 실패해도 "복원됨" 으로 치고 빈 상태를 저장해, 서버 기록을 지우고 있었습니다.

⚠️ **자몽 저장은 `PUT /curriculum/grades` 전체 교체입니다.** 화면이 빈 상태로 한 번만
저장하면 그 계정 기록이 통째로 사라집니다. 그걸 막으라고 `restored` 플래그가 있었는데,
`.catch(() => null)` 로 실패를 삼킨 뒤 **성공·실패 가리지 않고 플래그를 켜고 있었습니다.**
네트워크가 한 번 튀면 사용자가 아무것도 안 해도 600ms 뒤 빈 저장이 나갑니다.

Playwright 로 `GET /curriculum/grades` 를 실패시켜 재현·확인했습니다 — 고친 뒤에는 서버의
52과목이 그대로 남고 경고 배너가 뜹니다.

같은 모양이 Trade 에도 있었습니다. `loadState` 가 **"저장된 게 없다" 와 "못 받았다" 를
똑같이 `null`** 로 돌려줘서, 부르는 쪽이 빈 상태를 복원하고 저장해 버립니다. 못 받았고
로컬 사본도 없으면 예외를 던지게 바꿨고, Trade 는 그 경우 `restored` 를 안 켭니다 —
이번 화면에서 저장이 안 되는 편이 남아 있던 계획을 지우는 것보다 낫습니다.

**저장이 전체 교체인 화면에서는 "복원 성공" 을 반드시 응답으로 확인하세요.** 예외를
삼키고 기본값으로 넘어가면 그게 곧 삭제입니다.

## 2026-08-10 — 동시수강 과목을 나란히 배치

- 변경 파일: `frontend/src/lib/curriculum.ts`
- 요약: 이론과 실험(일반물리학1 ↔ 일반물리학실험1)을 같은 열에 붙여 놓습니다.

워크북이 이 둘을 **선으로** 이어 놨는데 선수관계가 아니라 **동시수강**입니다 — 워크북이
상자를 좌→우로만 늘어놓을 수 있어서 그렇게 그린 것뿐입니다. 그대로 두니 실험이 한 칸
오른쪽으로 밀려서 다음 학기 과목처럼 보였습니다.

배치에서만 그 선을 빼고 짝을 같은 열에 위아래로 붙입니다(간격 4px, 이웃은 14px).
`CoursePrereq` 데이터는 그대로 둡니다 — 이수 판정에는 그게 맞습니다.

짝은 이름으로 찾습니다(`실험` 제거 → 없으면 `1` 붙여 재시도). 일곱 쌍이 잡히고,
`물리학및실험1`·`실험물리학특론`처럼 실험이 이름의 일부인 단일 과목은 안 걸립니다 —
규칙을 넓히기 전에 이 다섯 개를 꼭 확인하세요.

열 정렬이 칸 **수**가 아니라 **높이** 기준으로 바뀌었습니다. 짝을 붙이면서 칸 수와
높이가 더는 비례하지 않습니다.

## 2026-08-10 — 자몽 기능 추가 (드래그·엑셀 업로드·캐싱) + 융합 판 정리

- 변경 파일: `backend/{workbook,zamong_import,curriculum_router}.py`,
  `frontend/src/lib/{cache,curriculumApi,curriculum,zamong}.ts`,
  `frontend/src/components/{CourseGraph,zamong/*}.tsx`, `frontend/src/pages/ZamongPage.tsx`
- 요약: 로드맵 드래그 이동, Zamong 엑셀 업로드, 응답 캐싱, 타 학과 선수 표기 정리,
  카드 셀 색 정상화.

**엑셀 업로드는 서버가 읽습니다.** 브라우저에서 xlsx 를 열려면 파서를 하나 더 들여야
하는데(SheetJS 는 1MB 가까이 됩니다), 서버에는 교육과정 seed 를 만들 때 쓰던 리더가
이미 있었습니다. 그걸 `workbook.py` 로 빼서 둘이 같이 씁니다 — 표준 라이브러리만 쓰는
코드라 의존성도 안 늘어납니다. 실제 워크북으로 검증했더니 학기별 학점이 원본 상단표와
정확히 일치했습니다 (18/21/16/25/27/22/4, 합계 133, AP 24).

⚠️ **업로드는 합치지 않고 갈아끼웁니다.** 워크북이 그 사람의 자몽 전체라 일부만 덮으면
앱에서 지운 과목이 되살아납니다. 그리고 **학기가 없는 카드는 건너뜁니다** — 워크북에는
안 들은 과목의 카드도 다 있고 제목만 EC 로 골라 둔 카드도 흔한데, 그걸 넣으면 "학기
미지정" 이 무더기로 생깁니다.

**타 학과 선수를 판에서 뺐습니다.** 융합 판에 수학·물리 카드가 섞여 나와서 저게 융합
과목인지 구별이 안 됐습니다. 이제 카드에 `선수 미적분학1` 처럼 글로 답니다
(`outsidePrereqs`). 그러자 융합 판에 선이 하나도 안 남아 카드 열여섯 장이 세로로 죽
늘어섰는데, **선이 없으면 사다리가 아니라 격자**로 그리게 고쳤습니다.

**카드의 학기·평어 칸을 검게 칠하던 걸 되돌렸습니다.** 파스텔 판 위에 잉크를 쏟은 것처럼
보였습니다 — 원본은 그냥 값이 적힌 셀이고, 채웠는지는 테두리 굵기가 말합니다.

**캐싱은 `lib/cache.ts` 한 곳입니다.** localStorage TTL 과 **같은 키 요청 묶기**를 같이
합니다. 묶기가 없으면 첫 방문에 캐시가 비어 있어서 Zamong·Browse·Trade 가 동시에 열릴 때
`/curriculum` 이 그대로 세 번 나갑니다. 급식은 지난 날만 오래 답니다 — 앞날을 길게 잡으면
아직 안 올라온 날이 빈 채로 굳습니다.

## 2026-08-10 — 자몽 전면 재개편 (학교 Zamong 워크북 이식)

- 변경 파일: `frontend/src/pages/ZamongPage.tsx`, `frontend/src/lib/zamong.ts`,
  `frontend/src/components/zamong/*`, `frontend/src/lib/curriculum.ts`,
  `backend/{models,migrations,curriculum_router,state_router,import_curriculum,build_curriculum_seed}.py`,
  `backend/curriculum_seed.json`
- 요약: 학교에서 돌려 쓰는 Zamong 워크북(엑셀)의 조작 방식을 그대로 옮기고, 자몽을
  실제 수강 이력에서 떼어냈습니다.

**⚠️ 자몽과 수강 이력을 분리한 것이 이 작업의 핵심입니다.** 예전에는 수집된 학기를 그대로
이수로 박아 두고 손도 못 대게 했는데, 재수강한 과목이 두 학기에 나타나면 어느 쪽이
인정인지 화면이 정할 수 없었고 사람이 고칠 길도 없었습니다. 학사 사이트를 붙이면 같은
문제가 더 커집니다. 지금 이력이 쓰이는 곳은 **자몽이 빈 사람에게 한 번 권하는 밑칠**
뿐이고(`/state/zamong` 의 `seeded` 가 그 한 번을 기억), 화면에 이력을 늘어놓는 자리는
없앴습니다. 되돌리지 마세요 — 탭으로 나눠 나란히 두는 것도 같은 실수입니다.

**조작 단위가 바뀌었습니다.** 체크박스를 없애고 카드마다 `학기`·`평어` 를 직접 고릅니다.
학기를 고르는 행위가 곧 이수이고(워크북 안내문의 "학기를 써야 학점 인정됨"), 비우면
기록이 지워집니다. 재수강은 학기를 다시 고르면 끝이라 `CourseGrade` 의
`UNIQUE(user_id, course_id)` 를 그대로 뒀습니다. 학기 칸은 **1~8 + 계절**입니다(휴학).

`CourseGrade` 에 `term`·`is_ec` 가 붙었습니다. 학기 없는 옛 행은 화면이 "학기 미지정"
으로 골라내 채우게 합니다.

**`import_curriculum` 이 이수 기록을 지우고 있었습니다.** 과목을 통째로 갈아끼우면서
`CourseGrade` 를 `delete()` 했는데, 교육과정을 다시 적재할 때마다 모든 계정의 자몽이
사라집니다. 과목 **이름**으로 다시 이어 붙이도록 고쳤습니다 — 이번 배포에 교육과정
재적재가 끼어 있어서 안 고쳤으면 그대로 날아갔습니다.

**과목 분류(핵심/심화/AP/특강/융합)와 심화필수를 워크북에서 새로 추출했습니다.** 상자
배경색이 곧 분류이고 제목의 굵은 밑줄이 심화필수입니다. `build_curriculum_seed.py` 에
넣어 재현 가능하게 뒀는데, **덩어리를 전부 카드로 보면 안 됩니다** — 물리학 시트에는
트랙 조건을 설명하는 카테고리 표가 있고 거기 적힌 `일물1` 이 실제 카드보다 왼쪽 위라,
그냥 두면 일반물리학1 이 AP 가 아니라 심화로 잡힙니다. 덩어리에 `학점` 줄이 있는지로
가릅니다.

**색은 워크북 hex 를 그대로 쓰고 불투명이어야 합니다.** 앱 팔레트를 옅게 깔아 봤더니
반투명이라 크림색 배경과 섞여 심화(시안)가 초록으로 보였습니다. 판 바탕도 흰색입니다.

**트랙은 판정하지 않고 문장만 보여 줍니다.** 조건이 학과마다 제각각이라(물리학은
카테고리 표, 지구과학은 "6개 중 택2") 규칙으로 옮기면 틀렸을 때 알아채기 어렵고, 틀린
"졸업 가능" 이 맞는 문장보다 해롭습니다. 비교과 시수도 데이터가 없어 손으로 받습니다.

**배치를 한 번 갈아엎었습니다.** 처음에는 로드맵을 학과와 나란한 탭으로 두고 결론·학기·
요건을 카드 셋으로 나눴는데, 요건 막대가 화면 절반을 먹고 "내가 어디쯤인지" 보려면 매번
탭을 옮겨야 했습니다. 지금은 **로드맵이 맨 위 고정 카드 한 장**이고 그 아래에서 학과를
고릅니다 — 워크북도 `Zamong` 시트가 첫 장이고 학과 시트가 그걸 채우러 가는 곳입니다.
학과 선택은 앱의 `RetroButton` 을 씁니다(직접 만든 탭 버튼이 다른 페이지와 톤이 달랐습니다).

좁은 화면에서는 사다리를 접고 단 순서대로 늘어놓습니다 — 390px 에서 1100×1240 짜리 판을
두 방향으로 굴리면 화면에 늘 빈 데가 잡힙니다. 가장자리 그림자는 `ResizeObserver` 로
켭니다(창 크기가 아니라 상자를 봐야 `hidden` → 보임 전환까지 잡힙니다).

# Logs

## 2026-08-04 — 문서의 색 토큰을 코드와 맞춤

- 변경 파일: `frontend/design-guide.md`, `frontend/src/index.css.guide.md`, `frontend/src/components/Navigation.guide.md`, `frontend/src/components/molecules/BarChartRow.guide.md`
- 요약: 색 값을 세 군데에 베껴 두었다가 셋 다 어긋났습니다. 값은 `index.css` 하나만 갖고, 문서는 가리키기만 합니다.

`design-guide.md` 의 색 표는 **여섯 줄이 전부 틀려 있었습니다** — primary 를 `#ff3e3e`(빨강)로 적어 뒀는데 실제는 `#ff4eba`(핑크), bg 도 fg 도 secondary 도 accent-light 도 green 도 달랐고, `accent1`~`accent5` 다섯 개는 표에 아예 없었습니다. 표를 믿고 색을 고르면 엉뚱한 색이 나오는 상태였습니다.

**원인은 중복입니다.** 같은 값이 `index.css`·`design-guide.md`·`index.css.guide.md` 세 곳에 각각 적혀 있었고, 코드를 고칠 때 문서는 따라오지 않았습니다. `index.css.guide.md` 에서는 값을 통째로 지우고 "실제 값은 `index.css` 를 열어 보라" 로 바꿨습니다 — 가이드가 값을 들고 있으면 언젠가 또 갈라집니다. 컴포넌트 가이드 둘(`Navigation`·`BarChartRow`)에 박혀 있던 hex 도 토큰 이름만 남겼습니다.

같이 잡은 것 —

- ⚠️ **`retro-primary` 와 `retro-accent3` 은 같은 핑크(`#ff4eba`)** 입니다. 한쪽만 바꾸면 같은 뜻이던 두 자리가 조용히 갈라집니다 — 표에 경고로 적었습니다
- **테두리는 `retro-fg`(`#222222`)가 아니라 순수 검정**입니다. 표가 `retro-fg` 를 "기본 텍스트 + 테두리" 라고 적고 있었는데, 실제 테두리는 전부 `border-black` 입니다
- 초록 마름모를 "빈 교실 공통 슬롯" 이라고 적어 뒀는데 실제로는 **달력의 개인 일정 표식**입니다
- HeroUI 오버라이드가 `[data-slot="base"]` 하나라고 적혀 있었지만 실제로는 네 개의 slot 입니다
- `Navigation.guide.md` 의 높이가 `h-16` 으로 적혀 있었는데 실제는 `h-20` 입니다
- 죽은 링크(`../../design-guide.md`, 루트에 그런 파일이 없습니다)를 `index.css` 와 `design-guide.md` 로 고쳤습니다

**`bench-frontend/` 쪽 사본은 그대로 뒀습니다** — 동결 상태라 손대지 않는 게 규칙입니다. 나중에 그쪽을 깨울 때 같은 작업이 한 번 더 필요합니다.


## 2026-08-04 — `.env` 를 git 에서 걷어내고 `.env.example` 로 대체

- 변경 파일: `.gitignore`, `frontend/.env.example`(새로), `bench-frontend/.env.example`(새로)
- 요약: 두 프론트의 `.env` 가 공개 저장소에 커밋돼 있던 걸 추적 해제하고, 같은 내용을 `.env.example` 로 옮겼습니다.

들어 있던 값은 `VITE_API_BASE_URL` 과 `VITE_GOOGLE_CLIENT_ID` 둘뿐이라 **유출된 시크릿은 없습니다** — 어차피 브라우저 번들에 그대로 실려 나가는 공개 값입니다. 문제는 `.gitignore` 에 `.env` 가 아예 없었다는 쪽입니다. 지금은 안전한 값만 있지만, 나중에 서버 키 한 줄을 같은 파일에 적는 순간 그대로 공개됩니다.

`.env.example` 에는 값을 그대로 남겼습니다. 공개 값이라 가려 봐야 얻는 게 없고, clone 한 쪽에서 구글 로그인 버튼이 안 뜨는 이유를 찾아 헤매게 됩니다.


## 2026-08-04 — 모바일 헤더 정리 · More 판을 구석으로

- 변경 파일: `frontend/src/components/Navigation.tsx`, `frontend/src/components/BottomNav.tsx`, `frontend/src/App.tsx`
- 요약: 390px 에서 헤더가 잘리던 걸 두 요소로 줄이고, 로그아웃을 하단 More 안 우상단으로 옮겼습니다.

**헤더에는 로고와 학기 선택 둘만 남겼습니다.** 계정 이름과 로그아웃까지 넣었더니 서로를 밀어내 잘렸습니다. 로그아웃은 자주 쓰지도 않는데 헤더에서 제일 좋은 자리를 차지하고 있었습니다.

**로그아웃은 More 판 우상단의 28px 정사각형이고 두 번 눌러야 실행됩니다.** 잘못 누르면 되돌릴 방법이 다시 로그인뿐이라, 한 번에 끝나면 안 됩니다. 확인 버튼은 **왼쪽에** 새로 나타나고 방금 누른 오른쪽 구석은 **취소**가 되므로, 같은 자리를 두 번 눌러도 로그아웃되지 않습니다. 5초 지나면 자동으로 풀립니다.

**More 판은 햄버거 바로 위 오른쪽 구석**(`w-52`)에 박힙니다. 폭을 다 차지하면 방금 누른 버튼과 멀어져 어디서 열렸는지가 안 보이고, 여섯 줄짜리 목록에 390px 을 다 쓸 이유도 없습니다.

⚠️ **`transition-all` 이 레이아웃을 깼습니다.** 확인 버튼이 나타날 때 폭까지 애니메이션되는 바람에 그 100ms 동안 버튼이 좁아 "로그아웃" 이 두 줄로 접혔고, 높이는 `h-7` 로 고정해 둔 탓에 둘째 줄이 상자 아래로 삐져나왔습니다. `transition-colors` + `whitespace-nowrap` 두 겹으로 막았습니다 — 하나는 원인(폭 변화), 하나는 증상(줄바꿈).


## 2026-08-04 — 메뉴 정리 (순서·사이드바 고정·모바일 5버튼)

- 변경 파일: `frontend/src/lib/navigation.ts`(새로), `frontend/src/components/Sidebar.tsx`, `frontend/src/components/BottomNav.tsx`, `frontend/src/App.tsx`
- 요약: 메뉴 순서를 한 곳에서 정하고, 사이드바는 상태 카드를 아래 고정, 모바일은 다섯 버튼으로 줄였습니다.

**순서를 두 벌 들고 있었습니다.** 사이드바와 하단 바가 각자 배열을 가지고 있어 한쪽만 고치면 순서가 서로 달라졌습니다 — `lib/navigation.ts` 하나로 모았습니다. 새 순서는 **홈 · 서치 · 룸 · 캘린더 · 브라우즈 · 아날리시스 · 자몽 · 트레이드 · 어바웃**입니다. 두 가지가 순서를 정합니다 — 앞 넷은 **매일 여는 것**(그대로 모바일 하단 바에 올라갑니다), 그다음은 **누구 것을 보는가**로 묶었습니다(브라우즈·아날리시스는 전교생과 통계, 자몽·트레이드는 내 이수현황과 내 수강).

**System Status 를 맨 아래에 붙였습니다.** 메뉴가 길어지면 상태 카드가 화면 밖으로 밀려났는데, 그건 "서버가 살아 있나" 를 보려고 스크롤해야 한다는 뜻이라 표시등의 의미가 없어집니다. 스크롤은 메뉴 영역만 합니다. Admin 아래 구분선은 지웠습니다 — 바로 밑 상태 카드 테두리와 겹쳐 선이 두 줄로 읽혔습니다.

**모바일 하단 바는 다섯 개입니다** — 자주 여는 넷 + 나머지를 여는 햄버거. 여덟 개를 390px 에 욱여넣으면 글자가 4px 이 되고 손가락이 옆 버튼을 누릅니다. 펼친 판은 하단 바 바로 위에 붙습니다(화면 한가운데에 띄우면 방금 누른 버튼과 멀어져 어디서 열렸는지가 안 보입니다). Admin 도 여기로 들어와서 모바일에서 처음으로 관리 화면에 갈 수 있게 됐습니다.


## 2026-08-04 — 홈 배치 V2 (낮고 가로로 긴 판본)

- 변경 파일: `frontend/src/components/home/TodayCardV1.tsx`(새로, V1 을 옮김), `frontend/src/components/home/TodayCardV2.tsx`(새로), `frontend/src/lib/homeView.ts`(새로), `frontend/src/pages/HomePage.tsx`, `frontend/src/components/TodayTimeline.tsx`, `backend/home_router.py`
- 요약: **낮고 가로로 긴 V2 로 확정**했습니다. V1 은 되돌아볼 자리로 남기되 개발에서만 보이고 프로덕션 번들에는 안 들어갑니다.

V1 은 하루를 처음부터 끝까지 늘어놓느라 수업이 일곱이면 카드가 600px 을 넘어갑니다. V2 는 **세로를 아낍니다** — 왼쪽 1/3 에 지금 상태(수업·교실·교시·시각)와 오늘 학사일정, 오른쪽 2/3 에 수업 목록을 두되 **지금을 가운데 두고 앞뒤만** 보여 주고 나머지는 스크롤로 넘깁니다.

- **`HomePage.tsx` 를 껍데기로 비웠습니다** — fetch·시계·스위치만 남기고 화면은 `components/home/TodayCardV{1,2}.tsx` 가 그립니다
- 파생값(지금 몇 교시·현재 수업·빈 시간)은 `lib/homeView.ts` **한 곳에서만** 셉니다. 판본마다 따로 계산하면 비교하다 어긋난 걸 배치 탓으로 오해하게 됩니다
- 스크롤은 `scrollIntoView` 가 아니라 `box.scrollTop` 으로 옮깁니다 — `scrollIntoView` 는 페이지까지 끌고 내려갑니다
- `GET /home` 에 **오늘 학사일정(`events`)** 을 실었습니다 (공용 + 내 개인 일정, `/calendar` 와 같은 조건). 하루치라 몇 줄 안 되고, 화면이 `/calendar` 를 또 부르지 않아도 됩니다
- 급식을 **같은 카드 안의 한 칸으로** 넣고 크기를 줄였습니다 (패딩·글자·버튼, `MealCard bare`). 따로 두면 한 행이 아니라 "큰 상자 + 작은 상자" 로 읽힙니다
- **밸런스가 안 맞던 진짜 원인은 열 너비가 내용과 무관하게 고정이었던 것**입니다. 방학엔 시간표가 없는데도 가운데 열이 `2fr` 을 그대로 먹어 600px 짜리 빈 상자가 남았습니다. 너비를 내용에서 뽑고(18rem/1fr/17rem — 과목명 한 줄·메뉴 한 줄이 안 접히는 폭), **시간표가 없는 날은 그 칸을 아예 없애** 2열로 줄였습니다. 남는 폭은 `VacationBar` 가 씁니다
- **카드 높이는 "지금" 칸과 "급식" 칸 중 큰 쪽이 정합니다.** 급식은 스크롤하지 않고 그냥 늘어나고, 시간표 칸만 그 안에서 흐릅니다. 열한 줄짜리 시간표가 높이 계산에 끼면 카드가 700px 이 되므로 그 칸만 `absolute inset-0` 으로 띄워 계산에서 뺐습니다
- `RetroButton` 에서 **`color` 버튼의 `scale-105` 를 뺐습니다.** 확대하면 테두리도 2px → 2.1px 로 같이 커져서 나란히 붙은 버튼 무리에서는 "선이 굵어졌다" 로 읽힙니다. 색이 이미 골랐다는 걸 말하므로 확대는 덧붙임일 뿐입니다
- 방학 막대의 캐럿을 **막대 안으로** 넣었습니다(`inset-y-0`). 위아래로 삐져나오니 막대가 깔끔하게 안 끝나고 선이 걸쳐 있는 것처럼 보였습니다 — D-14 칩이 막대 위에 얹힌 꼬리표가 되면서 조용해졌습니다
- ⚠️ 스크롤 영역까지 **flex 사슬이 이어져야** 합니다. 중간에 그냥 `div` 를 끼웠더니 높이 제약이 안 내려가 목록이 카드 밖으로 흘러넘쳤습니다
- V2 목록은 **공강도 한 교시씩** 줄로 그립니다 — 묶어서 "3–4교시 비어 있음" 한 줄로 두면 줄 높이가 제각각이라 스크롤할 때 리듬이 끊깁니다
- V2 에서 **시계를 뺐습니다.** 지금이 몇 교시인지가 이 화면의 단위이고, 시각은 목록 줄마다 이미 붙어 있습니다
- 급식 원문에서 `과일(메론` / `파인애플)` 처럼 **괄호가 안 닫힌 채 두 줄로 갈린 메뉴**를 앞 줄에 붙입니다 (`_lines`). `&` 로 갈린 것과 같은 이유입니다
- ⚠️ **V1** 분기 앞에 `import.meta.env.DEV &&` 를 붙였습니다. 이게 없으면 rollup 이 `layout` 이 절대 `"v1"` 이 안 된다는 걸 증명할 수 없어 두 판본을 다 실어 보냅니다. 번들에서 `빈 시간`(V1 전용)·DEV 바 라벨·데모 표본이 전부 빠진 걸 확인했습니다
- **홈 문구는 사용자가 직접 썼습니다.** 화면에 나오는 문구 28개를 `id`/`where`/`max`/`value` 로 뽑아 JSON 으로 넘기고 돌려받은 값을 반영했습니다 — 개발자가 문장을 지어내는 것보다 낫고, `max` 를 같이 적어 두면 화면이 안 깨집니다
- **넘치는 한 줄은 자르지 않고 훑습니다** (`MarqueeText`). Trade 배너처럼 문장 전체가 곧 내용인 자리에서 `…` 는 뒷말을 영영 못 읽게 만듭니다. 넘칠 때만 움직이고 속도는 거리와 무관하게 45px/s 고정, 양 끝에서 잠깐 멈춥니다. `prefers-reduced-motion` 이면 정지
- 급식 메뉴 글씨를 13 → 15px 로 키웠습니다. 카드 높이는 급식이 정하므로 그만큼 카드도 커집니다
- 하루가 다 끝났을 때 목록이 **마지막 수업**에 맞춰 열립니다. 맨 위(1교시)를 보여 주면 오늘 뭘 했는지가 안 보입니다


## 2026-08-03 — 홈 전면 개편 + 수업 없는 날 시간표 비우기

- 변경 파일: `frontend/src/pages/HomePage.tsx`, `frontend/src/components/DayRuler.tsx`(새로), `frontend/src/components/TodayTimeline.tsx`(새로), `frontend/src/components/VacationBar.tsx`(새로), `frontend/src/lib/homeDemo.ts`(새로, dev 전용), `frontend/src/lib/utils.ts`, `frontend/src/lib/friendsApi.ts`, `frontend/src/components/MealCard.tsx`, `frontend/src/components/atoms/RetroButton.tsx`, `backend/home_router.py`
- 요약: 방학인데 월요일 시간표가 그대로 나오던 걸 학사일정으로 걸러 내고, 홈을 "하루를 재는 자" 중심으로 다시 그렸습니다.

**시간표가 방학에도 나온 이유**는 학기 데이터가 `ClassTime.day` 로 **요일만** 들고 있어서입니다 — 날짜를 모르니 방학 중 월요일에도 월요일 시간표를 그대로 돌려줬습니다. `session_state` 옆에 휴업일 조회를 붙여 `off_reason`(`vacation`·`weekend`·`holiday`)을 정하고, 걸리면 수업 질의를 아예 하지 않습니다. 휴업일은 공용 일정(`owner_id IS NULL`)만 봅니다 — 개인 일정이 남의 수업을 없애면 안 됩니다.

**Today 가 구렸던 진짜 이유**는 목록이 "무엇이 있는가" 만 말하고 **"언제 비는가" 를 말하지 못해서**입니다. 여섯 줄이 전부 같은 상자였고, 2교시 다음이 5교시라는 사실도 `10교시` 가 두 줄로 접히는 것도 그대로 보였습니다. 그래서 하루(08:40~21:20)를 길이가 정해진 물건으로 놓고 교시를 **실제 시각에 비례해서** 얹었습니다(`DayRuler`) — 점심과 저녁·퇴실이 눈에 보이는 구멍으로 남고, 9교시 다음이 곧바로 10교시가 아니라는 사실이 그림 하나로 읽힙니다.

**채운 면 vs 점선 빈 면**이 "수업이 있다/없다" 의 유일한 신호입니다. 자 위의 칸은 장식(`aria-hidden`)이라 거기서만 알 수 있는 정보를 두지 않았고, 같은 내용을 `TodayTimeline` 이 글로 다시 씁니다.

**방학에는 재는 대상을 바꿉니다.** 하루 자를 그려 봐야 열한 칸이 전부 빈 상자라, 종업~개학을 재는 막대(`VacationBar`)에 오늘을 캐럿으로 찍고 주 단위 눈금을 넣었습니다. 문법(길이가 정해진 물건 + 캐럿)은 같고 눈금만 시각에서 날짜로 바뀝니다.

시각은 **하나의 시계에서만** 나옵니다 — 서버가 준 `now.minute` 에 그 뒤로 흐른 시간을 더해 씁니다. 따로 계산하면 1분 사이에 큰 글씨는 "공강" 인데 자는 수업 칸을 밝히는 어긋남이 생깁니다. 교시 시각표(`periods`)와 시간대(`breaks`)는 `GET /home` 이 실어 보냅니다 — 화면이 상수를 따로 들면 `periods.py` 만 고쳤을 때 조용히 어긋납니다.

**색을 한 겹 더 얹었습니다 (같은 날 추가).** 화면이 흑백에 시안 하나뿐이라 밋밋했는데, 정작 시간표에서 **아직 아무 색도 안 쓰던 축**이 하나 있었습니다 — 학과입니다. `GET /home` 이 `Subject → Course → Department` 를 타고 `department` 를 같이 보내고, 화면은 `getDepartmentColor()` 로 11색을 매핑합니다.

**색과 시간 상태를 다른 축으로 뒀습니다** — 색이 학과, 진하기가 지남(12%)/예정(26%)/지금. 시안과 핑크는 학과 팔레트에서 뺐습니다. 홈에서 이미 "지금"과 "한시 기능"을 뜻해서 학과에 주면 한 색이 두 뜻을 갖습니다. "지금" 칸만 학과색이 시안으로 덮이므로 아래에 학과 띠를 남겨, 지금 듣는 과목만 색을 잃는 일이 없게 했습니다.

지구과학을 청록에서 파랑으로 옮겼습니다 — 시안(=지금) 옆에 놓이는 일이 가장 잦은 학과인데 청록이면 두 칸이 같은 색 계열로 붙어 보였습니다.

**홈 상태 미리보기(개발 전용)도 붙였습니다.** 홈은 대부분이 "오늘이 무슨 날인가" 에 달려 있어서, 방학에 붙잡혀 있으면 수업 중 화면을 볼 방법이 개학까지 기다리는 것뿐이었습니다. `src/lib/homeDemo.ts` 가 **실제 응답 위에 상황만 덧씌웁니다** — 교시 시각표·급식·학기는 진짜 값 그대로라 목업이 아니라 진짜 데이터로 도는 화면을 봅니다. 프로덕션 번들에는 안 남는 걸 빌드 산출물에서 확인했습니다.



**여러 번 접었다 폈습니다 (같은 날).** 최종 형태는 이렇습니다 —

```
[Trade 배너]
[ Today (날짜·시계 / 가로 자 / 수업 줄) ────── | Meal ]   2.3fr : 1fr
```

거쳐 온 것과 남긴 판단:

- **가로 자를 세로 레일로 접어 봤다가 되돌렸습니다.** 세로는 목록과 한 몸이 되는 대신 하루의 모양이 안 보였습니다. 지금은 Today 카드 안에서 **자가 "언제 비는가" 를, 줄이 "무엇이 있는가" 를** 말합니다
- **⚠️ 가장 큰 실수: 카드 안에 카드를 열아홉 개 넣었습니다.** 자를 테두리 있는 칸 열한 개로, 목록을 줄마다 `border-2` 로 그렸더니 테두리가 4겹으로 중첩돼 전부 같은 목소리로 떠들었습니다. 게다가 글자가 죄다 10~15px `font-black` 이라 타입 스케일도 없어 시선이 멈출 곳이 없었습니다. 정작 `design-guide.md` 에는 "내부 구분선: `black/10`" 이라고 적혀 있었습니다. 자는 h-5 띠로, 목록은 `divide-y` 로 바꾸고 **면을 가진 건 진행 중인 줄 하나만** 남겼습니다 (그 줄은 카드 여백까지 꽉 채워 흐릅니다). 타입 스케일도 세웠습니다 — 시계 24 / 과목 17 / 시각 15 / 부연 11 / 눈금 9
- **히어로 카드는 없앴습니다.** 큰 글씨가 `일반지구과학 / 과학관 204 · 이도현 · 2분반` 이라고 외쳤는데 **바로 아래 핑크 줄이 글자까지 똑같은 말**을 하고 있었습니다. 날짜와 시계만 살려 시간표 카드 머리로 올렸습니다. 교훈 — **큰 글씨가 새로 주는 게 없으면 자리만 차지합니다**
- **학과색은 결국 전부 걷어냈습니다.** 열한 색 팔레트를 만들어 면 → 띠 → 점으로 계속 줄여 봤지만, 어느 크기로 줄여도 하루가 색표처럼 보이고 정작 "지금" 이 묻혔습니다. **면을 가진 건 진행 중인 줄 하나뿐**입니다. 핑크가 "지금" 을 가져가면서 Trade 배너는 시안으로 옮겼습니다. 색을 빼자 자가 밋밋해져서 빗금 두 종류로 성격을 주려 했는데 **더 안 읽혔습니다.** 진짜 문제는 다른 데 있었습니다 — **칸에 교시 번호가 없어서** 검은 막대가 몇 교시인지 알 방법이 자체가 없었고, 그래서 아래 목록과 연결이 끊겼던 겁니다. 무늬를 전부 걷고 칸마다 번호를 달았습니다(내 수업=실선 테두리·공강=점선·지난 수업=옅은 회색·점심 저녁=빗금, **면을 채우는 건 "지금"(핑크) 하나뿐**, 마름모 캐럿). 테두리도 **칸마다** 둘렀습니다 — 바깥을 하나로 감싸면 교시 사이 쉬는시간이 칸 안쪽 여백처럼 보여 칸을 세어야 알 수 있습니다. **부족한 건 장식이 아니라 이름표였습니다.** (버린 팔레트: 수학 `#7828c8` · 정보과학 `#00c2a8` · 물리학 `#ff7a00` · 화학 `#00a82d` · 생물학 `#8fd400` · 지구과학 `#2563ff` · 국어 `#ff3b3b` · 사회 `#ffb300` · 외국어 `#c2255c` · 예체능 `#ffd500` · 융합 `#5b4bff`. `department` 필드는 응답에 남아 있습니다)
- **큰 제목 뒤의 형광펜도 뺐습니다** — 제목이 배지가 되어 버립니다. "지금" 은 카드 머리의 교시 칩 하나가 답니다
- 급식 끼니 버튼과 방학 막대에는 색을 **넣었습니다** — 둘 다 뜻이 있는데 색이 없던 자리입니다. 끼니는 아침 노랑에서 저녁 보라로 해가 지듯 넘어가고(고른 것만 채움), 방학 막대는 **남은 쪽**을 계절색으로 칠합니다
- 급식은 시간표의 절반이 아니라 **1/3** 폭입니다. 메뉴는 늘 예닐곱 줄이라 넓히면 글자 오른쪽이 빕니다. `h-full` 을 걸어 두면 grid item 에서 **행 높이**로 풀려 `items-start` 가 무의미해집니다 (실제로 그렇게 늘어나 있었습니다)
- `RetroButton` 에 `color` 를 붙였습니다 — 값이 런타임에 정해지는 버튼을 원시 `<button>` 으로 다시 짜지 않게. 꽉 찬 면 위 글자색은 밝기로 정합니다(`readableInk`)


## 2026-08-01 — 컴포넌트 인벤토리 (`/inventory`, 개발 전용)

- 변경 파일: `frontend/src/pages/InventoryPage.tsx` (새로), `frontend/src/App.tsx`
- 요약: funky-ui 토큰 구조를 다시 잡기 전에, 이 앱이 실제로 그리고 있는 버튼·인풋·카드·색을 종류별로 늘어놓은 표본집입니다.

표본은 지어낸 게 아니라 **코드에서 그대로 옮겼고** 항목마다 `파일:줄` 을 답니다. 인벤토리의 목적은 "무엇을 만들까" 가 아니라 **"이미 무엇을 만들어 버렸나"** 를 보는 것이라, 정리하지 않고 있는 그대로 늘어놓습니다.

세어 본 결과가 이렇습니다 — 원시 `<button>` **76개** vs `RetroButton` **26개**, 서로 다른 버튼 아키타입 **31종**, 그중 하드 그림자(정체성)를 가진 건 **8개**. `RetroButton` 의 `variant` 4종 중 실제로 쓰인 건 `black` 하나뿐이고, `secondary` 는 어떤 분기도 타지 않는 죽은 값입니다. 가장 많이 쓰인 축은 색이 아니라 `isSelected` 불리언(41회)이었습니다.

**축이 부족하면 축을 우회합니다.** 76개 중 25개가 테두리도 배경도 없는 무리인데 atom 으로는 표현할 방법이 없고, TradePage 는 색마다 진한 면/옅은 면을 짝지어 쓰면서 옅은 면을 만드는 방식만 네 가지(`white/50`·`accent4/25`·`white/60`·`green/20`)로 갈라져 있습니다. 사실상 `color × variant` 를 손으로 재발명한 것입니다.

색은 이름만 다르고 **값이 funky-ui accent 와 전부 같습니다** (`retro-accent1` = cyan `#3decfd` …). 옮길 때 색을 새로 고를 일이 없다는 뜻입니다. 다만 `retro-primary` 와 `retro-accent3` 이 같은 hex 이고, `design-guide.md` 의 색 표는 코드와 어긋나 있습니다 — primary 를 `#ff3e3e`(빨강)로 적고 있는데 실제 값은 `#ff4eba`(핑크)입니다.

그림자는 가이드가 둘이라고 적은 자리에 **12개**가 있습니다.

축이 정해지고 funky-ui 로 옮기고 나면 이 페이지는 지웁니다. `import.meta.env.DEV` 로 라우트를 걸어 메뉴에는 올리지 않았습니다.

## 2026-08-01 — 트레이드에서 과목에 호버하면 시간표에서 그것만 남습니다

- 변경 파일: `frontend/src/components/TimetableGrid.tsx`, `frontend/src/pages/TradePage.tsx`, `frontend/src/components/atoms/RetroCard.tsx`
- 요약: My Subjects 목록에서 과목에 마우스를 올리면 시간표의 나머지 칸이 흐려집니다.

과목이 열 개쯤 되면 "이게 어느 칸이지" 를 눈으로 좇게 됩니다. 목록과 시간표를 잇는 가장 싼 방법이 호버입니다.

**색을 덧칠하지 않았습니다.** 칸 색은 이미 상태(이동·충돌·들어옴)를 뜻하고 있어서, 강조색을 하나 더 얹으면 그 뜻이 흐려집니다. 대신 나머지를 `opacity-20` 으로 눕혀서 남은 것이 도드라지게 했습니다.

`RetroCard` 가 div 속성을 그대로 받도록 열었습니다 — 마우스 이벤트를 붙이려고 카드를 한 겹 더 감싸면 그림자와 여백이 어긋납니다.

**지목된 칸에는 테두리까지 켭니다.** 마우스를 직접 올렸을 때 뜨는 오버레이를 그대로 재사용해서, 목록에서 가리키든 칸을 짚든 같은 모양이 되게 했습니다. 과목 카드도 호버하면 4px 떠오르고 그림자가 커집니다 — 어느 줄을 가리키고 있는지 손이 아니라 화면이 알려 줘야 합니다.

## 2026-08-01 — 트레이드에서 못 듣는 과목 걸러내기, 스피너를 원으로

- 변경 파일: `frontend/src/pages/TradePage.tsx`, `frontend/src/components/atoms/RetroSpinner.tsx`, `frontend/src/pages/HomePage.tsx`, 가이드 문서
- 요약: 드랍한 과목을 시간표에서 빼고, 추가 후보에서 이미 들었거나 선수를 안 채운 과목을 걸러 냈습니다. 스피너는 paper-ui 의 도는 원으로 바꿨습니다.

**드랍한 과목을 시간표에 그리지 않습니다.** 한때 색만 바꿔 남겨 뒀지만, 어느 시간이 비는지는 칸이 비어야 가장 잘 보입니다 — 남겨 두면 새로 넣을 자리가 여전히 차 있어 보입니다. 범례의 "빠짐" 도 같이 없앴습니다.

**`Opened by Drop` 을 `You Can Add` 로 바꾸고, 담을 수 없는 과목을 뺐습니다.** 이미 들은 과목(또는 선수 체인상 들었을 과목)과 선수를 안 채운 과목은 권할 이유가 없습니다. 판단은 `GET /curriculum` 의 선수관계와 `GET /curriculum/progress/{stuId}` 의 이수 내역으로 합니다 — Zamong 이 쓰던 `courseState()` 를 그대로 씁니다.

**검색은 거르지 않습니다.** 굳이 찾아보고 싶을 수 있어서 결과에는 남기되, 담을 수 있는 것부터 놓고 나머지는 회색으로 눕히고 `이수함`·`선수 미이수` 를 붙였습니다. 교육과정에 없는 과목(외국인 전형 등)은 판단할 근거가 없어 그냥 통과시킵니다 — 모르는 걸 막으면 멀쩡한 과목이 사라집니다.

**스피너는 paper-ui 의 `Spinner` 를 옮겼습니다** — 옅은 링에 위쪽 한 조각만 진한 원, 900ms linear. 블록 3개보다 이쪽이 로딩으로 더 곧장 읽힙니다.

## 2026-08-01 — HeroUI 스피너가 한 번도 돈 적이 없었습니다

- 변경 파일: `frontend/src/components/atoms/RetroSpinner.tsx` (신규), `frontend/src/{components/MealCard.tsx,components/FriendsManager.tsx,pages/HomePage.tsx,pages/SearchPage.tsx,pages/AnalysisPage.tsx}`, `frontend/{design-guide.md,component-guide.md}`
- 요약: HeroUI `<Spinner />` 를 걷어내고 `RetroSpinner` 로 바꿨습니다.

급식 스피너가 "선 하나" 로 보인다는 지적을 받고 DOM 을 뜯어보니 **회전이 아예 없었습니다** (`animation-name: none`). 크기도 `4px x 20px` — `w-5` 가 안 먹어서 테두리만 남은 세로선이었습니다.

원인은 **Tailwind v4 가 소스를 훑어서 유틸리티를 만든다**는 데 있습니다. `animate-spinner-ease-spin`·`w-5` 는 `node_modules/@heroui` 안에만 있어서 스캔 대상이 아니고, 그래서 CSS 가 생성되지 않았습니다. 빌드된 CSS 에 `animate-spinner-ease-spin` 이 **0개** 였습니다. 홈·검색·통계·친구까지 **다섯 군데 전부** 같은 상태였고, 처음부터 그랬습니다.

HeroUI 소스를 `@source` 로 추가하는 길도 있지만 원형 스피너는 어차피 전역 `rounded-none!` 과 싸웁니다. 직각 블록 3개가 어긋나게 깜빡이는 쪽이 나머지 화면과 같은 말을 해서 자체 컴포넌트로 갔습니다.

**같은 함정이 다른 HeroUI 컴포넌트에도 있을 수 있습니다.** `classNames` 로 우리가 직접 스타일을 주는 곳(`SearchInput` 등)은 멀쩡하지만, 라이브러리 기본 클래스에 기대는 부분은 조용히 빠집니다.

## 2026-08-01 — 급식 로딩·빈 상태, 2열 높이 맞춤

- 변경 파일: `frontend/src/components/MealCard.tsx`, `frontend/src/pages/HomePage.tsx`, 가이드 문서
- 요약: 급식을 받는 동안 스피너를 돌리고, 비면 `No Data Found` 를 적고, 가로로 놓인 두 카드의 높이를 맞췄습니다.

학교 API 가 3~5초씩 걸리는 날이 있어서 **빈 칸으로 두면 고장난 것처럼 보였습니다.** 받는 동안은 스피너, 그날이나 그 끼니가 비어 있으면 `No Data Found` 입니다.

**Now 와 급식은 높이를 맞춥니다** — `h-full` 로 늘리고 안쪽을 `flex-1` 로 밀어서, Now 카드의 아래 줄(다음 수업·개학까지)이 바닥에 붙습니다. 나란히 놓고 한쪽만 짧으면 어긋나 보입니다.

## 2026-08-01 — 홈 배치를 2열로, 친구를 Browse 로

- 변경 파일: `frontend/src/pages/{HomePage.tsx,BrowsePage.tsx}`, `frontend/src/components/{FriendsManager.tsx (신규),MealCard.tsx,FriendsModal.tsx (삭제)}`, `frontend/src/{App.tsx,lib/friendsApi.ts}`, `backend/home_router.py`, 가이드 문서
- 요약: 시계 카드를 없애고 Now·급식을 가로로 놓았습니다. 급식은 끼니 토글, Trade 배너는 맨 위 핑크, 친구 관리는 Browse 탭으로 옮겼습니다.

**시계 카드를 없앴습니다.** 큰 시각 하나가 맨 위를 차지하니 정작 "어디로 가야 하는가" 가 아래로 밀렸습니다. 요일과 교시는 Now 카드 오른쪽 위에 작게 붙였습니다.

**Now 와 급식을 2열로 놓았습니다** (`md:grid-cols-2`). 세로로만 쌓으니 화면이 길어지기만 하고 위쪽이 비었습니다. 급식은 세 끼를 한꺼번에 늘어놓던 걸 **끼니 토글**로 바꿨습니다 — 한 끼가 예닐곱 줄이라 셋을 펼치면 카드 혼자 한 칸을 다 먹습니다. 지금 시간대의 끼니로 열립니다.

**친구는 Browse 의 Friends 탭으로 옮겼습니다.** 홈에 모달로 뒀더니 홈에서 할 일은 등록뿐인데 창을 열어야 했고, 사람을 찾는 일은 어차피 Browse 가 하는 일입니다. `GET /home` 도 친구를 안 돌려줍니다 — 홈이 안 그리는 걸 계산할 이유가 없습니다. 공강 하이라이트는 시안(`accent1`)으로 맞췄습니다: 이 앱에서 시안은 "지금" 입니다.

**Trade 배너를 맨 위로 올리고 핑크로 바꿨습니다.** 친구 배지가 홈에서 빠지면서 핑크가 비었고, 배너가 노랑일 때 급식의 "지금 끼니" 강조와 색이 겹쳤습니다. 이제 시안=지금, 핑크=한시 기능입니다.

## 2026-08-01 — 배포 서버가 UTC 였습니다

- 변경 파일: `backend/{periods.py,home_router.py,friends_router.py,terms.py,CLAUDE.md}`
- 요약: "지금" 을 읽는 곳을 전부 `periods.now()` / `periods.today()` (KST) 로 돌렸습니다.

급식을 붙이고 운영에서 확인하다가 서버가 **7월 31일**이라고 답하는 걸 봤습니다. 서버 타임존이 `Etc/UTC` 였고, `datetime.now()` 를 그대로 쓰던 홈은 **지금 시각이 9시간 어긋나고 날짜가 하루 밀린 상태로 배포돼 있었습니다.** 방학이라 시간표가 비어 있어서 아무도 눈치채지 못했습니다 — 개학했으면 "지금 몇 교시" 와 "오늘 시간표" 가 둘 다 틀렸을 겁니다.

고친 곳은 넷입니다: 홈(지금 교시·오늘), 급식 날짜 범위, 지금 공강인 친구, 학기 판정. 세션 만료처럼 **UTC 로 저장하고 UTC 로 비교하는 값은 그대로** `utcnow()` 를 씁니다 — 섞으면 만료가 9시간 어긋납니다.

**시스템 타임존이 아니라 코드를 고쳤습니다.** 서버를 옮겨도 학교 시각은 KST 여야 하고, 그 서버에 있는 다른 서비스의 타임존까지 우리가 정할 일은 아닙니다.

## 2026-08-01 — 급식 날짜 넘기기

- 변경 파일: `backend/{home_router.py,CLAUDE.md,api-guide.md}`, `frontend/src/components/MealCard.tsx` (신규), `frontend/src/pages/HomePage.tsx`, `frontend/src/lib/friendsApi.ts`, `frontend/{CLAUDE.md,component-guide.md}`
- 요약: 홈 급식을 `MealCard` 로 떼어내고 날짜를 앞뒤로 넘겨 볼 수 있게 했습니다 (`GET /meal?date=`).

**메뉴를 홈 응답에서 떼어냈습니다.** 배포하고 나서야 알았는데 학교 API 가 서버에서 3~5초씩 걸립니다 (처음엔 타임아웃 5초에 걸려 급식이 통째로 안 나왔습니다). 홈 응답에 묶여 있으면 그동안 홈 전체가 비어 있어서, `/home` 은 급식 키가 있는지와 지금 끼니만 알려 주고 메뉴는 `MealCard` 가 `GET /meal` 로 따로 받습니다. 홈은 0.03초, 급식 칸만 기다립니다. 그날 첫 조회만 느리고 다음부터는 DB 에서 바로 옵니다.

**홈이 1분마다 다시 받아도 보고 있던 날짜는 그대로 둡니다** — 어제 급식을 읽는 중에 화면이 오늘로 튀면 안 됩니다.

범위는 **앞뒤 31일**입니다. 화살표를 계속 누르면 학교 API 를 그만큼 두드리게 되고, 그 밖은 볼 이유도 없습니다.

`/home` 의 `meal` 은 **키가 없으면 통째로 null** 로 바꿨습니다. 예전에는 `menu` 만 null 이라 "키가 없다" 와 "그날 급식이 없다" 가 구분되지 않았고, 날짜를 넘길 수 있게 되면서 이 둘이 다른 화면이 됐습니다.

## 2026-08-01 — 홈 다듬기 (친구 모달·급식·Trade 배너·방학)

- 변경 파일: `backend/{models.py,home_router.py,CLAUDE.md,api-guide.md}`, `frontend/src/components/{FriendsModal.tsx,atoms/SearchInput.tsx}`, `frontend/src/pages/HomePage.tsx`, `frontend/src/lib/friendsApi.ts`, `frontend/{CLAUDE.md,component-guide.md}`
- 요약: 친구 모달을 검색창 하나로 합치고, 급식을 DB 에 쌓고, Trade 배너를 넣고, 방학 화면을 학기 중과 같은 레이아웃으로 되돌렸습니다.

**친구 모달 — 검색창이 하나입니다.** 먼저 등록한 사람을 걸러 보여 주고, 거기 없으면 그때 전교생에서 찾아 "추가" 를 내밉니다. 찾기와 추가를 따로 두면 "이미 등록했나?" 를 확인하러 두 곳을 봐야 합니다. 모달은 가운데로 옮기고(위에 붙어 잘려 보였습니다), 검색창은 `SearchInput` 에 `size="sm"` 을 붙여 줄였습니다 — 모달 안에 `lg` 를 두면 검색창이 창을 다 차지합니다. 추가는 행 전체가 아니라 **버튼**입니다.

**급식을 `meal_menus` 에 쌓습니다.** DB 를 먼저 보고 없을 때만 학교 API 를 부릅니다 — 지난 급식은 바뀌지 않고, 메모리에만 두면 서버를 재시작할 때마다 처음부터 다시 받습니다. **아직 안 올라온 날은 저장하지 않습니다**: 빈 값으로 적어 두면 영영 빈 채로 굳습니다. 원문이 개행 섞인 한 덩어리라 서버가 줄로 쪼개 보내고, 그 김에 축산물 이력번호(`찹스테이크(호주산801000310667)`)를 지우고 `&` 로 시작하는 줄을 앞 줄에 붙입니다.

**한 색은 한 뜻입니다.** Trade 배너를 넣자 급식의 "지금 끼니" 강조와 같은 노랑이 됐습니다. 시안이 이미 Now 카드에서 "지금" 을 뜻하고 있어서 급식 강조를 시안으로 옮기고, 노랑은 한시 기능(Trade) 하나만 쓰게 뒀습니다. 친구 배지(핑크)는 `counted` 일 때만 그립니다 — 수업 시간이 아니면 "공강" 이라고 할 게 없는데 핑크가 떠 있으면 거짓말입니다.

**방학 화면을 되돌렸습니다.** "여름방학입니다" 를 큰 카드로 띄우니 방학 안내가 홈의 주인공이 됐습니다. 학기 중과 자리를 똑같이 두고 Now 에 한 줄, Today 에 연한 한 줄로만 알립니다. 주말도 같은 방식이라 분기가 하나 줄었습니다.


## 2026-08-01 — 홈 화면

- 변경 파일: `backend/home_router.py` (신규), `backend/main.py`, `frontend/src/pages/HomePage.tsx`·`components/FriendsModal.tsx` (신규), `frontend/src/pages/FriendsPage.tsx` (삭제), `frontend/src/{App.tsx,lib/friendsApi.ts,components/Sidebar.tsx,components/BottomNav.tsx}`, 가이드 문서 일습
- 요약: `/` 를 홈으로 바꾸고 검색을 `/search` 로 옮겼습니다. 홈은 지금 교시·가야 할 교실·오늘 시간표·급식·지금 공강인 친구를 한 화면에 보여 줍니다.

**`GET /home` 한 번에 다 받습니다.** 켜자마자 보이는 자리라 여러 번 물어보면 화면이 조각조각 채워지는 게 그대로 보입니다. 1분마다 다시 받습니다 — "지금 몇 교시" 가 떠 있는 값이라 멈춰 있으면 틀립니다.

**방학은 학사일정의 `개학`·`종업` 표지로 판단합니다.** "방학" 이라는 일정이 DB 에 따로 없어서, 마지막 종업이 마지막 개학보다 뒤면 방학입니다. 방학이면 시간표 자리를 남은 날짜와 한마디로 바꿉니다 — 그대로 두면 빈 칸만 남습니다. 한마디는 날짜로 골라서 하루 안에는 안 바뀝니다.

**Friends 탭을 없앴습니다.** 시간표를 겹쳐 보는 일은 Trade 의 Timetable Compare 가 이미 하고 있어서, 홈의 "지금 공강인 친구" 를 누르면 뜨는 모달에 목록·추가·삭제만 남겼습니다.

**검색 경로가 바뀌었습니다.** 예전에 공유된 `/?q=…` 링크가 죽지 않도록 홈에 `?q=` 가 붙어 오면 `/search` 로 넘깁니다.

**급식은 코드만 붙었습니다.** `api.ksain.net/v1/meal.php` (옛 ksalendar 서버가 쓰던 것)를 그대로 쓰는데 `KSAIN_API_KEY` 가 필요합니다. 키가 없으면 **급식 칸만 비고 나머지는 그대로 돕니다** — 홈 전체가 죽으면 안 됩니다.


## 2026-07-31 — 교시 시각표를 「생활관 일과 운영」 표로 확정

- 변경 파일: `backend/periods.py`, `backend/friends_router.py`, `backend/CLAUDE.md`, `deploy-guide.md`
- 요약: 생활관에 붙은 공식 표로 대조해 확정했습니다. **1~9교시는 맞았고 10·11교시가 틀렸습니다.**

**9교시 다음이 10교시가 아닙니다.** 9교시가 18:30 에 끝나고 한 시간을 건너뛰어 10교시가 19:30 에 시작합니다(그 사이가 저녁·퇴실). 5교시부터 50+10 을 그대로 이어붙이면 18:40·19:40 이 나오는데 틀립니다 — 처음에 그렇게 잡았다가, 자습과 안 맞는 걸 보고도 "겹치는 시간대"로 넘긴 게 잘못이었습니다. 두 번째로 고칠 때는 10·11교시를 자습 창(19:30~21:30)의 절반씩으로 잡았는데 그것도 틀렸습니다 — **10·11교시도 50분 수업(19:30~20:20 / 20:30~21:20)이고, 자습이 그걸 감싸는 더 넓은 창**입니다.

그 밖에 바로잡은 것: 점심 `~13:20` → **`~13:10`**(13:10~13:20은 퇴실), `AA 미팅` → **`학급모임`**. 아침식사·입실도 표에 있어 추가했습니다.

**저녁과 자습은 교시와 겹칩니다** — 저녁은 9교시와, 자습은 10·11교시를 감쌉니다. 그래서 `current_period()` 와 `current_break()` 를 배타적으로 보면 안 됩니다.

**배포는 Netlify 가 아니라 Vercel 입니다.** 문서를 고쳤고, 올리는 프론트는 `frontend/` 하나뿐입니다 — `bench-frontend/` 는 동결이라 어디에도 안 올립니다.

## 2026-07-31 — 백엔드를 서버 하나로 합침 + bench 는 캐시하지 않음

- 변경 파일: `backend/main.py`·`classes_router.py`·`bench_router.py`, `backend/bench_main.py` (삭제), `bench-frontend/src/App.tsx`·`vite.config.ts`·`.env`, `frontend/src/App.tsx`, 가이드 문서 일습
- 요약: 진입점을 둘로 나눠 두던 걸 접고 **서버 하나가 두 프론트를 받습니다.** 대신 ksa-bench 는 수업 데이터를 localStorage 에 캐시하지 않습니다.

**왜 합쳤나** — Trade 를 되살리며 명단을 응답에 되돌리자 두 앱의 `GET /` 이 사실상 같아졌습니다(bench 판에만 학번 분포가 더 있었는데 그건 class-explorer 에도 넣으면 그만입니다). 같은 응답을 두 벌 유지하고 프로세스를 둘로 띄울 이유가 없어졌고, 배포도 systemd 유닛 하나로 끝납니다. `bench_main.py` 는 지웠습니다.

**대신 잃은 것을 분명히 해 둡니다.** 접근 제어가 "라우터를 그 앱에 안 붙인다"에서 **권한 검사**로 옮겨졌습니다. 새 엔드포인트가 남의 데이터를 돌려줄 수 있으면 이제 의존성으로 막아야 합니다 — 가이드 세 곳에 적어 뒀습니다.

**두 프론트의 차이는 이제 둘뿐입니다.** ① 훑는 UI 가 없다(전교생 목록·다중 검색·불린·초성 없음) ② **명단을 localStorage 에 안 남긴다.** ②가 이번에 새로 한 것입니다 — 응답에 분반 명단이 들어 있어서 캐시하면 전교생 명단이 브라우저에 파일로 남습니다. bench 는 캐시를 통째로 걷어내고 메모리에만 두며, class-explorer 를 쓰던 브라우저에 남은 옛 캐시는 시작할 때 지웁니다.

localStorage 쓰기를 전수 확인했습니다 — 세션 토큰·선택 학기·검색 히스토리뿐이고 명단이 남는 경로는 없습니다.


## 2026-07-31 — 교시 시각표 + 친구 기능을 두 앱 공통으로

- 변경 파일: `backend/periods.py`·`friends_router.py` (신규), `backend/main.py`·`bench_main.py`·`bench_router.py`, `frontend/src/pages/FriendsPage.tsx`·`lib/friendsApi.ts` (신규), `frontend/src/{App.tsx,components/Sidebar.tsx}`, `bench-frontend/` 동일 반영, 가이드 문서 일습
- 요약: 교시별 시각표를 서버에 두고, 친구 기능을 **class-explorer 에도** 넣었습니다. 메인은 다시 class-explorer 입니다.

**교시 시각표는 서버가 원본을 갖습니다** (`backend/periods.py`, `GET /periods`). 50분 수업 + 10분 쉬는시간으로 1~4교시(08:40~12:30) → 점심·AA 미팅 → 5~11교시(13:40~20:30). 프론트가 상수를 따로 들면 한쪽만 고쳤을 때 조용히 어긋나서, 화면도 서버에서 받아 씁니다.

⚠️ **표에 저녁 식사 시간이 없습니다.** 9~10교시가 17:40~19:30 으로 이어지는데 실제로 그 사이에 저녁이 있다면 10·11교시가 통째로 밀립니다. 자습(19:30~21:30)도 11교시(19:40~20:30)와 겹칩니다. 둘 다 확인되면 `periods.py` 의 표만 고치면 됩니다.

**"지금"은 서버 시계로 정합니다.** 클라이언트 시계는 틀어져 있거나 손댈 수 있어서, "지금 공강" 이 사람마다 다르게 보이면 안 됩니다.

**친구는 두 앱 공통 라우터(`friends_router.py`)로 뺐습니다.** 처음엔 `bench_router` 안에 있었는데 class-explorer 에도 필요해졌습니다 — 복사하면 두 벌이 되니 옮겼습니다. 등록은 **단방향**이고 수락이 없습니다: 두 앱 모두 남의 시간표를 이미 볼 수 있어서 승인 절차는 마찰만 늘고 막아 주는 게 없습니다.

**사람 찾기만 앱마다 다릅니다.** ksa-bench 는 명단이 없어 `GET /students/search` 로 서버에 묻고, class-explorer 는 이미 학기 전체를 들고 있어 로컬에서 찾습니다. 두 글자 이상·20명 상한은 같습니다.

`/friends/busy`·`/friends/now` 응답에는 **과목명이 없습니다** — 슬롯(`"MON-3"`)만 나갑니다. 공강을 맞추는 데 필요 없고, 주면 "누가 뭘 듣는지" 훑는 화면이 됩니다.

**메인은 다시 class-explorer 입니다.** ksa-bench 는 배포 여부가 안 정해져 보류로 두고, 새 기능은 class-explorer 에 먼저 넣습니다.


## 2026-07-31 — ksa-bench: Trade 복원 + 친구(단방향)

- 변경 파일: `backend/models.py`·`bench_router.py`, `bench-frontend/src/pages/FriendsPage.tsx` (신규), `bench-frontend/src/{App.tsx,types/index.ts,lib/benchApi.ts,lib/searchEngine.ts}`, `bench-frontend/src/pages/TradePage.tsx`·`lib/tradeEngine.ts` (복원), 가이드 문서 일습
- 요약: 선을 "명단이 안 내려간다"에서 **"훑는 화면이 없다"**로 옮겼습니다. Trade 를 되살리려면 명단이 필요해서입니다.

**Trade 를 되살리며 분반 명단을 응답에 되돌렸습니다.** `findTradePartners` 가 "내가 원하는 분반의 수강생 전원을 훑어 내 분반을 받을 수 있는 사람을 찾는" 함수라, 명단이 입력이고 이름 목록이 출력입니다. 섹션 단위로 좁히려면 TradePage(1500줄)가 전체 학생 인덱스를 미리 만드는 구조부터 갈아야 해서, 이번엔 그대로 복원했습니다. **가이드에 이 사실을 명시**해 뒀습니다 — 문서가 "명단이 안 나간다"고 계속 주장하면 다음에 읽는 사람이 틀린 전제로 기능을 붙입니다.

그래서 지금 ksa-bench 가 class-explorer 보다 좁은 지점은 셋입니다: 검색이 한 번에 한 명(다중·불린·초성 없음), 전교생을 늘어놓는 화면 없음, `/admin/*`·남의 누적 이수 라우터가 등록되지 않음.

**친구는 단방향입니다.** 수락 절차를 두지 않은 이유는, 남의 시간표가 어차피 `GET /students/{stu_id}` 로 한 명씩 열리기 때문입니다 — 승인은 마찰만 늘고 막아 주는 게 없습니다. 그래서 이 표는 새로 뭘 여는 게 아니라 북마크에 가깝습니다.

**친구 격자는 과목명을 안 받습니다.** `GET /friends/busy` 가 `["MON-3", …]` 슬롯만 돌려줍니다. 공강을 맞추는 데 과목이 필요 없고, 주면 "누가 뭘 듣는지" 훑는 화면이 됩니다. Analysis 의 Timetable Compare 를 이걸로 대체했습니다.

**검증 스크립트가 거짓 통과하고 있었습니다.** venv 의 FastAPI 가 최신이라 `include_router` 한 것을 `_IncludedRouter` 래퍼로 들고 있는데, `app.routes` 를 그대로 세면 기본 4개만 보입니다. 그 상태로는 "bench 에 금지 라우터 없음" 이 항상 통과합니다 — 래퍼를 재귀로 펼치고, 펼치기 실패 시 단언으로 죽게 고쳤습니다(explorer 42 / bench 36).

미결: **교시별 시각표가 앱 어디에도 없습니다.** "지금 공강인 친구"는 그게 있어야 만들 수 있어 주간 격자까지만 했습니다.


## 2026-07-31 — ksa-bench: 명단 없이 도는 앱 만들기

- 변경 파일: `backend/app_factory.py`·`bench_main.py`·`bench_router.py`·`classes_router.py` (신규), `backend/main.py`·`curriculum_router.py`, `bench-frontend/src/lib/{benchApi.ts,searchEngine.ts}`, `bench-frontend/src/components/StudentLookup.tsx` (신규), `bench-frontend/src/{App.tsx,types/index.ts}`, `bench-frontend/src/pages/{SearchPage,BrowsePage,AnalysisPage,ZamongPage}.tsx`, 가이드 문서 일습
- 요약: 전교생에게 열 ksa-bench 를 만들면서, 분반 명단을 응답에서 빼고 사람은 서버에 한 명씩 물어보게 바꿨습니다.

**진입점을 둘로 갈랐습니다.** `backend.main:app`(class-explorer)과 `backend.bench_main:app`(ksa-bench)이 같은 코드·같은 DB 를 쓰되 라우터가 다릅니다. 권한 검사로 가르지 않은 이유는, 한 앱에 명단 엔드포인트와 공개 서비스가 같이 살면 **나중에 기능을 붙이다 의존성 하나를 빠뜨리는 것이 곧 사고**가 되기 때문입니다. 등록이 안 되면 그 실수가 성립하지 않습니다. bench 에 안 붙인 것: `GET /`(명단 포함), `/curriculum/progress/{stu_id}`(아무 학생의 누적 이수), `/admin/*`(`/admin/students` 가 전교생 명단).

**화면에서 가리는 건 아무 의미가 없습니다.** 지금 구조는 `GET /` 하나로 학기 전체가 브라우저에 내려오고 localStorage 에 1시간 남습니다. 그래서 UI 가 아니라 **응답**을 바꿨습니다 — `bench_router` 의 `GET /` 에는 분반 `students` 배열이 없고, 사람은 `GET /students/search`(이름·학번만, 2글자 이상, 20명 상한) → `GET /students/{stuId}`(한 명) 두 단계로 갑니다. 계정 단위 rate limit(검색 40/분, 상세 30/분)을 걸어 전교생을 훑는 데 20분 넘게 걸리게 했습니다.

**목표는 차단이 아니라 비용입니다.** 가온누리에도 전교생 시간표 검색이 있고 학번이 연속이라 순회하면 긁힙니다. 완전히 막는 건 의미가 없으니, 명단을 얻는 비용을 거기와 같게 맞추는 게 기준입니다. 작정한 사람은 어차피 못 막고, 문제는 재미로 훑는 수백 명입니다.

**학번 분포는 남겼습니다.** 처음에는 뺐습니다 — 1학년 필수 과목이 P/F 라 Fail 하면 재수강하고, 그 과목에서 혼자 다른 학번이면 이름을 지워도 "누군가 재수강 중"이 드러나기 때문입니다. 다만 **그게 누구인지는 여전히 한 명씩 찾아야** 하므로, 선을 "이름이 나가지 않는다"에 두기로 하고 되돌렸습니다(분반 `year_counts`, 과목 `subject_year_counts`). 학년 필터도 명단 대신 이 숫자로 돕니다.

**분석 화면**은 명단을 프론트가 들고 직접 세던 구조라, 세는 일을 서버로 옮겼습니다(`GET /stats/enrollment` — 주당 교시·수강 과목 수 분포). "Timetable Compare"(학생 여럿을 골라 겹쳐 보기)는 다중 조회라 뺐습니다.

**옮기지 못한 것**: `/trade` 는 명단으로 상대를 찾는 방식이라 그대로는 안 됩니다 — 양쪽이 등록했을 때만 맞춰 주는 형태로 새로 만들어야 합니다. `/admin` 은 안전한 것만 골라 새로 만듭니다.

검증: 두 앱의 라우트를 **핸들러 기준**으로 대조해 bench 에 금지 핸들러가 없음을 확인했습니다(explorer 42 / bench 32 — 경로가 아니라 어느 함수가 붙었는지로 봐야 합니다. bench 에도 `GET /` 은 있고, 명단 없는 자체 구현입니다). 로컬 DB 로 응답에 개인 필드가 없고 rate limit 이 실제로 429 를 내는 것까지 확인. 두 프론트 모두 빌드 통과. lint 는 53 → 10 errors (`searchEngine.ts` 의 `any` 40개가 사라짐).

## 2026-07-31 — ksa-bench 를 위한 두 번째 프론트 분리

- 변경 파일: `bench-frontend/` (신규, `frontend/` 복사본), `CLAUDE.md`, `.gitignore`, `tasks.md`
- 요약: 전교생에게 열 ksa-bench 를 만들기 위해 프론트를 둘로 나눴습니다. class-explorer(`frontend/`)는 손대지 않고 그대로 둡니다.

**왜 리포를 안 갈랐나** — 백엔드가 두 벌이 되면 KEIS 응답이 한 번 바뀔 때마다 같은 수정을 두 번 하게 되고, 몇 달 뒤엔 둘이 서로 달라져 "복사해서 시작한" 이득이 사라집니다. 한 리포에 두면 백엔드 공유가 지켜야 할 규율이 아니라 **구조**가 됩니다.

**`frontend/` 는 이름도 안 바꿨습니다.** Netlify base directory·CI·문서가 전부 그 경로를 물고 있어서, 리네임하는 순간 "class-explorer 는 건드리지 않는다"가 깨집니다. `bench-frontend` 와 비대칭인 건 감수했습니다.

**격리는 권한이 아니라 프로세스로 갑니다.** 하나의 앱에 명단을 통째로 주는 엔드포인트와 공개 서비스가 같이 살면, 새 기능을 붙이다 권한 검사 한 줄을 빠뜨리는 것이 곧 사고가 됩니다. ASGI 진입점을 둘로 두어 bench 쪽에는 그 라우터가 **아예 등록되지 않게** 할 예정입니다(아직 안 했습니다).

검증: 두 프론트 모두 빌드 통과, lint 53 errors / 5 warnings 로 **동일** — 복사가 온전하고 새로 생긴 문제가 없다는 뜻입니다.

## 2026-07-31 — 학사일정 달력 + 권한 3단계

- 변경 파일: `backend/models.py`, `backend/migrations.py`, `backend/auth.py`, `backend/admin_router.py`, `backend/auth_router.py`, `backend/create_user.py`, `backend/calendar_router.py` (신규), `backend/parse_calendar_pdf.py` (신규), `backend/import_calendar.py` (신규), `backend/calendar_seed.json` (신규), `backend/main.py`, `frontend/src/pages/CalendarPage.tsx` (신규), `frontend/src/components/{CalendarGrid,EventFormModal,RequestSidebar}.tsx` (신규), `frontend/src/lib/calendar.ts` (신규), `frontend/src/lib/utils.ts`, `frontend/src/types/index.ts`, `frontend/src/App.tsx`, `frontend/src/components/Sidebar.tsx`, `frontend/src/pages/AdminPage.tsx`, 가이드 문서 일습
- 요약: 연간 학사일정 PDF를 읽어 DB에 넣고 달력으로 보여 줍니다. 개인 일정을 얹을 수 있고, 일반 계정은 공용 일정을 제안하면 매니저가 허용합니다.

**권한** — `is_admin` 불리언을 `role`(`user < manager < admin`) 한 컬럼으로 옮겼습니다. 불리언을 하나 더 붙이면 "둘 다 켜진 계정"의 뜻이 애매해집니다. 옛 컬럼은 값을 옮긴 뒤 지웠습니다 — 모델에 없는 컬럼이 스키마에 남으면 다음에 읽는 사람이 뭐가 진짜인지 헷갈립니다.

**PDF 파싱** — `extract_tables()`는 한 칸에 여러 줄이 들어가면 행이 쪼개지고 글자가 잘려서 좌표로 직접 읽었습니다. 세 가지가 함정이었습니다: ① 칸 경계를 `+30pt`로 어림했더니 낱말이 통째로 빠져 제목이 잘렸습니다(표의 세로선을 쓰니 해결) ② 4월 칸이 우상단 개정일 `2026. 4. 1.` 과 세로로 겹쳐 크롭 텍스트가 `2026. 4. 1. 4월` 이 되는 바람에 **4월 한 달이 통째로 누락**됐습니다(맨 뒤 `N월`을 쓰도록 수정) ③ 칸 폭에 안 맞아 접힌 제목이 두 일정으로 갈렸습니다(줄 끝이 `및`·`,` 이거나 괄호가 안 닫혔거나, 다음 줄이 `(` 로 시작하면 이어 붙임).
- **분류는 색이 아니라 글자로 합니다.** 색은 검증용으로만 뽑아, 분류와 어긋나는 건을 보고하게 했습니다. 서식이 조금만 바뀌어도 조용히 어긋나는 걸 막으려는 것입니다.
- 년도는 문서에 없어서 진행 순서로 정하고(12월 → 1월이면 해가 바뀜), **PDF에 적힌 요일과 계산한 요일을 대조**해 검증합니다 — 13개월 299건 전부 일치했습니다.

**일정 모델** — `owner_id` 가 비면 공용, 차 있으면 개인입니다. 공개 범위 컬럼을 따로 두지 않은 이유는 지금 경우의 수가 둘뿐이어서입니다. 남의 개인 일정을 건드리려 하면 403이 아니라 **404**를 돌려줍니다 — 403이면 그런 일정이 있다는 사실이 새어 나갑니다.
- **반복은 규칙이 아니라 실제 행으로 펼칩니다**(`series_id` 로 묶음). 규칙으로 두면 조회할 때마다 펼쳐야 하고 한 회차만 빼기가 어려운데, 행이면 그냥 삭제입니다.
- `source='pdf'` 만 재적재 때 갈아끼웁니다. 개정판이 나와도 사람이 넣은 일정과 개인 일정은 남습니다.

**달력 화면** — 격자에는 **그날 시작하는** 일정만 적고 걸쳐 있는 건 `⋯ N` 으로 접습니다. 처음엔 걸치는 날마다 다 적었는데 `교직원 건강검진(~11.30)` 하나가 열 달을 도배해 못 쓸 지경이었습니다. 주차는 `N주차 종료` 표지에서 거꾸로 세워 매주 일요일 칸에 표시합니다.

## 2026-07-30 — 4층 구조 정리에 문서·프론트 맞추기

- 변경 파일: `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/CLAUDE.md`, `frontend/component-guide*.md`, `frontend/src/components/molecules/PageHeader.guide.md`, `frontend/src/pages/StudentsPage.tsx` (삭제), `frontend/src/pages/TeachersPage.tsx` (삭제), `frontend/src/components/atoms/RetroFeatureTag.tsx` (삭제)
- 요약: DB를 4층으로 쪼개고 인증을 줄이는 동안 문서와 프론트가 옛 구조를 그대로 설명하고 있었습니다. 실제 코드와 대조해 맞췄습니다.
- 문서가 틀렸던 곳: `models.py` "6개 테이블"(실제 12), 없는 `SubjectAlias` 서술, 없는 `PUT /admin/subjects/{subject}/aliases`, `CourseGrade`가 `course (FK→Course.name)`(실제 `course_id`), `User` 박스에 `email`·`stu_id` 누락, `GET /` 응답 예시가 `aliases`를 반환하는 옛 모양.
- 어디서도 import되지 않는 파일 3개를 지웠습니다 — `StudentsPage`·`TeachersPage`는 Browse로 합쳐진 뒤 남았고, `RetroFeatureTag`는 `PageHeader`가 우상단 태그를 그리지 않게 바뀌면서 죽었습니다. `PageHeader.guide.md`도 없는 `tag` prop을 설명하고 있어 다시 썼습니다.
- lint 에러는 61 → 58로만 줄었습니다. 남은 53개 중 40개가 `searchEngine.ts`의 `any`라, 고아 파일과는 별개 부채입니다.

## 2026-07-30 — 배포 가이드를 실제 서버에 맞춤

- 변경 파일: `deploy-guide.md`
- 요약: 문서가 옛 프로젝트(`visualfailfinder`) 기준으로 남아 있었습니다. 적힌 API 도메인 `classes_api.bsiku.dev`는 **호스트명에 밑줄을 못 써서 아예 존재하지 않는 주소**였고(실제는 `classesapi.bsiku.dev`), 프론트도 rsync가 아니라 Netlify 자동 배포입니다.
- 실제 값으로 맞췄습니다 — 경로 `/srv/ksa-class-finder`, 서비스 `class-explorer.service`, HTTPS는 Cloudflare가 앞단에서 처리하고 nginx는 80으로만 받습니다. 평소 배포 절차를 맨 위로 올렸습니다.

## 2026-07-30 — 학기 선택을 드롭다운으로

- 변경 파일: `frontend/src/components/TermSwitcher.tsx`, `frontend/component-guide.md`
- 요약: 학기 버튼을 나란히 늘어놓던 걸 접었습니다. 학기가 쌓일수록(지금 6개) 네비게이션을 다 잡아먹었고, 모바일에서는 로고와 부딪혔습니다.
- 바깥 클릭·Esc로 닫습니다. 고른 항목은 `bg-black text-white` — 디자인 가이드의 "선택됨" 규칙 그대로입니다.

## 2026-07-30 — 구글 계정은 학번 확인 전용, 로그인은 아이디·비밀번호만

- 변경 파일: `backend/auth_router.py`, `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/src/pages/LoginPage.tsx`, `frontend/CLAUDE.md`, `frontend/component-guide.md`
- 요약: 아는 사람만 쓰는 서비스라 스스로 계정을 만드는 길을 닫았습니다. `POST /auth/google`을 지웠습니다 — 학교 구글 계정만 있으면 누구나 계정이 생기던 자리라 그대로 두면 초대제가 아니게 됩니다. 로그인 화면도 탭을 걷어내고 아이디·비밀번호 하나로 되돌렸습니다.
- `POST /auth/link-student`도 함께 지웠습니다. 구글이 대체한 옛 학번 인증인데, 남겨 두면 이름만 맞히면 학번을 가져갈 수 있어 구글 확인을 우회하는 길이 됩니다.
- 구글은 이제 `POST /auth/link-google` 한 곳에서만 씁니다. 이메일이 곧 학번이라(`25-059@ksa.hs.kr`) 한 번 거치면 신원이 정해지고, `email`이 빈 계정은 **닫을 수 없는 확인 창**을 만납니다.

## 2026-07-30 — 학교 구글 계정으로 학번 확인

- 변경 파일: `backend/models.py`, `backend/migrations.py`, `backend/auth_router.py`, `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/src/components/GoogleLoginButton.tsx` (신규), `frontend/src/components/GoogleLinkModal.tsx` (신규), `frontend/src/components/LinkStudentModal.tsx` (삭제), `frontend/src/pages/LoginPage.tsx`, `frontend/src/pages/ZamongPage.tsx`, `frontend/src/App.tsx`, `frontend/vite.config.ts`, `frontend/.env`
- 요약: 학교 계정 이메일이 곧 학번이라(`25-059@ksa.hs.kr`) 구글을 한 번 거치면 신원과 학번이 함께 정해집니다. 학번·이름을 손으로 받던 창은 없앴습니다.
- 토큰 검증은 구글의 `tokeninfo`로 합니다. JWT 라이브러리를 하나 더 들이는 대신 왕복 한 번을 택했고 — 계정마다 한 번뿐이라 — `aud`와 이메일 인증 여부는 직접 확인합니다. 둘 중 하나라도 빠뜨리면 다른 앱용 토큰으로 남의 학번을 가져갈 수 있습니다.
- `email`이 빈 계정은 로그인 후 **닫을 수 없는 확인 창**을 만납니다. 누구 계정인지 모르면 이수 기록을 남길 수 없어서, 확인 전에는 앱 화면을 아예 렌더링하지 않습니다. 학번이 이미 정해진 계정은 구글 계정의 학번과 같아야 합니다.
- 구글 버튼은 iframe이라 안쪽을 손댈 수 없어, **생김새는 인풋과 같게 우리가 그리고 구글 버튼은 투명하게 그 위에 겹쳤습니다.** 눌리는 건 구글의 진짜 버튼이라 ID 토큰 흐름과 서버 검증은 그대로입니다. 구글 버튼이 박스를 다 덮지 못해서(높이 44px 고정, `margin: -2px`, 폭 400px 상한) `transform`으로 넉넉히 늘린 뒤 박스 경계로 잘라냅니다 — `transform`은 클릭 판정도 같이 늘려 주니 박스 안 어디를 눌러도 들어갑니다.
- 이 버튼만 hover 때 그림자를 숨기지 않습니다. 포인터가 iframe 위에 있으면 부모 문서에 `:hover`가 아예 잡히지 않아서, 인풋처럼 focus 때만 눌립니다. 커서 모양은 구글 버튼이 직접 바꿔 줍니다.
- dev 서버 포트를 **5188**로 고정했습니다. 구글 OAuth 허용 origin 에 등록된 주소라 바꾸면 확인이 막힙니다.

## 2026-07-30 — 교육과정 그래프를 Browse로, Plan을 Zamong으로

- 변경 파일: `frontend/src/components/CourseGraph.tsx` (신규), `frontend/src/lib/curriculum.ts`, `frontend/src/pages/BrowsePage.tsx`, `frontend/src/pages/ZamongPage.tsx` (PlanPage 개명), `frontend/src/App.tsx`, `frontend/src/components/Sidebar.tsx`, `frontend/src/components/BottomNav.tsx`, 가이드 문서
- 요약: Plan 안에 인라인으로 있던 선수관계 그래프를 `CourseGraph` 컴포넌트로 빼고 Browse에 Courses 탭으로 얹었습니다. Plan은 Zamong으로 이름을 바꿨고 라우트도 `/zamong`입니다.
- **전체 보기 추가**: 학과를 가로 레인으로 늘어놓고 가로축을 선수 깊이로 잡았습니다. 왼쪽이 선수 없는 과목, 오른쪽으로 갈수록 쌓아 올린 과목이고, 학과를 가로지르는 선이 융합 과목의 타 학과 선수를 드러냅니다. 학과별 그래프에서는 볼 수 없던 흐름입니다.
- **선택 강조**: 과목을 누르면 그 과목과 직접 이어진 것만 남고 나머지는 흐려집니다. 선택한 과목과 연결선은 핑크입니다. 빈 곳을 누르면 풀립니다.
- Browse의 그래프는 **학번 없이도** 보입니다 — 교육과정 구조를 훑는 건 개인 이수 현황과 별개니까요. 이수 상태를 넘기지 않으면 `CourseGraph`가 색을 칠하지 않습니다.

## 2026-07-30 — 과목 4층 구조 분리

- 변경 파일: `backend/models.py`, `backend/migrations.py`, `backend/subject_names.py` (신규), `backend/parser_run.py`, `backend/import_curriculum.py`, `backend/main.py`, `backend/curriculum_router.py`, `backend/admin_router.py`, `backend/import_credits.py` (삭제), `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/src/types/index.ts`, `frontend/src/lib/curriculum.ts`, `frontend/src/lib/searchEngine.ts`, `frontend/src/pages/PlanPage.tsx`, `frontend/src/pages/TradePage.tsx`, `frontend/src/pages/AdminPage.tsx`, `frontend/src/App.tsx`, 가이드 문서
- 요약: `classes.subject` 문자열 하나가 표시 이름과 과목 식별자를 겸하던 것을 `Department → Course → Subject → Class` 네 층으로 나눴습니다. `Course`는 언어·표기를 벗겨낸 과목 정체성이고 `Subject`는 KEIS 개설명입니다.
- **(EC)는 English Class**였습니다. KEIS는 영어강의와 한국어강의를 별개로 개설하고 실제로 19쌍이 함께 열리는데, 카탈로그에는 EC 표기가 4개뿐이라 18건이 엉뚱하게 이어져 있었습니다. 이제 `Subject.is_ec`가 언어를 담고 둘은 같은 `Course`를 가리킵니다 — 학점·선수관계는 하나, 개설·수강은 별개.
- 로마숫자 과목(`물리학및실험Ⅰ`)은 외국인 전형 과목이라 `물리학및실험1`과 합치지 않습니다. 수강생이 100% 외국인 학번인 것을 확인했습니다.
- `subject_credits` 폐기 — 학점은 `Subject → Course`로 조회합니다. `subject_aliases`도 폐기 — 98개 중 89개가 유사도 검색으로 이미 잡히고 나머지는 음차(칼큘→Calculus)였습니다.
- EC 졸업 요건을 수강 이력 기준으로 바꿨습니다. 워크북 표기(4개)가 아니라 실제로 영어강의를 들었는지로 셉니다.
- 마이그레이션 검증: 분반 1,539 · 수강 19,801 · 시간 4,224행 전부 보존, 고아 레코드 0건, `name_raw`로 원문 100% 복원 가능. 교육과정 연결이 학기별로 84~88 → 91~98개로 늘었습니다.

## 2026-07-30 — 한 학번 = 한 계정

- 변경 파일: `backend/models.py`, `backend/migrations.py`, `backend/auth_router.py`, `backend/CLAUDE.md`, `backend/api-guide.md`
- 요약: 이미 다른 계정이 등록한 학번은 쓸 수 없게 했습니다. 라우터에 검사가 있었지만 애플리케이션 레벨뿐이라, 두 요청이 동시에 들어오면 검사와 커밋 사이를 파고들어 둘 다 통과할 수 있었습니다. `users.stu_id`에 유니크 인덱스를 걸고 `IntegrityError`를 409로 바꿉니다. NULL은 유니크 검사에서 빠지므로 미등록 계정은 얼마든지 공존합니다.
- 마이그레이션은 기존 인덱스를 유니크로 재생성하며, 이미 중복이 있으면 먼저 등록한 계정만 남기고 나머지를 비웁니다.
- 검증: 순차 등록 시 두 번째 409, 동시 등록 시 한쪽만 200이고 DB에 하나만 남는 것까지 확인.

## 2026-07-30 — 계정에 학번 등록

- 변경 파일: `backend/models.py`, `backend/migrations.py`, `backend/database.py`, `backend/auth_router.py`, `backend/curriculum_router.py`, `backend/main.py`, `backend/create_user.py`, `backend/import_credits.py`, `backend/import_curriculum.py`, `backend/parser_run.py`, `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/src/components/LinkStudentModal.tsx` (신규), `frontend/src/pages/PlanPage.tsx`, `frontend/src/pages/TradePage.tsx`, `frontend/src/App.tsx`, `frontend/CLAUDE.md`, `frontend/component-guide.md`, `frontend/src/lib/curriculum.guide.md`
- 요약: `User.stu_id`를 두어 계정이 누구인지 정하게 했습니다. 본인이 **학번과 이름을 함께** 입력해 등록하며, 둘 중 하나만 맞아도 반려합니다 — 아무 학번이나 골라 남의 이름으로 성적을 기록해 두는 걸 막으려는 검증입니다. 어느 쪽이 틀렸는지는 알려주지 않고, 이미 다른 계정이 쓰는 학번이면 409입니다.
- 덕분에 `CourseGrade.stu_id`가 사라졌습니다. 이수 기록은 본인 것만 남고 `/curriculum/grades`도 학번을 받지 않습니다. Plan 화면에서 학생 선택 UI를 걷어냈고, Trade는 본인이 기본값이되 다른 학생도 고를 수 있게 뒀습니다(친구 계획을 봐주는 쓰임).
- 미등록 계정은 Plan에서 `LinkStudentModal`로 안내합니다. 학번을 다른 학번으로 바꾸면 이전 사람의 이수 기록을 지웁니다.
- 함께 고친 것: CLI 스크립트들이 `create_all`만 하고 마이그레이션을 돌리지 않아, 서버에서 계정 생성을 먼저 하면 `no such column: users.stu_id`로 죽었습니다. `database.init_schema()`로 모아 앱·CLI가 같은 경로를 쓰게 했습니다.
- 검증: 이름 불일치·없는 학번·한쪽만 일치를 전부 400으로 막고, 띄어쓰기 차이는 허용. 중복 학번 409, 학번 변경 시 기존 성적 삭제, 옛 `course_grades.stu_id` 제거 마이그레이션(본인 것만 이관, 주인 불명은 폐기)까지 확인.

## 2026-07-30 — 성적 입력 + 계정별 상태 저장

- 변경 파일: `backend/models.py`, `backend/state_router.py` (신규), `backend/curriculum_router.py`, `backend/main.py`, `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/src/lib/userState.ts` (신규), `frontend/src/lib/curriculum.ts`, `frontend/src/lib/curriculum.guide.md`, `frontend/src/pages/PlanPage.tsx`, `frontend/src/pages/TradePage.tsx`, `frontend/CLAUDE.md`, `frontend/component-guide.md`
- 요약: 과목별 평어를 입력해 평점(4.3 만점)을 내고, 작업 중인 계획을 기기가 아니라 계정에 저장하도록 바꿨습니다. `UserState`(화면 상태 JSON)와 `CourseGrade`(이수+평어) 두 테이블을 추가했고, 예전 기기에 남은 localStorage 값은 처음 열 때 한 번 서버로 옮기고 지웁니다.
- 평점: 평어를 넣은 과목만 셈에 넣고 미입력은 제외합니다(0점 아님). P/F 과목은 등급이 아니라 통과 여부라 평어가 있어도 뺍니다. 전체 평점과 자연계열 평점을 따로 냅니다. 환산표는 서버가 `grade_points`로 내려줍니다.
- 이수 기록과 평어를 한 테이블로 합쳤습니다 — 행이 있으면 이수, `grade`는 선택. 덕분에 수집 이전 학기를 채우는 수단과 성적 입력이 하나로 정리됩니다.
- 고친 버그: PlanPage가 불러오기 전에 저장을 돌려 서버 상태를 `{stuId: null}`로 덮어쓰고 있었습니다. 실사용이면 계획이 통째로 날아갔을 문제라 두 페이지 모두 `restored` 가드를 넣었습니다.
- 검증: 계정 격리(같은 학번도 계정이 다르면 안 보임), 옛 localStorage 이관, 평점 계산(A+·2 + B0·2 + A0·2 = 3.77 → 미적분학1 A0 추가 시 전체 3.86·자연 4.00), 새로고침 후 유지까지 확인.

## 2026-07-30 — Sweet Zamong 통합 (교육과정 이수 현황)

- 변경 파일: `backend/models.py`, `backend/curriculum_router.py` (신규), `backend/build_curriculum_seed.py` (신규), `backend/import_curriculum.py` (신규), `backend/curriculum_seed.json` (신규), `backend/main.py`, `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/src/lib/curriculum.ts` (신규), `frontend/src/lib/curriculum.guide.md` (신규), `frontend/src/pages/PlanPage.tsx` (신규), `frontend/src/App.tsx`, `frontend/src/components/Sidebar.tsx`, `frontend/src/components/BottomNav.tsx`, `frontend/CLAUDE.md`, `frontend/component-guide.md`, `CLAUDE.md`
- 요약: SweetZamong의 교육과정 도메인을 7번째 기능 `/plan`으로 들여왔습니다. 버전 관리(브랜치·커밋)는 가져오지 않았고, 기존 `SubjectCredit`을 확장하는 형태로 `Course`(145과목)와 `CoursePrereq`(선수관계 117개)를 추가했습니다. `Class.subject → SubjectCredit.matched_name → Course.name`으로 이어져 실제 수강 이력이 교육과정에 그대로 붙습니다.
- 선수관계 파싱: Zamong 워크북에 선수관계가 **데이터가 아니라 셀 배경색으로 그린 그림**으로 들어 있었습니다. 회색(D2D2D6) 셀을 4방향으로 이어 붙여 선 한 가닥을 만들고, 그 좌우에 닿은 과목 상자를 엮는 방식으로 읽었습니다. 융합 시트만 표기법이 달라(선수 과목명을 글자로 적고 "OR"로 택일 표시) 별도 규칙을 씁니다. 결과는 `curriculum_seed.json`으로 커밋 — 원본 xlsx(685KB)와 SweetZamong DB는 넣지 않습니다.
- 이수 추정: 수집 학기가 2026-1부터라 그 이전이 비어 있어, 그냥 두면 미적분학1을 이수했는데 선수인 수학1·수학2가 잠긴 채 남습니다. 이수 과목에서 선수 체인을 거슬러 올라가 채우되(택일은 제외) 추정분은 학점에 넣지 않고 점선으로 구분합니다. 눌러서 확정하면 합산됩니다.
- 검증: 25-059 기준 2026-1·2026-2 자동 반영(48학점, AP 13), 일반화학1 확정 시 52학점·자연 32학점으로 갱신되고 화학과예술이 열리는 것까지 확인.

## 2026-07-29 — 과목 학점 도입

- 변경 파일: `backend/models.py`, `backend/import_credits.py` (신규), `backend/main.py`, `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/src/lib/tradeEngine.ts`, `frontend/src/pages/TradePage.tsx`, `frontend/src/types/index.ts`
- 요약: KEIS restapi에 학점 엔드포인트가 없어(schedule 외 전부 404) SweetZamong `courses` 테이블을 정본으로 삼아 `SubjectCredit`에 임포트. 과목명 표기가 달라(`미적분학2(EC)(Calculus2(EC))` vs `미적분학2(EC)`) 뒤쪽 영문 괄호를 괄호 균형 기준으로 떼어낸 뒤 EC 태그를 붙였다 뗐다 하며 매칭 — 139과목 중 138개 매칭(실패: 한국정치사(조선붕당정치)). `GET /` 응답에 `credits`/`is_ec`/`is_pf` 추가. 트레이드 화면에 현재 학점과 계획 적용 후 학점(증감 포함), 과목 카드별 학점, 조합별 학점을 표시. 학점 미등록 과목은 합계에서 빠지며 그 사실을 화면에 알립니다.
- 후속: 분반 교환 상대를 구하는 글 템플릿 추가. 이동 목표를 정하면 카드에 글이 뜨고 `CopyButton`으로 복사합니다 (Trade Partners 카드에도 동일). 시간은 `월67 목9`처럼 붙여 쓰되 10교시 이상이 섞이면 `화10,11`로 나눕니다.
- 후속: 배포 직후 학점이 0으로 보이던 문제 — 브라우저가 1시간 캐시로 학점 필드 없는 옛 응답을 들고 있었습니다. 캐시 payload에 `v`(CACHE_VERSION)를 넣어 스키마가 바뀌면 자동 무효화되게 했습니다. API 응답에 필드를 추가할 때는 이 값을 올려야 합니다.
- 함께 수정: `Opened by Drop` 목록이 드랍만 반영하고 이동·추가를 무시하던 문제 — `effectiveSchedule` 기준으로 변경.

## 2026-07-29 — 수강 변경 탐색(Trade) 및 유사도 검색

- 변경 파일: `frontend/src/lib/tradeEngine.ts` (신규), `frontend/src/lib/features.ts` (신규), `frontend/src/pages/TradePage.tsx` (신규), `frontend/src/components/SectionsTimetable.tsx` (신규), `frontend/src/lib/searchEngine.ts`, `frontend/src/App.tsx`, `frontend/src/components/Sidebar.tsx`, `frontend/src/components/BottomNav.tsx`, `frontend/src/types/index.ts`
- 요약: 분반 이동·드랍·추가신청을 한 화면에서 처리하는 `/trade` 추가. 과목마다 유지/이동/드랍을 지정하고 추가 과목은 분반까지 고정할 수 있으며, 시간 슬롯이 겹치지 않는 조합을 백트래킹으로 찾아 나열합니다(상한 200, 변경 건수 오름차순). 조합을 누르면 적용 후 시간표를 미리 보여줍니다. 드랍한 과목은 빨간색으로 표시되고 목록 하단으로 내려가며, 드랍으로 비는 시간에 들어갈 수 있는 과목을 따로 제시합니다. 과목 카드를 펼치면 해당 과목 전 분반을 한 그리드에서 비교할 수 있고 충돌하는 분반은 회색 처리됩니다. 검색은 `fuzzyMatch`를 도입해 별칭 등록 없이 "정3"으로 "정보과학3"을 찾습니다 — 서브시퀀스 매칭에 구간 길이 제약(`검색어×3+2`)을 걸어 오탐을 억제했습니다. 기능 전체는 `TRADE_FEATURE.enabled` 한 줄로 끌 수 있고 2026-2에서만 노출됩니다. 정원 정보가 없어 시간 충돌만 판정합니다.
- 후속 10: 충돌이 조용히 지나가던 문제 수정 — (1) 충돌 판정 기준을 `effectiveSchedule`(드랍 제외 + 이동 반영 + 확정된 추가 포함)로 통일해, 이동끼리·추가끼리 부딪히는 경우도 잡습니다. (2) 색 우선순위에서 파랑(이동)이 주황(충돌)을 덮어 가리던 것을 충돌 우선으로. (3) "자동"으로 맡긴 항목이 있으면 첫 조합을 자동으로 미리보기 — 자동인데 시간표가 그대로여서 아무 일도 안 일어난 것처럼 보였습니다.
- 후속 9: 색 규칙 정리 — 같은 의미에 다른 색을 쓰던 곳을 통일(분반 그리드와 배지 목록이 "갈 수 있는 분반"을 각각 시안·초록으로 칠하고 있었음). 정보 표시는 현재=시안·가능=초록·충돌=주황으로 공유하고, 이동은 `retro-accent5` 파랑으로 카드 배경까지 부여. accent1(청록)은 크림 배경 위에서 연두로 보여 추가(초록)와 겹쳤습니다.
- 후속 8: 시간표에서 이동을 파랑으로 분리(빠짐=핑크 · 이동=파랑 · 추가=초록 · 충돌=주황). 선택된 버튼이 카드 색과 무관하게 검정이던 것을 카드 색(초록·시안·핑크)으로 맞추고, 이동 카드 배경은 흰색으로 — 시안 반투명이 크림 배경 위에서 연두로 보여 추가 카드와 헷갈렸습니다.
- 후속 7: 분반 이동을 직접 고를 수 있게 개선 — "이동"이 조합 탐색 스위치 역할만 해서 눌러도 반응이 없어 보이던 문제. 이제 이동을 켜면 분반 버튼(자동/1/2/…)이 나타나고, 고르면 시간표에 즉시 반영됩니다(초록=이동 후, 주황=밀려나는 과목). `PlanRequest.moveTargets`로 탐색 후보를 고정 분반으로 제한합니다.
- 후속 6: 분반 검색에 콤마 목록 지원 — `체육4/2,3`으로 여러 분반을 한 번에. 분반 여러 개인 카드를 누르면 `생활체육2/1,4`로 검색됩니다. About 가이드북에 유사도·분반 검색 안내 추가.
- 후속 5: 통합 뷰에서 과목·분반을 누르면 `체육4(4)/4`처럼 잘못된 검색어가 만들어지던 버그 수정 — `formatSubjectWithSection` 출력의 뒤쪽 괄호만 분반으로 파싱하도록 변경(과목명의 숫자·괄호를 분반으로 오인하지 않게). 분반이 여럿이면 과목명으로만 검색합니다.
- 후속 4: 드랍한 과목이 충돌 판정에서 빠지지 않던 버그 수정 — `activeSchedule`(드랍 제외 시간표)을 만들어 추가 후보·분반 배지 계산에 일괄 적용. 카드 안 버튼은 텍스트까지 카드 색(핑크/초록/주황)을 따르게 통일.
- 후속 3: 충돌 분반을 회색이 아니라 핑크로 칠하고(선택 불가처럼 보이던 문제), 충돌을 감수하고 분반을 고정하면 부딪히는 기존 과목을 시간표에서 주황으로 표시. 추가 과목이 들어갈 분반이 하나도 없을 때 카드에 이유를 적고, 분반 버튼 툴팁에 충돌 과목명을 넣었습니다. 추가 과목 카드는 목록 맨 위로.
- 후속 2: 계획을 localStorage에 자동 저장·복원. 교환 상대 찾기 추가 — 상대가 내 목표 분반을 듣고 있고 내 자리로 와도 충돌이 없는 학생만 골라, 조합 선택 시 목록으로 보여줍니다(`buildStudentIndex`로 학생별 시간표를 한 번에 만들어 재조회를 피함). 분반 목록에도 분반별 교환 상대 수를 표시. 검색은 분반 지정(`체4/5`)에도 유사도를 적용. 분반 그리드는 호버 시 하단 바에 상세를 띄우고 내 분반만 `retro-accent1`로 진하게, 카드·버튼 테두리를 검정으로 통일, 학기 전환 버튼을 Logout 버튼과 같은 스타일로 맞췄습니다.
- 후속: 조합을 고르기 전에도 지정한 드랍·추가가 왼쪽 시간표에 즉시 반영되도록 수정. 빠지는 과목은 빨강, 들어오는 과목은 초록으로 칠하고 범례를 붙였습니다(`TimetableGrid`에 `colorFor` prop 추가 — 기존 사용처는 영향 없음). 빠지는 과목을 지우지 않고 남겨둬 어느 시간이 비는지 보이게 했고, 분반을 정하지 않은 추가 과목은 그리지 않습니다.

## 2026-07-29 — KEIS API 전환 및 학기별 데이터 구조 도입

- 변경 파일: `backend/parser.py`, `backend/parser_run.py`, `backend/models.py`, `backend/migrations.py` (신규), `backend/terms.py` (신규), `backend/main.py`, `backend/admin_router.py`, `frontend/src/App.tsx`, `frontend/src/components/Navigation.tsx`, `frontend/src/components/TermSwitcher.tsx` (신규), `frontend/src/types/index.ts`
- 요약: 데이터 소스를 `api.ksain.net`에서 `keis.ksa.hs.kr/restapi/v1/schedule/{stuId}/{year}/{semester}`로 전환. 응답 포맷은 동일하나 교시를 배열 인덱스가 아닌 `kyosi` 필드로 읽도록 파서 재작성(12교시 대응, 강의실 `"배정중"` 처리, 대표 강의실 선정). `Class`에 `year`/`semester`를 추가해 학기별 데이터를 한 DB에 공존시키고, `UniqueConstraint`를 `(subject, section, teacher, year, semester)`로 확장 — SQLite 제약 변경 불가로 `migrations.py`에서 테이블 재생성(id 보존해 FK 유지, 기존 데이터는 2026-1로 지정). `parser_run.py`는 학기 단위 원자적 교체 방식으로 재작성(전원 수집 후 반영, 재시도 2회, 요청 실패 과반 시 중단, `students.txt` 파괴적 덮어쓰기를 `--prune` 옵션으로 분리). `GET /`에 `year`/`semester` 쿼리와 `GET /terms` 추가, admin의 teachers/subjects도 학기 필터. 프론트는 `TermSwitcher`로 학기 전환하며 캐시 키를 학기별로 분리하고 선택 학기를 `ksa_selected_term`에 보존. 2026-2 수집 결과 342명 동기화(237분반), 2026-1(357명·247분반) 보존 확인.

## 2026-03-17 — 효율성 수정 3건

- 변경 파일: `backend/auth_router.py`, `frontend/src/lib/searchEngine.ts`, `frontend/src/pages/AnalysisPage.tsx`
- 요약: (1) Rate Limiter `_maybe_cleanup()` 추가 — 5분 주기로 만료된 IP 항목 정리해 메모리 누수 방지 (threading.Lock 이중 체크). (2) `getChosung()` 모듈 레벨 `_chosungCache` Map 추가 — 동일 문자열 재계산 제거. (3) AnalysisPage `allClassesData` 순회 5개 useMemo → `periodStats`·`subjectStats` 2개로 통합 (주당 교시 분포 + 연도별, 수강 과목 수 분포 + 연도별 + 과목별 연도 분포 각 1회 순회).

## 2026-03-17 — 배포 설정 및 가이드 작성

- 변경 파일: `frontend/src/lib/api.ts`, `deploy-guide.md` (신규)
- 요약: api.ts 주석에 실제 배포 URL 반영 (classes_api.bsiku.dev). nginx + certbot + systemd 기반 배포 가이드 작성.

## 2026-03-17 — 보안 취약점 추가 수정 (SEC-T01~T04, T06, T13, T14)

- 변경 파일: `backend/auth_router.py`, `backend/main.py`
- 요약: 타이밍 공격 방지(dummy bcrypt), Rate Limit IP 실제 추출(X-Forwarded-For), CSP 헤더 추가, Cache-Control(no-store), Retry-After 헤더, CORS allow_methods/headers 최소화.

## 2026-03-17 — 가이드 문서 전체 정비

- 변경 파일: `CLAUDE.md`, `backend/CLAUDE.md`, `backend/api-guide.md`, `frontend/CLAUDE.md`, `frontend/component-guide.md`, `frontend/src/App.guide.md`, `frontend/src/pages/AnalysisPage.guide.md`, `frontend/src/pages/RoomsPage.guide.md`, `frontend/src/components/SearchResultDisplay.guide.md`
- 요약: 현재 코드 상태 기준으로 모든 가이드 문서 최신화. 주요 변경: src/utils.ts→lib/utils.ts 경로 수정, 페이지 목록(BrowsePage/SettingsPage→/about), 라우팅 테이블, React.lazy 코드스플리팅, 보안 항목(headers/rate-limit/validation), API 응답에 aliases·is_admin 필드 추가, AnalysisPage 충돌감지·teacherLoadDistribution, RoomsPage onRoomSearch prop 반영.

## 2026-03-17 — 로컬 HTTPS 설정 (mkcert + Vite)

- 변경 파일: `frontend/vite.config.ts`, `frontend/.gitignore`, `.gitignore`
- 요약: mkcert로 localhost 인증서 생성, vite.config.ts에 https 옵션 + proxy target https 전환, .gitignore에 *.pem 추가.

## 2026-03-17 — 보안 감사 및 수정 (SEC-01 ~ SEC-15)

- 변경 파일: `backend/main.py`, `backend/auth_router.py`, `backend/admin_router.py`, `requirements.txt`
- 요약: 15개 보안 항목 전수 검사. 실제 수정된 취약점 5건:
  - [SEC-07] 입력값 검증: 모든 Pydantic 스키마에 max_length, pattern, Literal 검증 추가
  - [SEC-09] stderr 노출: sync 실패 시 내부 에러를 클라이언트에 노출하던 것 → server log only
  - [SEC-10] 보안 헤더: SecurityHeadersMiddleware 추가 (X-Content-Type-Options, X-Frame-Options 등)
  - [SEC-11] Rate Limiting: /auth/login IP당 60초 10회 제한 구현 (429 반환, 성공 시 초기화)
  - [SEC-14] Dependency: requirements.txt 최소 버전 고정 (bcrypt>=4.0.0 등)
  - 안전 판정 10건: SQL Injection(ORM), Command Injection(하드코딩), XSS(React), CSRF(Bearer), 인증우회, IDOR, 파일업로드(없음), 비즈니스로직, Race Condition(SQLite), 에러처리

## 2026-03-17 — 교사 부하 시각화, 충돌 감지, 번들 스플리팅

- 변경 파일: `frontend/src/pages/AnalysisPage.tsx`, `frontend/src/components/SearchResultDisplay.tsx`, `frontend/src/App.tsx`, `frontend/vite.config.ts`
- 요약: Analysis에 Teacher Load Distribution 아코디언 추가 (교사별 담당 교시 수 분포 차트). 비교 그리드 충돌 시간대(주황) 강조 + 충돌 카운트 배지 표시. 논리 학생 검색 시 SearchResultDisplay에 충돌 경고 블록 추가. App.tsx 전체 페이지 React.lazy 전환 + Suspense 래퍼, vite.config에 manualChunks로 HeroUI/vendor 청크 분리.

## 2026-03-17 — 빈 교실 → 검색 연동

- 변경 파일: `frontend/src/pages/RoomsPage.tsx`, `frontend/src/App.tsx`
- 요약: Rooms 페이지에서 교실 선택 시 나타나는 "Search" 버튼 클릭으로 해당 강의실 room: 검색으로 이동. `onRoomSearch` prop 추가 및 App.tsx에서 `handleSearchSelect` 연결.

## 2026-03-17 — Data Management 아코디언 + 가이드북 초성 검색 안내

- 변경 파일: `backend/admin_router.py`, `frontend/src/pages/AdminPage.tsx`, `frontend/src/pages/SettingsPage.tsx`, `backend/CLAUDE.md`
- 요약: Admin에 Data Management 아코디언 추가 (Students/Teachers/Subjects 탭). 학생 이름 인라인 편집, 교사 이름 일괄 변경, Subject Aliases 통합. 가이드북에 초성 검색(ㅈㄱ 등) 설명 추가.

## 2026-03-17 — 과목 별칭(alias) 검색 시스템

- 변경 파일: `backend/models.py`, `backend/main.py`, `backend/admin_router.py`, `frontend/src/lib/searchEngine.ts`, `frontend/src/pages/AdminPage.tsx`, `backend/CLAUDE.md`
- 요약: `SubjectAlias` 테이블 추가 (subject + alias UniqueConstraint). GET / 응답에 `aliases` 필드 포함. Admin에 `GET/PUT /admin/subjects` 엔드포인트 추가. searchEngine의 sectionPool에 aliases 포함. AdminPage에 Subject Aliases 아코디언 섹션 추가 (과목명 필터 + 인라인 편집 UI).

## 2026-03-17 — Browse/Settings 탭 재편 + 가이드북

- 변경 파일: `pages/BrowsePage.tsx` (신규), `pages/SettingsPage.tsx` (신규), `pages/SearchPage.tsx`, `App.tsx`, `components/Sidebar.tsx`, `components/BottomNav.tsx`
- 요약: Students+Teachers 통합 → `/browse` (모드 토글 + 학년 필터). `/settings`에 기능 가이드북(검색 문법, prefix, 논리 연산자, Browse, Rooms 설명) + About 추가. SearchPage Help 버튼 제거. 사이드바/하단 메뉴 Search/Rooms/Analysis/Browse/Settings로 재편.

## 2026-03-17 — Admin 대시보드 구현

- 변경 파일: `backend/admin_router.py` (신규), `backend/models.py`, `backend/auth.py`, `backend/auth_router.py`, `backend/main.py`, `backend/create_user.py`, `pages/AdminPage.tsx` (신규), `App.tsx`, `components/Navigation.tsx`, `components/Sidebar.tsx`
- 요약: 관리자 전용 API(`/admin/*`) + UI 구현. User에 `is_admin`, Session에 `ip_address` 추가. 서버 기동 시 컬럼 자동 마이그레이션. Admin 유저는 Sidebar에 Admin 메뉴 노출, `/admin` 라우트 접근 가능. 기능: 유저 생성/삭제/admin 권한 토글, 세션 목록(IP/기기/만료일) + 강제 종료, 데이터 재수집(KSAIN API).

## 2026-03-17 — 배포 환경 분리 (VITE_API_BASE_URL, CORS)

- 변경 파일: `frontend/src/lib/api.ts` (신규), `frontend/.env` (신규), `frontend/src/App.tsx`, `frontend/src/pages/LoginPage.tsx`, `backend/main.py`, `frontend/CLAUDE.md`, `backend/CLAUDE.md`
- 요약: axios 인스턴스(`src/lib/api.ts`) 추가, `VITE_API_BASE_URL` 환경변수로 baseURL 분기(미설정 시 `/api` 프록시). 백엔드에 CORS 미들웨어 추가(`CORS_ORIGINS` 환경변수). Netlify 배포 시 두 환경변수만 설정하면 됨.

## 2026-03-17 — 프론트엔드 로그인 UI 구현

- 변경 파일: `pages/LoginPage.tsx` (신규), `App.tsx`, `components/Navigation.tsx`, `frontend/CLAUDE.md`, `frontend/component-guide.md`, `frontend/src/App.guide.md`
- 요약: 로그인 페이지(전체화면, PC/모바일 대응) 추가. App.tsx에 sessionToken 상태 관리 + handleLogin/handleLogout + 401 자동 로그아웃 처리. Navigation에 Logout 버튼(모바일 아이콘만, PC 텍스트+아이콘) 추가.

## 2026-03-17 — 세션 기반 인증으로 전환 (1계정 1세션)

- 변경 파일: `backend/auth.py`, `backend/auth_router.py`, `backend/models.py`, `backend/api-guide.md`, `backend/CLAUDE.md`, `requirements.txt`
- 요약: JWT 제거 → session_token(랜덤 48바이트) DB 저장 방식으로 전환. 1계정 1세션 강제(로그인 시 기존 세션 전부 삭제), 만료 30일. `/auth/refresh` 엔드포인트 삭제. Session 컬럼 `refresh_token`→`session_token` + `expires_at` 추가. **DB 마이그레이션 필요** (sessions 테이블 드롭 후 재기동).

## 2026-03-17 — JWT 인증 시스템 구현

- 변경 파일: `backend/models.py`, `backend/main.py`, `backend/auth.py` (신규), `backend/auth_router.py` (신규), `backend/create_user.py` (신규), `requirements.txt` (신규), `backend/CLAUDE.md`, `backend/api-guide.md`
- 요약: JWT Access(30분)+Refresh(30일) 기반 인증 추가. User/Session 테이블 신설, 계정당 최대 2세션(초과 시 가장 오래된 것 자동 삭제), `GET /` 인증 보호. `python -m backend.create_user <username> <password>`로 계정 생성.

## 2026-03-17 — Teaching Load / Classroom Utilization 간격 수정

- 변경 파일: `pages/AnalysisPage.tsx`
- 요약: 두 아코디언 감싸는 grid gap-8 → gap-4 md:gap-6, 부모 flex gap과 통일


## 2026-03-17 — 아코디언 내부 더 촘촘하게

- 변경 파일: `SubjectAccordionItem.tsx`, `SectionCard.tsx`
- 요약: 아코디언 내부 패딩 px-4 pb-4 pt-4, space-y-6, teachers mb-4 / SectionCard 타이틀 text-base px-3 py-1, space-y-2.5, 메타 space-y-1.5로 압축


## 2026-03-17 — Analysis 상단 3개 차트 vertical 레이아웃으로 통일

- 변경 파일: `pages/AnalysisPage.tsx`
- 요약: Subjects by Enrollment, Weekly Periods, Subject Count에서 layout="horizontal" 제거 → Teaching Load/Classroom Utilization과 동일한 vertical 디자인 적용


## 2026-03-17 — 전체 UI 패딩/여백 압축

- 변경 파일: `SubjectAccordionItem.tsx`, `SectionCard.tsx`, `molecules/AccordionSection.tsx`, `pages/AnalysisPage.tsx`, `pages/SearchPage.tsx`
- 요약: 아코디언 내부(pt-10 pb-12→pt-5 pb-6, space-y-12→space-y-8, mb-12→mb-6), SectionCard gap-8→gap-4~6, AccordionSection p-6→p-4, AnalysisPage gap-8→gap-4~6, SearchPage mt-6→mt-4


## 2026-03-17 — BarChartRow 모바일 깨짐 수정

- 변경 파일: `components/molecules/BarChartRow.tsx`
- 요약: horizontal 레이아웃에서 `w-56` 고정 라벨이 모바일 화면 폭 초과 → 모바일 flex-col(라벨 위/바 아래), sm+ 에서 기존 가로 레이아웃 유지


## 2026-03-17 — Filter 버튼 학생수 카운트 제거

- 변경 파일: `components/FilterSection.tsx`
- 요약: 필터 버튼에서 `({count})` 제거 → 버튼 폭 축소로 모바일 1줄 배치 개선


## 2026-03-17 — Filter 모바일 1줄 가로스크롤, N명표시중 마진, SearchInput shadow 통일, Feature태그 제거, subtitle 단축, 물음표버튼 우측배치

- 변경 파일: `FilterSection.tsx`, `pages/StudentsPage.tsx`, `pages/TeachersPage.tsx`, `atoms/SearchInput.tsx`, `molecules/PageHeader.tsx`, `pages/SearchPage.tsx`, `pages/AnalysisPage.tsx`, `pages/RoomsPage.tsx`, `pages/StudentsPage.tsx`, `pages/TeachersPage.tsx`
- 요약: Filter 모바일에서 overflow-x-auto 단일행(shrink-0+whitespace-nowrap), N명표시중 -mt-4→-mb-4, SearchInput shadow opacity 0.1→0.2, Feature태그 전부 제거, subtitle 1~2단어 축약, 물음표버튼 plain button 정사각형+우측 고정

## 2026-03-17 — 모바일 반응형 피드백 반영

- 변경 파일: `atoms/SearchInput.tsx`, `pages/SearchPage.tsx`, `components/FilterSection.tsx`, `pages/StudentsPage.tsx`, `components/SubjectAccordionItem.tsx`, `pages/RoomsPage.tsx`, `molecules/AccordionSection.tsx`
- 요약: SearchInput 폰트 크기 축소(placeholder 깨짐 수정), FilterSection 항상 flex-row/Refresh 우측 배치/className prop 추가, SubjectAccordionItem 모바일 flex-col, Rooms 방 버튼 flex-wrap 2줄, AccordionSection 타이틀 폰트 반응형

## 2026-03-17 — 모바일 반응형 디자인 적용

- 변경 파일: `components/BottomNav.tsx` (신규), `App.tsx`, `components/TimetableGrid.tsx`, `components/SearchResultDisplay.tsx`, `components/molecules/PageHeader.tsx`, `pages/RoomsPage.tsx`, `pages/AnalysisPage.tsx`
- 요약: 모바일용 하단 네비게이션(BottomNav) 추가, 시간표 그리드에 overflow-x-auto 적용, PageHeader/SearchResultDisplay 텍스트·패딩 반응형 조정
